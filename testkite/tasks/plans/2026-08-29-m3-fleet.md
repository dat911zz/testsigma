# M3 — Fleet: worker, executor, memory governance, systemd/runnerd Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dựng NỬA RUNNER/FLEET của M3 — worker `apps/runner` zero-credential (claim → chạy chain → báo cáo), executor Playwright 1 context/chain với timeout lồng, memory governance L1–L3 đúng `memory-governance.ts`, quarantine + breaker, artifact ring-buffer/trace/presigned PUT, và fleet systemd (`ts-workers.slice` + `ts-worker@` + `runnerd`) — để lớp lỗi OOM của hệ cũ (blueprint §1) trở thành thứ **không thể viết ra**.

**Architecture:** Worker là **process riêng**, không import `@testkite/core`, nói chuyện với control plane **chỉ qua internal HTTP**; toàn bộ giả định về hợp đồng đó bị cô lập vào **một file duy nhất** `apps/runner/src/control-plane-client.ts` (xem §"Hợp đồng giả định"). Mọi thứ chạm hạ tầng thật (cgroup, Playwright, filesystem, HTTP) đứng sau một interface hẹp có **fake impl** — nhờ vậy toàn bộ logic governance/executor/quarantine test được ở vòng lặp đơn vị, còn phần chỉ chứng minh được trên host thật (cgroup v2 `memory.max`/`memory.events`, `oom_score_adj` âm, PSI, systemd) được đánh dấu tường minh và có gate riêng. Cách ly = tính năng: **một BrowserContext cho một chain**, đóng trong `finally` không ngoại lệ.

**Tech Stack:** Node 22, TypeScript strict (`exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`), NodeNext, pnpm workspace, vitest 3, zod 3, `playwright-core` (chromium-headless-shell + CDP), `@testkite/run-compiler` (kiểu `RunPlan`/`ChainPlan`/`CasePlan`/`StepPlan` + `chainTimeoutSeconds`), `@testkite/verb-kit` (`VerbDefinition`/`OpContext`/`OpResult`), `@testkite/contract` (`RUN_VERDICTS`, `AppError`, `RetryableInfraError`, `FatalInfraError`, `AssertionFailure`), systemd 255 (host), cgroup v2 (host).

**Spec:** `../../../docs/SYSTEM_DESIGN.md` §1 (pháp y OOM hệ cũ — lý do tồn tại của milestone này), §5 (fleet 2 mặt phẳng, 4 tầng trần bộ nhớ, lease/epoch), §5.1 (replay/trace), §5.2 (screenshot ring-buffer + WebP + dedup). Backlog: `../M3-orchestration-fleet.md`. Hằng số đã chốt: `apps/runner/src/memory-governance.ts`.

## Global Constraints

- **Ngôn ngữ:** mọi CODE/TEST/comment/log/message tiếng ANH (CLAUDE.md Luật 4). Gate CI ngôn ngữ hiện quét `testkite/apps/core/src|test`, `packages/*/src`, `tools`, `*.mjs|*.ts` gốc, `scripts` — **Task 1 mở rộng gate sang `testkite/apps/runner/src` và `testkite/apps/runner/test`**.
- TypeScript strict + `exactOptionalPropertyTypes` + `noUncheckedIndexedAccess`. Không `any`, không `!` phi lý.
- **Ảnh API KHÔNG chứa browser** — bất biến từ M1, gate `grep -rE "playwright|puppeteer|chromium" testkite/apps/core/src` phải luôn 0 hit. Browser CHỈ nằm trong image runner (`apps/runner`). Plan này **không** thêm một dòng browser nào vào `apps/core`.
- **Worker zero-credential:** worker không có credential team, không có DB, không có key object-store. Nó chỉ giữ (a) một worker token do control plane cấp lúc register, (b) một **run token scope theo run** đi kèm mỗi job claim. Artifact upload qua **presigned PUT** do control plane ký.
- **Epoch bắt buộc:** mọi mutation gửi lên control plane mang `leaseEpoch`. Nhận `409 STALE_EPOCH` ⇒ **dừng ngay lập tức, không ghi gì thêm** (zombie tự tử) — kể cả artifact.
- **Taxonomy lỗi một vị từ:** `err instanceof AppError && err.retryable === true` là điều kiện retry DUY NHẤT. `AssertionFailure` ⇒ verdict `failed`, KHÔNG retry.
- **Timeout lồng chặt:** action 15s < navigation 30s < step 60s < chain `clamp(90 + 12×steps, 180..900)`s — lấy từ `MEMORY.timeoutsSec` và `chainTimeoutSeconds()` của compiler, **không tự chế lại công thức**.
- **Hằng số bộ nhớ chỉ có một nguồn:** `apps/runner/src/memory-governance.ts`. Cấm hard-code 350/500/75/85/92/400 ở nơi khác; test khẳng định điều đó.
- Runner **không import `@testkite/core`** (nó là process riêng) — cưỡng chế bằng lint rule ở Task 1.
- Commit nhỏ sau mỗi task; TDD đúng nghi thức (test ĐỎ trước, code sau).

---

## Kết quả spike hạ tầng (ĐÃ CHẠY THẬT trong sandbox — 2026-08-29)

Mọi số dưới đây lấy từ lần chạy thật, không phải ước lượng. Đây là căn cứ cho ranh giới "test được ở sandbox" vs "chỉ chứng minh được trên host thật".

### 1. Playwright + Chromium — CHẠY ĐƯỢC (nhưng KHÔNG sandbox — xem đính chính)

> **ĐÍNH CHÍNH 30-08-2026 (review chặn T12).** Dòng `noSandbox=false` bên dưới là **SAI**: nó chỉ
> ghi nhận "ta không tự truyền `--no-sandbox`", không phải cái chromium thực sự nhận.
> playwright-core 1.56.1 mặc định `chromiumSandbox: false` và **tự thêm `--no-sandbox`** trừ khi
> option đúng bằng `true` (`lib/server/chromium/chromium.js:288`). Đọc
> `/proc/<pid>/cmdline` của browser vừa launch: `--no-sandbox` CÓ mặt. Thêm nữa, sandbox dev/CI
> chạy **uid 0**, mà zygote của chromium từ chối sandbox khi là root
> (`Running as root without --no-sandbox is not supported`) — nên launch có sandbox **không thể**
> chạy được ở đây. Hệ quả cho mọi task dùng engine này: launcher phải truyền
> `chromiumSandbox: true` (mặc định), và chỗ nào cần chạy trong sandbox dev/CI thì phải **xin
> tường minh** `sandbox: "off-root-dev-only"` (chỉ chấp nhận cho uid 0; uid khác ⇒ ném lỗi, để
> container worker uid 10001 không bao giờ tụt xuống `--no-sandbox`). Chứng minh sandbox BẬT
> thật sự chỉ làm được trên host non-root: `apps/runner/test/host/chromium-sandbox.test.ts`.

```
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers
/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell   (+ chromium-1194)
playwright 1.56.1 (global, /opt/node22/lib/node_modules) — registry có 1.62.1
node v22.22.2 · pnpm 10.33.0 · kernel 6.18.44 · 4 vCPU · 16GB RAM · swap 0
```

`chromium.launch({ channel: 'chromium-headless-shell' })` chạy được:

```
launch ms=1118  noSandbox=false  version=141.0.7390.37
```

RSS thật (đọc `/proc/<pid>/statm`), một browser + một context + `goto(data:…)` + `click` + `fill`:

| Thành phần | RSS |
|---|---|
| node (process.memoryUsage.rss) | **84.3 MB** |
| node (`/proc/self/status` VmRSS) | 84.8 MB |
| node (`/proc/self/statm`) | 84.8 MB |
| chromium browser process | 71.7–91.2 MB |
| chromium GPU process | 46.8 MB |
| chromium NetworkService | 41.6 MB |
| chromium renderer (1 page trống) | 47–84 MB |
| **tổng cây chromium, 1 context** | **381.5 MB** (6 process, đo lúc vừa mở) |
| **tổng cây chromium, 4 context (K=4 batch)** | **648.1 MB** (9 process) |
| tổng cây chromium sau `context.close()` ×4 | 285.8 MB |

Kết luận: L1 container 3072MB cho K=4 batch là **rộng rãi** với trang trống (~650MB); phần còn lại là ngân sách cho DOM/JS của app-under-test — đúng như thiết kế, và đúng chỗ mà L3 350/500MB per-context đứng gác.

### 2. Quy tội RSS theo context — CÓ CƠ CHẾ THẬT, đã kiểm chứng

Đây là câu hỏi khó nhất của L3 (làm sao biết context nào ăn RAM). **Đã tìm ra và đo được**: CDP `SystemInfo.getProcessInfo` (browser-level session) trả pid + type; **diff tập pid quanh `newContext()+newPage()`** cho ra đúng renderer pid của context đó; RSS đọc từ `/proc/<pid>/statm`.

```
baseline: browser:2654=71.7MB GPU:2673=46.8MB network:2675=41.6MB
new pids for ctxA: renderer:2697
new pids for ctxB: renderer:2706
sau khi bơm 240MB vào ctxB:
  ctxA renderers: renderer:2697=83.9MB      ← KHÔNG đổi
  ctxB renderers: renderer:2706=332.4MB     ← đúng context có tội
sau ctxB.close(): 2706=0.0MB (process biến mất) | 2697=83.9MB (ctxA nguyên vẹn)
```

`Target.getTargets` cũng trả `browserContextId` cho từng target — dùng làm kiểm tra chéo. `Performance.getMetrics` **KHÔNG** dùng được thay RSS: lúc renderer RSS 84MB thì `JSHeapUsedSize` chỉ 866KB (JS heap không phải RSS).

### 3. cgroup — sandbox là **v1 hybrid**, KHÔNG có memory controller trên v2

```
$ mount | grep cgroup
cgroup  on /sys/fs/cgroup/memory  type cgroup  (rw,memory)      ← v1
cgroup2 on /sys/fs/cgroup/unified type cgroup2 (rw)             ← v2, nhưng:
$ cat /sys/fs/cgroup/unified/cgroup.controllers
hugetlb                                    ← CHỈ hugetlb; KHÔNG có memory
$ echo "+memory" > /sys/fs/cgroup/unified/cgroup.subtree_control   → exit 1
$ ls /sys/fs/cgroup/unified/memory.max /sys/fs/cgroup/unified/memory.events
ls: No such file or directory                ← API v2 mà blueprint gọi tên KHÔNG TỒN TẠI ở đây
```

⇒ **`memory.max` / `memory.events` / `memory.peak` KHÔNG kiểm chứng được trong sandbox này.** Trên host thật thì có: `systemd 255 … default-hierarchy=unified`.

**Nhưng cơ chế thì ĐÃ CHỨNG MINH ĐƯỢC qua API v1** (cùng kernel, cùng memcg, khác tên file). Tạo cgroup con, đặt trần 200MB, chạy process bơm RAM:

```
$ mkdir -p /sys/fs/cgroup/memory/tkspike/oomtest
$ echo 209715200 > .../memory.limit_in_bytes          → ghi được
$ ( echo $BASHPID > .../cgroup.procs; exec node balloon.mjs )   # bơm 800MB
alloc 8MB / alloc 88MB / alloc 168MB → BỊ GIẾT
$ cat .../memory.max_usage_in_bytes  → 209715200   (đúng bằng trần)
$ cat .../memory.oom_control         → oom_kill 1   (parent SỐNG, đọc được counter)
$ dmesg | tail
oom-kill:constraint=CONSTRAINT_MEMCG, oom_memcg=/tkspike/oomtest, task=node, pid=3115
Memory cgroup out of memory: Killed process 3115 (node) anon-rss:203392kB
```

**Đây chính là kịch bản L2:** trần cgroup lồng → kernel giết process **bên trong** → tiến trình cha sống sót, đọc counter, tự chẩn đoán. Chỉ khác tên file giữa v1 và v2:

| Ý nghĩa | cgroup v1 (sandbox) | cgroup v2 (host/prod — blueprint) |
|---|---|---|
| Đặt trần | `memory.limit_in_bytes` | `memory.max` |
| Đang dùng | `memory.usage_in_bytes` | `memory.current` |
| Đỉnh | `memory.max_usage_in_bytes` | `memory.peak` |
| Đếm OOM kill | `memory.oom_control` → `oom_kill N` | `memory.events` → `oom_kill N` |
| Nạp process | `cgroup.procs` | `cgroup.procs` |

⇒ Thiết kế bắt buộc: interface **`MemoryLimiter`** với **2 impl production** (`CgroupV2MemoryLimiter` cho host, `FakeMemoryLimiter` cho test) + một integration test **`skipIf` khi không phát hiện memory controller trên cgroup v2**. Toàn bộ logic governance (ngưỡng, shed, recycle, quy tội, báo `browser_oom`) chạy trên fake ⇒ xanh ở mọi máy; đúng-sai của **đường ống cgroup** thì gate riêng trên host.

### 4. `oom_score_adj` — chỉ NÂNG được, không HẠ được trong sandbox

```
$ printf -- "500"  > /proc/self/oom_score_adj  → OK   (chromium +500 ✔)
$ printf -- "-500" > /proc/self/oom_score_adj  → Permission denied (dù uid=0)
$ capsh --decode=000001fffeffffff | grep sys_resource → (không có)
```

CAP_SYS_RESOURCE bị drop ⇒ **`node −500` chỉ verify được trên host thật**. Hệ quả thiết kế: `oom_score_adj` là **hai lời gọi tách rời** — `+500` cho chromium (test được ở mọi nơi) và `−500` cho node (skip khi thiếu CAP_SYS_RESOURCE, có gate host riêng). Không gộp vào một hàm "làm cả hai hoặc không làm gì".

### 5. PSI — KHÔNG có trong sandbox

```
$ cat /proc/pressure/memory → No such file or directory   (cả cpu, io)
```

⇒ `runnerd` PSI watermark: parser viết + test bằng **fixture chuỗi** (dạng `some avg10=… total=…`), đọc file thật là đường I/O mỏng có fake; watermark thật chỉ chứng minh trên host.

### 6. systemd — binary CÓ, daemon KHÔNG chạy; `systemd-analyze verify` DÙNG ĐƯỢC offline

```
$ ps -p 1 -o comm=            → process_api        (không phải systemd)
$ ls /run/systemd/system      → No such file or directory
$ systemd-analyze --version   → systemd 255 (255.4-1ubuntu8.14) … default-hierarchy=unified
```

**Phát hiện quan trọng — `systemd-analyze verify` KHÔNG dùng exit code để báo lỗi cú pháp:**

```
$ systemd-analyze verify bad.service        # MemoryMax=nonsense + OOMPolicyy=continue
bad.service:7: Invalid memory limit 'nonsense', ignoring: Invalid argument
bad.service:8: Unknown key name 'OOMPolicyy' in section 'Service', ignoring.
exit=0                                      ← VẪN LÀ 0!

$ systemd-analyze verify ts-workers.slice   # slice hợp lệ
exit=0, không in gì

$ systemd-analyze verify 'ts-worker@.service'
ts-worker@i.service: Command /usr/bin/podman is not executable: No such file or directory
exit=1                                      ← chỉ ExecStart thiếu mới ra exit≠0
```

⇒ Gate offline phải **bắt lỗi theo OUTPUT, không theo exit code**: chạy verify, và **fail nếu stderr/stdout có bất kỳ dòng nào** (trừ dòng "Command … is not executable" cho binary runtime chưa cài trong CI). Task 15 viết đúng script đó.

### 7. Nguồn số liệu RSS cho poll 5s — chọn `/proc/<pid>/statm`

1000 lần đọc:

| Nguồn | Chi phí | Ghi chú |
|---|---|---|
| `process.memoryUsage.rss()` | **6 ms** | chỉ đọc được CHÍNH process node — vô dụng cho renderer |
| `/proc/<pid>/statm` (field 2 × 4096) | **7 ms** | đọc được MỌI pid, rẻ nhất trong nhóm đọc được pid khác |
| `/proc/<pid>/status` (`VmRSS:`) | 12 ms | đắt gấp đôi, phải regex |

Ba nguồn khớp nhau trong 0.5MB (84.3 / 84.8 / 84.8 MB). **Chọn `/proc/<pid>/statm`** cho cả node lẫn renderer: một hàm duy nhất, rẻ nhất, đọc được pid bất kỳ. Với poll 5s và ≤10 process ⇒ ~0.07ms/tick — không đáng kể.

### 8. Mini-soak 30 chain (tiền trạm cho soak 200)

Một browser sống lâu, 30 context tuần tự, mỗi context 8 step + 8 screenshot WebP q70 qua CDP:

```
n , nodeRssMB , chromiumTreeRssMB , procCount , msPerChain
1  , 87.0 , 211.3 , 3 , 854
5  , 92.0 , 212.6 , 3 , 768
10 , 93.1 , 211.7 , 3 , 748
15 , 94.4 , 212.4 , 3 , 765
20 , 95.2 , 213.1 , 3 , 752
25 , 95.0 , 213.7 , 3 , 755
30 , 95.3 , 213.7 , 3 , 752
total wall ms=22756
```

- **node RSS 87.0 → 95.3MB rồi phẳng** (+9.5%, đứng yên từ chain 20). `MEMORY.recycle.containerRssFloorGrowthPct = 130` là ngưỡng đúng — nhiễu bình thường không chạm.
- **cây chromium phẳng ở ~212MB, đúng 3 process** sau mỗi lần `context.close()` ⇒ không rò context.
- **0 orphan chromium** sau `browser.close()` + 1.5s settle (đo ngay sau khi close thì thấy 2 — process chưa reap xong ⇒ **assertion soak phải có settle delay**, nếu không sẽ đỏ giả).
- 750ms/chain cho chain 8 step trên `data:` URL — sàn lý thuyết, **không** phải con số 75s/chain của sizing (đó là app thật + network).

### 9. Screenshot: WebP qua CDP + dedup content-hash

240 ảnh (30 chain × 8 step), viewport 1280×720:

```
CDP Page.captureScreenshot format=webp q70 →  2 362 B
CDP Page.captureScreenshot format=jpeg q70 →  6 392 B     (WebP nhỏ hơn 2.7×)
Playwright page.screenshot({type:'jpeg',quality:70}) → 9 503 B
tổng 240 ảnh: 832 740 B (tb 3 470 B) · trùng hash 231/240 (96.3%) · lưu thực 31 260 B ⇒ tiết kiệm 96.2%
```

Playwright API **không** hỗ trợ WebP ⇒ phải đi CDP `Page.captureScreenshot` (đúng như blueprint §5.2 đã ghi). Tỉ lệ dedup 96.3% là **cận trên** (trang synthetic gần như bất động); blueprint ước lượng 30–50% cho flow thật — plan này không hứa quá con số blueprint.

### 10. Trace + timeout lồng

```
ctx.setDefaultTimeout(15000) / ctx.setDefaultNavigationTimeout(30000)   → áp dụng OK
page.click('#nope', {timeout:800})  → TimeoutError sau 808ms, e.name='TimeoutError'
step-level race (Promise.race với ngân sách 500ms) → 'STEP_TIMEOUT' sau 501ms
   ⚠ action bên trong VẪN chạy tiếp — chỉ ctx.close() mới thật sự huỷ ⇒ close trong finally là BẮT BUỘC
ctx.tracing.start({screenshots:true,snapshots:true}) → tracing.stop({path}) → trace.zip 3 304 B
```

### Bảng ranh giới — cái gì chứng minh được ở đâu

| Hạng mục | Sandbox/CI | Chỉ host thật |
|---|---|---|
| Executor, verdict, timeout lồng, quarantine, breaker, shed, recycle, ring-buffer, dedup | ✅ unit + fake engine | — |
| Playwright thật: 1 context/chain, close trong finally, quy tội renderer RSS, WebP CDP, trace | ✅ integration (chromium có sẵn) | — |
| `MemoryLimiter` logic (đặt trần, đọc peak/oom_kill, báo `browser_oom`) | ✅ trên `FakeMemoryLimiter` | — |
| cgroup v2 `memory.max`/`memory.events`/`memory.peak` thật | ❌ (v2 không có memory controller) | ✅ gate `pnpm --filter @testkite/runner test:host` |
| `oom_score_adj` node **−500** | ❌ (thiếu CAP_SYS_RESOURCE) | ✅ |
| `oom_score_adj` chromium **+500** | ✅ | ✅ |
| PSI watermark | ✅ parser trên fixture | ✅ đọc `/proc/pressure/memory` thật |
| systemd unit: cú pháp | ✅ `systemd-analyze verify` (bắt theo OUTPUT) | — |
| systemd unit: hành vi (MemoryHigh/Max, OOMPolicy, Restart) | ❌ | ✅ |
| Soak 200 chain, API RSS phẳng, 0 orphan | ⚠️ chạy được nhưng 4 vCPU ⇒ dài | ✅ nghiệm thu trên host pilot |

---

## Hợp đồng GIẢ ĐỊNH với control plane (plan `2026-08-29-m3-orchestration.md` là chủ sở hữu)

Hai plan viết song song. Plan orchestration **SỞ HỮU** hợp đồng internal API và sẽ khai mục "Hợp đồng cho plan fleet". Plan này là **CLIENT**. Dưới đây là giả định của bên client; **mọi giả định bị nhốt vào đúng một file `apps/runner/src/control-plane-client.ts`** — hợp đồng thật khác đi thì sửa một file, không lan ra executor/governance.

### Endpoint giả định (base = `CONTROL_PLANE_URL`, đường `/internal/fleet/*`)

| Method + path | Dùng để | Auth |
|---|---|---|
| `POST /internal/fleet/workers/register` | worker/runnerd đăng ký, nhận `workerId` + `workerToken` | bootstrap token của host |
| `POST /internal/fleet/workers/{workerId}/heartbeat` | heartbeat 5s + PSI + lệnh trả về (`drain`/`continue`) | `workerToken` |
| `POST /internal/fleet/claim` | claim 1 job (conditional UPDATE bump `lease_epoch`) | `workerToken` |
| `POST /internal/fleet/jobs/{jobRunId}/heartbeat` | giữ lease của job đang chạy | `runToken` |
| `POST /internal/fleet/jobs/{jobRunId}/events` | báo tiến độ idempotent theo `seq` | `runToken` |
| `POST /internal/fleet/jobs/{jobRunId}/artifacts` | xin **presigned PUT** cho artifact | `runToken` |
| `POST /internal/fleet/jobs/{jobRunId}/complete` | báo verdict cuối | `runToken` |

### Payload giả định

```jsonc
// POST /internal/fleet/claim  →  200 { job } | 204 (rỗng)
{ "workerId": "w-1", "lane": "batch", "freeSlots": 3 }
// 200:
{
  "jobRunId": "jr_01H…", "leaseEpoch": 7, "runId": "run_01H…", "chainKey": "login>checkout",
  "attempt": 1, "runToken": "tk_…", "leaseDeadlineAt": "2026-08-29T10:00:00.000Z",
  "plan": { /* RunPlan nguyên văn của @testkite/run-compiler */ }
}

// POST …/jobs/{id}/events    (idempotent theo seq)
{ "leaseEpoch": 7, "seq": 12, "kind": "step_finished",
  "payload": { "caseId": "c1", "ordinal": 3, "status": "passed", "durationMs": 812,
               "screenshotSha256": "…" } }

// POST …/jobs/{id}/artifacts →  200 presigned PUT
{ "leaseEpoch": 7, "kind": "trace" | "screenshot_bundle", "contentType": "application/zip",
  "sha256": "…", "sizeBytes": 3304 }
// 200:
{ "url": "https://minio…/…?X-Amz-Signature=…", "method": "PUT",
  "headers": { "Content-Type": "application/zip" }, "expiresAt": "…" }

// POST …/jobs/{id}/complete
{ "leaseEpoch": 7, "verdict": "passed" | "failed",
  "steps": [ { "caseId": "c1", "ordinal": 1, "status": "passed", "durationMs": 91 } ],
  "artifacts": [ { "kind": "trace", "sha256": "…", "sizeBytes": 3304 } ] }
// hoặc báo lỗi hạ tầng (KHÔNG phải verdict):
{ "leaseEpoch": 7, "infraError": { "code": "browser_oom", "retryable": true,
  "message": "chromium killed by cgroup", "peakRssBytes": 1728053248 } }
```

### Quy tắc epoch (giả định, khớp blueprint §5)

1. Claim = conditional UPDATE **bump** `lease_epoch`; 0 row ⇒ không có job (204).
2. **Mọi** mutation (`heartbeat`/`events`/`artifacts`/`complete`) mang `leaseEpoch` của job.
3. Server thấy `leaseEpoch` < epoch hiện tại ⇒ **`409` + body `{ "code": "STALE_EPOCH" }`**.
4. Worker nhận `STALE_EPOCH` ⇒ **dừng ngay**: huỷ chain, đóng context trong `finally`, **không** gọi thêm bất kỳ endpoint nào cho job đó, **không** upload artifact, tăng counter `zombie_suicides`. Không retry, không "cố báo lần nữa".

### Mã lỗi giả định

| HTTP | body.code | Ý nghĩa | Worker làm gì |
|---|---|---|---|
| 204 | — | không có job | ngủ `claimIdleMs` rồi claim lại |
| 401 | `UNAUTHORIZED` | token hỏng/hết hạn | dừng worker, thoát 1 (systemd `Restart=always` sẽ dựng lại + register lại) |
| 404 | `NOT_FOUND` | job biến mất | bỏ job, tiếp tục vòng lặp |
| 409 | `STALE_EPOCH` | zombie | **tự tử** (quy tắc 4) |
| 410 | `JOB_CANCELLED` | run bị huỷ | huỷ chain, đóng context, KHÔNG complete |
| 429 | `RATE_LIMITED` | control plane quá tải | backoff mũ có jitter |
| 5xx | — | tạm thời | backoff mũ có jitter, tối đa `maxRetries` rồi coi là `host_death` |

### Điều KHÔNG giả định (worker tuyệt đối không làm)

Không kết nối DB · không giữ credential team · không ký S3 · không đọc bảng authoring/element · không tự quyết retry (chỉ báo `retryable` theo taxonomy, control plane quyết requeue).

### Task nào CHỜ control plane, task nào chạy ngay

| Chạy ngay (không phụ thuộc) | Chờ đối chiếu hợp đồng thật |
|---|---|
| T1 scaffold · T2 RSS · T3 MemoryLimiter · T4 oom_score + OOM reporter · T5 context monitor · T6 shedder · T7 recycler · T8 quarantine + breaker · T9 timeout + verdict · T10 engine interface + fake · T11 executor trên fake engine · T12 Playwright engine thật · T13 screenshot ring + dedup · T14 uploader presigned PUT (fake fetch) · T17 phần PSI/daemon (fake client) · T18 systemd units + manifest + gate cú pháp | T15 `control-plane-client.ts` · T16 worker loop · T17 heartbeat thật · T19 soak 200 chain (nghiệm thu cuối cần dispatcher thật) |

T15 vẫn **viết được ngay** theo hợp đồng giả định — nó chính là lớp cách ly. Khi plan orchestration chốt: chạy contract test chéo, sửa **một** file (`control-plane-client.ts`) và harness của nó.

---

## ĐỐI CHIẾU 29-08 — hợp đồng đã CHỐT (plan orchestration `8ee1a68`)

Plan orchestration đã nộp sau plan này và **cố ý bám theo hợp đồng giả định ở trên**: đường dẫn, tên
trường, mã lỗi, quy ước 204-khi-rỗng, `infraError`, `leaseDeadlineAt`, mô hình bootstrap → worker →
run token — **giữ y nguyên**. Nguồn sự thật từ nay là mục "Hợp đồng cho plan fleet" trong
`2026-08-29-m3-orchestration.md`; mục "Hợp đồng GIẢ ĐỊNH" phía trên chỉ còn giá trị lịch sử.

**Đúng 3 điểm người thực thi Task 15 phải sửa trong `apps/runner/src/control-plane-client.ts`
(+ `test/harness/fake-control-plane.ts`):**

1. **`events.kind` là enum ĐÓNG 7 giá trị** — `chain_started | case_started | case_finished |
   step_started | step_finished | screenshot | infra_error`. Gửi giá trị ngoài danh sách ⇒ **400**.
   Plan này mới liệt kê `step_finished`, phải khai đủ enum và ép kiểu ở client.
2. **`artifacts.kind` là enum ĐÓNG 5 giá trị** — `trace | screenshot | screenshot_bundle | video |
   log` — và **`sizeBytes` ≤ 2 147 483 647**; vượt ⇒ 400, server KHÔNG ký URL. Ring-buffer gom ảnh
   theo chain (Task 13) phải kiểm trần này TRƯỚC khi xin presign, đừng để 400 lúc upload.
3. **`complete` nhận thêm 4 trường TUỲ CHỌN cho mỗi step** — `renderedSentence`, `failureContext`,
   `screenshotArtifactId`, `thumbhash`. Thiếu thì server nhận mặc định (`""`/`null`), không lỗi —
   **nhưng gallery ảnh từng bước + ThumbHash (blueprint §5.2) sẽ chết lặng**. Task 11/13 phải
   chuyển các giá trị này ra tới `complete`, coi như bắt buộc về mặt sản phẩm.

**Không đổi gì** ở: `register` cần bootstrap token và trả `workerToken`; tên trường `leaseDeadlineAt`.

**Kiểu dùng chung:** `apps/runner` **import** `INTERNAL_ROUTES` + zod schema từ `@testkite/contract`
(file `packages/contract/src/routes/internal.ts` do plan orchestration sở hữu và tạo ở Task 13),
KHÔNG tự khai lại `ClaimedJob` hay các kiểu payload.

**Migration:** plan này KHÔNG tạo migration nào (worker zero-credential) — mọi migration của M3 thuộc
plan orchestration. Nếu thấy mình cần bảng mới thì dừng lại, đó là dấu hiệu thiết kế sai chỗ.

---

## File Structure

Tạo mới trong `apps/runner`:

| File | Trách nhiệm |
|---|---|
| `src/config.ts` | zod env của worker (`RUNNER_LANE`, `CONTROL_PLANE_URL`, `WORKSPACE_DIR`…), exit-on-invalid |
| `src/memory/rss.ts` | `readRssBytes(pid)` từ `/proc/<pid>/statm` — nguồn RSS DUY NHẤT |
| `src/memory/limiter.ts` | `MemoryLimiter` + `CgroupV2MemoryLimiter` + `FakeMemoryLimiter` + `detectCgroupV2Memory()` |
| `src/memory/oom-score.ts` | `setOomScoreAdj(pid, value)` — hai lời gọi tách rời (+500 chromium / −500 node) |
| `src/memory/oom-reporter.ts` | diff `oom_kill`/`peak` ⇒ `RetryableInfraError("browser_oom")` kèm `peakRssBytes` |
| `src/memory/context-monitor.ts` | L3: poll 5s, soft 350 / hard 500 MB, quy tội theo renderer pid |
| `src/memory/shedder.ts` | ngưỡng 75/85/92% ⇒ stop-admitting / abort-largest / fail-youngest (thuần) |
| `src/memory/recycler.ts` | quyết định recycle browser/container theo `MEMORY.recycle` (thuần) |
| `src/quarantine.ts` | `QuarantineLedger` (2 OOM ⇒ quarantine) + `FleetBreaker` + `QuarantineDecider` |
| `src/executor/timeouts.ts` | `budgetForChain(stepCount)` + `assertNested()` (thuần) |
| `src/executor/verdict.ts` | `classifyError()` — assertion / retryable-infra / fatal-infra |
| `src/executor/step-runner.ts` | chạy `StepPlan[]`: `action` qua verb registry, `for` theo `loopRows` |
| `src/executor/run-chain.ts` | 1 context / 1 chain, `finally` đóng, chain timeout, trace/screenshot hook |
| `src/browser/engine.ts` | `BrowserEngine` + `EngineContextHandle` (interface) + `FakeBrowserEngine` |
| `src/browser/playwright-engine.ts` | impl thật: chromium-headless-shell, CDP WebP, `SystemInfo.getProcessInfo` |
| `src/artifacts/screenshot-ring.ts` | ring-buffer NVMe + dedup SHA-256 + `drain()`/`discard()` |
| `src/artifacts/uploader.ts` | `ArtifactUploader` — presigned PUT, zero-credential |
| `src/control-plane-client.ts` | **DUY NHẤT** chứa giả định hợp đồng: endpoint, payload, epoch, mã lỗi |
| `src/worker.ts` | vòng đời claim → chạy → báo cáo; `STALE_EPOCH` ⇒ tự tử |
| `src/runnerd/psi.ts` | parse `/proc/pressure/memory` ⇒ GREEN/AMBER/RED |
| `src/runnerd/daemon.ts` | register / heartbeat 5s / PSI / drain |
| `src/runnerd/main.ts` | entrypoint `runnerd` |
| `test/harness/fake-control-plane.ts` | server HTTP giả cài đặt đúng hợp đồng giả định (có nút bấm `STALE_EPOCH`) |
| `test/harness/tmp-cgroup.ts` | `describeHostCgroup()` — `skipIf` khi không có memory controller trên v2 |
| `test/host/cgroup-v2.test.ts` | đường ống cgroup v2 thật — chỉ chạy bằng `test:host` |
| `test/deploy/manifest.test.ts` | manifest + unit systemd phải khớp `MEMORY` từng con số |
| `test/soak/synthetic-app.ts` | plan synthetic + `data:` URL cho soak |
| `test/soak/soak-200.test.ts` | soak 200 chain synthetic (gated `TESTKITE_SOAK=1`) |
| `vitest.config.ts` | timeout cho test có browser |
| `deploy/systemd/ts-workers.slice` | L0: `MemoryHigh=80%` / `MemoryMax=88%` |
| `deploy/systemd/ts-worker@.service` | template unit: `Restart=always`, `OOMPolicy=continue` |
| `deploy/systemd/runnerd.service` | daemon giám sát host |
| `deploy/runner-manifest.json` | trần bộ nhớ container — sinh/đối chiếu từ `MEMORY` |
| `scripts/verify-units.sh` | gate cú pháp unit offline (bắt lỗi theo OUTPUT, không theo exit code) |

Sửa: `apps/runner/package.json` (deps + scripts), `apps/runner/src/main.ts` (bỏ `throw TODO(M3)`, nối wiring), `testkite/eslint.config.mjs` (runner cấm import `@testkite/core`), `.github/workflows/testkite-ci.yml` (gate ngôn ngữ mở rộng sang runner + gate unit systemd + gate manifest memory limit).

**KHÔNG sửa:** `apps/runner/src/memory-governance.ts` (hằng số đã chốt) · bất cứ file nào trong `apps/core` (giữ bất biến ảnh API không browser) · code legacy.

---

## Task 1 — Scaffold runner: deps, vitest, env config, lint/CI gate

**Files:**
- Modify: `apps/runner/package.json`
- Create: `apps/runner/vitest.config.ts`
- Create: `apps/runner/src/config.ts`
- Test: `apps/runner/test/config.test.ts`
- Modify: `eslint.config.mjs`, `.github/workflows/testkite-ci.yml`

**Interfaces:**
- Consumes: `MEMORY` từ `src/memory-governance.ts` (đã có).
- Produces: `loadRunnerConfig(env: NodeJS.ProcessEnv): RunnerConfig`, kiểu `RunnerConfig { readonly lane: "interactive" | "batch"; readonly workerName: string; readonly controlPlaneUrl: string; readonly bootstrapToken: string; readonly workspaceDir: string; readonly claimIdleMs: number; readonly heartbeatIntervalMs: number; }`.

- [x] **Step 1: Thêm dependency + script**

```bash
cd testkite
pnpm --filter @testkite/runner add playwright-core@1.56.1 zod@^3.25.76
pnpm --filter @testkite/runner add -D vitest@^3.0.0 @types/node@^22.10.0
```

Sửa `apps/runner/package.json` phần `scripts`:

```json
{
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:host": "TESTKITE_HOST_CGROUP=1 vitest run test/host",
    "test:soak": "TESTKITE_SOAK=1 vitest run test/soak",
    "verify:units": "bash ../../scripts/verify-units.sh"
  },
  "dependencies": {
    "@testkite/contract": "workspace:*",
    "@testkite/run-compiler": "workspace:*",
    "@testkite/verb-kit": "workspace:*",
    "playwright-core": "1.56.1",
    "zod": "^3.25.76"
  }
}
```

- [x] **Step 2: Tạo `apps/runner/vitest.config.ts`**

```ts
import { defineConfig } from "vitest/config";

/**
 * Browser-backed integration tests launch chromium-headless-shell; measured cold start in the
 * spike was ~1.1s, and a chain of 8 steps ~0.75s. 30s per test leaves room on a loaded CI box
 * without letting a genuinely hung test hold the suite for minutes.
 */
export default defineConfig({
  test: {
    testTimeout: 30_000,
    hookTimeout: 30_000,
    include: ["test/**/*.test.ts", "src/**/*.test.ts"],
    exclude: ["test/soak/**", "test/host/**", "node_modules/**"],
  },
});
```

- [x] **Step 3: Viết test ĐỎ `apps/runner/test/config.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { loadRunnerConfig } from "../src/config.js";

const base = {
  RUNNER_LANE: "batch",
  RUNNER_WORKER_NAME: "host1-w1",
  CONTROL_PLANE_URL: "http://control-plane.internal:8080",
  RUNNER_BOOTSTRAP_TOKEN: "bootstrap-secret",
  RUNNER_WORKSPACE_DIR: "/var/lib/testkite/w1",
};

describe("loadRunnerConfig", () => {
  it("parses a complete environment and applies defaults", () => {
    const cfg = loadRunnerConfig(base);
    expect(cfg.lane).toBe("batch");
    expect(cfg.controlPlaneUrl).toBe("http://control-plane.internal:8080");
    expect(cfg.claimIdleMs).toBe(1_000);
    expect(cfg.heartbeatIntervalMs).toBe(5_000);
  });

  it("rejects an unknown lane instead of silently defaulting", () => {
    expect(() => loadRunnerConfig({ ...base, RUNNER_LANE: "turbo" })).toThrow(/RUNNER_LANE/);
  });

  it("rejects a missing control plane url", () => {
    const { CONTROL_PLANE_URL: _drop, ...without } = base;
    expect(() => loadRunnerConfig(without)).toThrow(/CONTROL_PLANE_URL/);
  });

  it("never accepts a database url — the worker is zero-credential", () => {
    const cfg = loadRunnerConfig({ ...base, DATABASE_URL: "postgres://user:pw@db/testkite" });
    expect(Object.values(cfg)).not.toContain("postgres://user:pw@db/testkite");
    expect(Object.keys(cfg)).not.toContain("databaseUrl");
  });
});
```

- [x] **Step 4: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test`
Expected: FAIL — `Cannot find module '../src/config.js'`.

- [x] **Step 5: Implement `apps/runner/src/config.ts`**

```ts
/**
 * Worker environment — validated once at boot, exit on invalid.
 *
 * The worker is ZERO-CREDENTIAL (docs/SYSTEM_DESIGN.md §5): there is deliberately NO
 * database url, NO object-store key and NO team secret in this schema. The only secret it
 * holds is the host bootstrap token used to register; everything else arrives per run from
 * the control plane and dies with the job.
 */
import { z } from "zod";
import { MEMORY } from "./memory-governance.js";

const schema = z.object({
  RUNNER_LANE: z.enum(["interactive", "batch"]),
  RUNNER_WORKER_NAME: z.string().min(1),
  CONTROL_PLANE_URL: z.string().url(),
  RUNNER_BOOTSTRAP_TOKEN: z.string().min(1),
  RUNNER_WORKSPACE_DIR: z.string().min(1),
  RUNNER_CLAIM_IDLE_MS: z.coerce.number().int().positive().default(1_000),
  RUNNER_HEARTBEAT_INTERVAL_MS: z.coerce.number().int().positive().default(5_000),
});

export interface RunnerConfig {
  readonly lane: "interactive" | "batch";
  readonly workerName: string;
  readonly controlPlaneUrl: string;
  readonly bootstrapToken: string;
  readonly workspaceDir: string;
  readonly claimIdleMs: number;
  readonly heartbeatIntervalMs: number;
  /** Slots served concurrently by this worker — derived from the lane, never hand-tuned. */
  readonly maxContexts: number;
  readonly containerLimitBytes: number;
}

export function loadRunnerConfig(env: NodeJS.ProcessEnv | Record<string, string | undefined>): RunnerConfig {
  const parsed = schema.safeParse(env);
  if (!parsed.success) {
    const issues = parsed.error.issues.map((i) => `${i.path.join(".")}: ${i.message}`).join("; ");
    throw new Error(`invalid runner environment — ${issues}`);
  }
  const e = parsed.data;
  return {
    lane: e.RUNNER_LANE,
    workerName: e.RUNNER_WORKER_NAME,
    controlPlaneUrl: e.CONTROL_PLANE_URL,
    bootstrapToken: e.RUNNER_BOOTSTRAP_TOKEN,
    workspaceDir: e.RUNNER_WORKSPACE_DIR,
    claimIdleMs: e.RUNNER_CLAIM_IDLE_MS,
    heartbeatIntervalMs: e.RUNNER_HEARTBEAT_INTERVAL_MS,
    maxContexts: MEMORY.contextsPerWorker[e.RUNNER_LANE],
    containerLimitBytes: MEMORY.containerLimitMb[e.RUNNER_LANE] * 1024 * 1024,
  };
}
```

- [x] **Step 6: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test`
Expected: PASS (4 test).

- [x] **Step 7: Lint rule — runner không import `@testkite/core`**

Thêm block vào `eslint.config.mjs` (đặt SAU block `apps/core/src/**`, trước `export default` đóng mảng — mỗi block một `files` riêng nên không đè nhau):

```js
  {
    /**
     * The runner is a SEPARATE PROCESS in a separate image (docs/SYSTEM_DESIGN.md §5). It talks
     * to the control plane over internal HTTP only. Importing the core app would (a) drag the DB
     * driver into the worker image, breaking the zero-credential rule, and (b) let a refactor
     * silently reintroduce direct table access from a worker.
     */
    files: ["**/apps/runner/src/**/*.ts"],
    rules: {
      "no-restricted-imports": [
        "error",
        {
          patterns: [
            {
              group: ["@testkite/core", "@testkite/core/*", "pg", "pg-*", "postgres", "drizzle-orm", "drizzle-orm/*"],
              message:
                "The runner is zero-credential and process-separate: it must not import the core app or any DB driver. Talk to the control plane through src/control-plane-client.ts (docs/SYSTEM_DESIGN.md §5).",
            },
          ],
        },
      ],
    },
  },
```

- [x] **Step 8: Mở rộng gate ngôn ngữ CI sang runner**

Trong `.github/workflows/testkite-ci.yml`, ở step `Gate — language`, thêm 2 mục vào mảng `TARGETS`:

```yaml
            testkite/apps/runner/src testkite/apps/runner/test
```

- [x] **Step 9: Verify + commit**

```bash
cd testkite
pnpm --filter @testkite/runner typecheck && pnpm --filter @testkite/runner test && pnpm lint
git add apps/runner/package.json apps/runner/vitest.config.ts apps/runner/src/config.ts \
        apps/runner/test/config.test.ts eslint.config.mjs pnpm-lock.yaml ../.github/workflows/testkite-ci.yml
git commit -m "M3-fleet T1: scaffold runner — deps, vitest, env config, lint/CI gate"
```

**Ghi chú thực thi T1 (lệch có chủ đích so với block code trong plan — đọc trước khi làm T3/T18):**

1. `vitest.config.ts` — `exclude` được điều kiện hoá theo env (`TESTKITE_HOST_CGROUP=1`,
   `TESTKITE_SOAK=1`) thay vì hằng số. Lý do: đối số đường dẫn của vitest CLI chỉ **thu hẹp**
   tập include, KHÔNG kéo lại file đã bị `exclude` loại — giữ nguyên `exclude: ["test/soak/**",
   "test/host/**"]` thì `pnpm --filter @testkite/runner test:host` khớp 0 file và exit ≠ 0 vĩnh
   viễn (Step 7 của Task 3 gọi đúng lệnh đó). Hợp đồng không đổi: `vitest run` mặc định vẫn
   loại soak + host; hai script chuyên dụng bật lại đúng thư mục của mình.
2. `test/config.test.ts` — thêm 3 test ngoài 4 test của plan: `maxContexts`/`containerLimitBytes`
   phải lấy từ `MEMORY` chứ không phải env (Global Constraint "hằng số chỉ có một nguồn"),
   coerce số hợp lệ, và chặn `RUNNER_CLAIM_IDLE_MS=0` (vòng claim quay không nghỉ). 7 test.
3. `eslint.config.mjs` — ngoài `no-restricted-imports` của plan còn thêm bản sinh đôi
   `no-restricted-syntax` cho `await import(...)` (chính comment `dynamicImportOf` trong file đó
   cảnh báo: `no-restricted-imports` KHÔNG thấy dynamic import). Kèm 3 fixture
   `tools/lint-fixtures/apps/runner/src/**` + 3 test trong `tools/lint-rules.test.ts` — đã kiểm
   chứng có răng: đổi glob `apps/runner` thành `apps/runner-NOPE` ⇒ 2 test "CATCHES" đỏ.
4. Gate ngôn ngữ mở sang `apps/runner/src|test` ⇒ hai file có sẵn (`main.ts`,
   `memory-governance.ts`) phải dịch sang tiếng Anh. `memory-governance.ts` chỉ đổi **comment**
   (`git diff` 9+/9− toàn dòng chú thích, mọi hằng số nguyên byte) — không phạm luật "KHÔNG sửa
   hằng số đã chốt". `main.ts` nhận thêm `loadRunnerConfig(process.env)` để config có một điểm
   dùng thật; `throw TODO(M3)` vẫn còn, T16 mới gỡ.

---

## Task 2 — Nguồn RSS duy nhất: `/proc/<pid>/statm`

**Files:**
- Create: `apps/runner/src/memory/rss.ts`
- Test: `apps/runner/test/memory/rss.test.ts`

**Interfaces:**
- Produces: `readRssBytes(pid: number, procRoot?: string): number | null` (null = process đã chết), `sumRssBytes(pids: readonly number[], procRoot?: string): number`, hằng `PAGE_SIZE_BYTES = 4096`.

**Vì sao `statm`:** spike đo 1000 lần đọc — `process.memoryUsage.rss()` 6ms nhưng CHỈ đọc được chính process node; `/proc/<pid>/statm` 7ms và đọc được **mọi pid** (cần cho renderer của từng context); `/proc/<pid>/status` 12ms. Ba nguồn khớp trong 0.5MB.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/memory/rss.test.ts`**

```ts
import { mkdtempSync, mkdirSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { describe, expect, it } from "vitest";
import { readRssBytes, sumRssBytes, PAGE_SIZE_BYTES } from "../../src/memory/rss.js";

/** Builds a fake /proc tree so the test does not depend on any live process. */
function fakeProc(entries: Readonly<Record<number, string>>): string {
  const root = mkdtempSync(join(tmpdir(), "tk-proc-"));
  for (const [pid, statm] of Object.entries(entries)) {
    mkdirSync(join(root, pid));
    writeFileSync(join(root, pid, "statm"), statm);
  }
  return root;
}

describe("readRssBytes", () => {
  it("reads field 2 of statm and converts pages to bytes", () => {
    // size resident shared text lib data dt — resident = 21_700 pages = 84.8MB (the spike's node process)
    const root = fakeProc({ 100: "302645 21700 9858 1 0 995328 0\n" });
    expect(readRssBytes(100, root)).toBe(21_700 * PAGE_SIZE_BYTES);
  });

  it("returns null for a process that no longer exists instead of throwing", () => {
    const root = fakeProc({});
    expect(readRssBytes(999_999, root)).toBeNull();
  });

  it("returns null for a malformed statm rather than NaN", () => {
    const root = fakeProc({ 101: "garbage\n" });
    expect(readRssBytes(101, root)).toBeNull();
  });

  it("sums only the pids that are still alive", () => {
    const root = fakeProc({ 100: "1 1000 0 0 0 0 0\n", 101: "1 2000 0 0 0 0 0\n" });
    expect(sumRssBytes([100, 101, 999_999], root)).toBe(3_000 * PAGE_SIZE_BYTES);
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/memory/rss.test.ts`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/memory/rss.ts`**

```ts
/**
 * The ONE source of RSS numbers in the runner.
 *
 * Spike (2026-08-29, 1000 reads each): process.memoryUsage.rss() 6ms but only ever reads THIS
 * process; /proc/<pid>/statm 7ms and reads ANY pid — which is what per-context attribution
 * needs, since a BrowserContext's memory lives in a separate renderer process;
 * /proc/<pid>/status 12ms plus a regex. All three agreed within 0.5MB, so the cheapest
 * pid-agnostic one wins.
 *
 * `procRoot` is injectable so tests build a fake /proc tree instead of racing live processes.
 */
import { readFileSync } from "node:fs";
import { join } from "node:path";

/** x86_64 page size. Chromium and Node both run on 4K pages on every host we target. */
export const PAGE_SIZE_BYTES = 4096;

export function readRssBytes(pid: number, procRoot = "/proc"): number | null {
  let raw: string;
  try {
    raw = readFileSync(join(procRoot, String(pid), "statm"), "utf8");
  } catch {
    return null; // the process exited between the snapshot and the read — a normal race, not an error
  }
  const resident = raw.trim().split(/\s+/)[1];
  if (resident === undefined) return null;
  const pages = Number.parseInt(resident, 10);
  if (!Number.isFinite(pages) || pages < 0) return null;
  return pages * PAGE_SIZE_BYTES;
}

export function sumRssBytes(pids: readonly number[], procRoot = "/proc"): number {
  let total = 0;
  for (const pid of pids) total += readRssBytes(pid, procRoot) ?? 0;
  return total;
}
```

- [x] **Step 4: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/memory/rss.test.ts`
Expected: PASS (4 test).

- [x] **Step 5: Commit**

```bash
git add apps/runner/src/memory/rss.ts apps/runner/test/memory/rss.test.ts
git commit -m "M3-fleet T2: RSS source — /proc/<pid>/statm, injectable proc root"
```

**Ghi chú thực thi T2 (lệch có chủ đích so với block code trong plan):**

- **5 test, không phải 4.** Bốn test trong plan chạy trên **fake /proc tree** ⇒ chúng chứng minh
  *bộ phân tích cú pháp*, KHÔNG chứng minh rằng field 2 của `statm` thật đúng là resident pages,
  cũng không chạm `procRoot` mặc định `/proc`. Đã kiểm bằng mutation: đổi `PAGE_SIZE_BYTES`
  4096 → 8192 thì **cả 4 test fake vẫn XANH** (chúng dùng hằng số một cách ký hiệu). Nên bổ sung
  test thứ 5 `readRssBytes against the real /proc` — đối chiếu `readRssBytes(process.pid)` với
  `process.memoryUsage.rss()` (spike đo hai nguồn lệch < 0.5MB, test cho biên 8MB). Mutation trên
  làm đúng test này ĐỎ (`expected 90279936 to be less than 8388608`).
- **Không phải `test:host`.** Khác cgroup v2, `/proc/<pid>/statm` ĐỌC ĐƯỢC trong sandbox này, nên
  test chạy ngay trong CI; chỉ chặn bằng `describe.skipIf(process.platform !== "linux")` vì /proc
  không tồn tại ngoài Linux. CI chạy Linux ⇒ 0 skip.
- Commit thực tế dùng tiền tố `M3-FLT T2:` cho khớp `M3-FLT T1` đã push, thay vì `M3-fleet T2:`.

---

## Task 3 — `MemoryLimiter`: cgroup v2 thật + fake

**Files:**
- Create: `apps/runner/src/memory/limiter.ts`
- Test: `apps/runner/test/memory/limiter.test.ts`
- Test (host-only): `apps/runner/test/host/cgroup-v2.test.ts`
- Create: `apps/runner/test/harness/tmp-cgroup.ts`

**Interfaces:**
- Consumes: `MEMORY` (`browserCgroupReserveMb`).
- Produces:
  ```ts
  interface MemorySnapshot { readonly limitBytes: number; readonly currentBytes: number; readonly peakBytes: number; readonly oomKillCount: number; readonly highCount: number; }
  interface MemoryLimiter { readonly kind: "cgroup-v2" | "fake"; setLimit(bytes: number): void; attach(pid: number): void; read(): MemorySnapshot; }
  class CgroupV2MemoryLimiter implements MemoryLimiter { constructor(cgroupDir: string) }
  class FakeMemoryLimiter implements MemoryLimiter { setCurrent(b): void; setPeak(b): void; raiseOomKill(n?): void }
  function browserCgroupLimitBytes(containerLimitBytes: number): number
  function detectCgroupV2Memory(cgroupRoot?: string): boolean
  ```

**Ranh giới spike:** sandbox là cgroup **v1 hybrid** — `/sys/fs/cgroup/unified/cgroup.controllers` chỉ có `hugetlb`, `memory.max`/`memory.events`/`memory.peak` KHÔNG tồn tại. Cơ chế đã được chứng minh trên v1 (trần 200MB ⇒ kernel giết process trong cgroup, cha sống, `oom_control: oom_kill 1`, `max_usage_in_bytes` = đúng trần). Vì vậy: logic test trên `FakeMemoryLimiter`, đường ống v2 gate riêng bằng `test:host`.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/memory/limiter.test.ts`**

```ts
import { mkdtempSync, readFileSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { describe, expect, it } from "vitest";
import { MEMORY } from "../../src/memory-governance.js";
import {
  browserCgroupLimitBytes,
  CgroupV2MemoryLimiter,
  detectCgroupV2Memory,
  FakeMemoryLimiter,
} from "../../src/memory/limiter.js";

const MB = 1024 * 1024;

describe("browserCgroupLimitBytes", () => {
  it("reserves exactly MEMORY.browserCgroupReserveMb for node and the rest of the container", () => {
    const container = MEMORY.containerLimitMb.batch * MB;
    expect(browserCgroupLimitBytes(container)).toBe(container - MEMORY.browserCgroupReserveMb * MB);
    // 3072MB container ⇒ 2672MB for the browser cgroup
    expect(browserCgroupLimitBytes(container) / MB).toBe(2672);
  });

  it("refuses a container limit smaller than the reserve rather than producing a negative limit", () => {
    expect(() => browserCgroupLimitBytes(100 * MB)).toThrow(/reserve/);
  });
});

describe("CgroupV2MemoryLimiter", () => {
  /** A directory shaped like a cgroup v2 node — the real kernel files are plain text too. */
  function fakeCgroupDir(): string {
    const dir = mkdtempSync(join(tmpdir(), "tk-cg-"));
    writeFileSync(join(dir, "memory.max"), "max\n");
    writeFileSync(join(dir, "memory.current"), "0\n");
    writeFileSync(join(dir, "memory.peak"), "0\n");
    writeFileSync(join(dir, "memory.events"), "low 0\nhigh 0\nmax 0\noom 0\noom_kill 0\n");
    writeFileSync(join(dir, "cgroup.procs"), "");
    return dir;
  }

  it("writes the limit to memory.max in bytes", () => {
    const dir = fakeCgroupDir();
    new CgroupV2MemoryLimiter(dir).setLimit(2672 * MB);
    expect(readFileSync(join(dir, "memory.max"), "utf8").trim()).toBe(String(2672 * MB));
  });

  it("moves a pid by appending it to cgroup.procs", () => {
    const dir = fakeCgroupDir();
    new CgroupV2MemoryLimiter(dir).attach(4242);
    expect(readFileSync(join(dir, "cgroup.procs"), "utf8").trim()).toBe("4242");
  });

  it("parses memory.events oom_kill and memory.peak", () => {
    const dir = fakeCgroupDir();
    writeFileSync(join(dir, "memory.events"), "low 0\nhigh 3\nmax 12\noom 2\noom_kill 1\n");
    writeFileSync(join(dir, "memory.peak"), String(1_728_053_248));
    writeFileSync(join(dir, "memory.current"), String(900 * MB));
    writeFileSync(join(dir, "memory.max"), String(2672 * MB));
    const snap = new CgroupV2MemoryLimiter(dir).read();
    expect(snap.oomKillCount).toBe(1);
    expect(snap.highCount).toBe(3);
    expect(snap.peakBytes).toBe(1_728_053_248);
    expect(snap.currentBytes).toBe(900 * MB);
    expect(snap.limitBytes).toBe(2672 * MB);
  });

  it("reads an unlimited cgroup (memory.max=max) as Infinity, not NaN", () => {
    const dir = fakeCgroupDir();
    expect(new CgroupV2MemoryLimiter(dir).read().limitBytes).toBe(Number.POSITIVE_INFINITY);
  });
});

describe("FakeMemoryLimiter", () => {
  it("records the limit and reports what the test scripted", () => {
    const fake = new FakeMemoryLimiter();
    fake.setLimit(2672 * MB);
    fake.setCurrent(2_600 * MB);
    fake.setPeak(2_672 * MB);
    fake.raiseOomKill();
    const snap = fake.read();
    expect(snap.limitBytes).toBe(2672 * MB);
    expect(snap.currentBytes).toBe(2600 * MB);
    expect(snap.peakBytes).toBe(2672 * MB);
    expect(snap.oomKillCount).toBe(1);
    expect(fake.kind).toBe("fake");
  });

  it("remembers attached pids so a test can assert the browser was placed in the nested cgroup", () => {
    const fake = new FakeMemoryLimiter();
    fake.attach(11);
    fake.attach(12);
    expect(fake.attachedPids).toEqual([11, 12]);
  });
});

describe("detectCgroupV2Memory", () => {
  it("is false when the unified hierarchy exposes no memory controller (the sandbox shape)", () => {
    const root = mkdtempSync(join(tmpdir(), "tk-cgroot-"));
    writeFileSync(join(root, "cgroup.controllers"), "hugetlb\n");
    expect(detectCgroupV2Memory(root)).toBe(false);
  });

  it("is true when memory is listed among the v2 controllers", () => {
    const root = mkdtempSync(join(tmpdir(), "tk-cgroot-"));
    writeFileSync(join(root, "cgroup.controllers"), "cpuset cpu io memory hugetlb pids\n");
    expect(detectCgroupV2Memory(root)).toBe(true);
  });

  it("is false when the file is absent altogether", () => {
    expect(detectCgroupV2Memory(join(tmpdir(), "definitely-not-a-cgroup-root"))).toBe(false);
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/memory/limiter.test.ts`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/memory/limiter.ts`**

```ts
/**
 * L2 of the four memory ceilings (docs/SYSTEM_DESIGN.md §5): a NESTED cgroup holding only the
 * browser, with memory.max = container − MEMORY.browserCgroupReserveMb. When the browser blows
 * through it, the kernel kills a process INSIDE that cgroup — Node, which lives outside it,
 * survives, reads memory.events, blames the largest context and reports browser_oom itself.
 *
 * Two implementations on purpose:
 *  - CgroupV2MemoryLimiter — production hosts (systemd 255, default-hierarchy=unified).
 *  - FakeMemoryLimiter — every unit test. The 2026-08-29 spike showed the dev sandbox runs a
 *    cgroup v1 HYBRID whose unified hierarchy carries only the hugetlb controller, so
 *    memory.max/memory.events/memory.peak do not exist there at all. The mechanism itself was
 *    proved on that box through the v1 API (200MB cap ⇒ kernel killed the process inside the
 *    cgroup, parent survived and read oom_kill=1), so what is faked here is the FILE INTERFACE,
 *    not the behaviour. The real v2 pipe is covered by test/host/cgroup-v2.test.ts.
 */
import { appendFileSync, readFileSync, writeFileSync } from "node:fs";
import { join } from "node:path";
import { MEMORY } from "../memory-governance.js";

export interface MemorySnapshot {
  /** Number.POSITIVE_INFINITY when the cgroup is unlimited (`memory.max` == "max"). */
  readonly limitBytes: number;
  readonly currentBytes: number;
  readonly peakBytes: number;
  readonly oomKillCount: number;
  /** memory.events `high` — how often the soft ceiling throttled the cgroup. */
  readonly highCount: number;
}

export interface MemoryLimiter {
  readonly kind: "cgroup-v2" | "fake";
  setLimit(bytes: number): void;
  attach(pid: number): void;
  read(): MemorySnapshot;
}

/** memory.max for the nested browser cgroup = container limit − the reserve Node needs to survive. */
export function browserCgroupLimitBytes(containerLimitBytes: number): number {
  const reserve = MEMORY.browserCgroupReserveMb * 1024 * 1024;
  if (containerLimitBytes <= reserve) {
    throw new Error(
      `container limit ${containerLimitBytes}B is not larger than the ${reserve}B reserve — Node would have no memory left to report the OOM`,
    );
  }
  return containerLimitBytes - reserve;
}

export class CgroupV2MemoryLimiter implements MemoryLimiter {
  readonly kind = "cgroup-v2" as const;
  readonly #dir: string;

  constructor(cgroupDir: string) {
    this.#dir = cgroupDir;
  }

  setLimit(bytes: number): void {
    writeFileSync(join(this.#dir, "memory.max"), String(Math.floor(bytes)));
  }

  attach(pid: number): void {
    // cgroup.procs takes ONE pid per write; appending is how the kernel wants it.
    appendFileSync(join(this.#dir, "cgroup.procs"), `${pid}\n`);
  }

  read(): MemorySnapshot {
    const events = this.#readText("memory.events");
    return {
      limitBytes: this.#readLimit(),
      currentBytes: this.#readNumber("memory.current"),
      peakBytes: this.#readNumber("memory.peak"),
      oomKillCount: eventCount(events, "oom_kill"),
      highCount: eventCount(events, "high"),
    };
  }

  #readText(name: string): string {
    try {
      return readFileSync(join(this.#dir, name), "utf8");
    } catch {
      return "";
    }
  }

  #readNumber(name: string): number {
    const n = Number.parseInt(this.#readText(name).trim(), 10);
    return Number.isFinite(n) ? n : 0;
  }

  #readLimit(): number {
    const raw = this.#readText("memory.max").trim();
    if (raw === "max") return Number.POSITIVE_INFINITY;
    const n = Number.parseInt(raw, 10);
    return Number.isFinite(n) ? n : Number.POSITIVE_INFINITY;
  }
}

/** `memory.events` is `key value` per line; a missing key means zero, never NaN. */
function eventCount(events: string, key: string): number {
  for (const line of events.split("\n")) {
    const [name, value] = line.trim().split(/\s+/);
    if (name === key) {
      const n = Number.parseInt(value ?? "", 10);
      return Number.isFinite(n) ? n : 0;
    }
  }
  return 0;
}

export class FakeMemoryLimiter implements MemoryLimiter {
  readonly kind = "fake" as const;
  #limit = Number.POSITIVE_INFINITY;
  #current = 0;
  #peak = 0;
  #oomKill = 0;
  #high = 0;
  readonly attachedPids: number[] = [];

  setLimit(bytes: number): void {
    this.#limit = bytes;
  }

  attach(pid: number): void {
    this.attachedPids.push(pid);
  }

  read(): MemorySnapshot {
    return {
      limitBytes: this.#limit,
      currentBytes: this.#current,
      peakBytes: this.#peak,
      oomKillCount: this.#oomKill,
      highCount: this.#high,
    };
  }

  setCurrent(bytes: number): void {
    this.#current = bytes;
    if (bytes > this.#peak) this.#peak = bytes;
  }

  setPeak(bytes: number): void {
    this.#peak = bytes;
  }

  raiseOomKill(by = 1): void {
    this.#oomKill += by;
  }

  raiseHigh(by = 1): void {
    this.#high += by;
  }
}

/**
 * True only when the unified hierarchy actually carries the memory controller. On the dev
 * sandbox this returns FALSE (controllers = "hugetlb"), which is exactly what gates the
 * host-only cgroup tests instead of letting them fail confusingly.
 */
export function detectCgroupV2Memory(cgroupRoot = "/sys/fs/cgroup"): boolean {
  try {
    return readFileSync(join(cgroupRoot, "cgroup.controllers"), "utf8").split(/\s+/).includes("memory");
  } catch {
    return false;
  }
}
```

- [x] **Step 4: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/memory/limiter.test.ts`
Expected: PASS (10 test).

- [x] **Step 5: Harness gate host `apps/runner/test/harness/tmp-cgroup.ts`**

```ts
/**
 * Gate for tests that need a REAL cgroup v2 memory controller. On the dev sandbox the unified
 * hierarchy only carries hugetlb (spike 2026-08-29), so these tests must skip rather than fail:
 * a red that only means "wrong kernel config" trains people to ignore reds.
 */
import { mkdtempSync, rmSync } from "node:fs";
import { describe } from "vitest";
import { detectCgroupV2Memory } from "../../src/memory/limiter.js";

export const HOST_CGROUP_AVAILABLE = process.env.TESTKITE_HOST_CGROUP === "1" && detectCgroupV2Memory();

export const describeHostCgroup = HOST_CGROUP_AVAILABLE ? describe : describe.skip;

/** Creates a throwaway child cgroup under the current one and removes it afterwards. */
export function withTempCgroup(parent: string, fn: (dir: string) => void): void {
  const dir = mkdtempSync(`${parent}/tk-test-`);
  try {
    fn(dir);
  } finally {
    rmSync(dir, { recursive: true, force: true });
  }
}
```

- [x] **Step 6: Test host-only `apps/runner/test/host/cgroup-v2.test.ts`**

```ts
import { readFileSync } from "node:fs";
import { expect, it } from "vitest";
import { CgroupV2MemoryLimiter } from "../../src/memory/limiter.js";
import { describeHostCgroup, withTempCgroup } from "../harness/tmp-cgroup.js";

/**
 * Runs ONLY on a host whose unified hierarchy carries the memory controller
 * (`pnpm --filter @testkite/runner test:host`). This is the one place the v2 file names
 * themselves are exercised; everything else in the runner runs on FakeMemoryLimiter.
 */
describeHostCgroup("cgroup v2 memory pipe (host only)", () => {
  it("writes memory.max and reads it back", () => {
    withTempCgroup("/sys/fs/cgroup", (dir) => {
      const limiter = new CgroupV2MemoryLimiter(dir);
      limiter.setLimit(256 * 1024 * 1024);
      expect(readFileSync(`${dir}/memory.max`, "utf8").trim()).toBe(String(256 * 1024 * 1024));
      expect(limiter.read().limitBytes).toBe(256 * 1024 * 1024);
    });
  });

  it("exposes oom_kill and peak through the same snapshot shape the fake uses", () => {
    withTempCgroup("/sys/fs/cgroup", (dir) => {
      const snap = new CgroupV2MemoryLimiter(dir).read();
      expect(snap.oomKillCount).toBe(0);
      expect(snap.peakBytes).toBeGreaterThanOrEqual(0);
    });
  });
});
```

- [x] **Step 7: Chạy cả hai, xác nhận trạng thái đúng**

Run: `pnpm --filter @testkite/runner test test/memory/limiter.test.ts && pnpm --filter @testkite/runner test:host`
Expected: bộ unit PASS; bộ host **SKIPPED** trên sandbox (in `↓ cgroup v2 memory pipe (host only)`) — đó là kết quả ĐÚNG ở đây, PASS trên host thật.

- [x] **Step 8: Commit**

```bash
git add apps/runner/src/memory/limiter.ts apps/runner/test/memory/limiter.test.ts \
        apps/runner/test/harness/tmp-cgroup.ts apps/runner/test/host/cgroup-v2.test.ts
git commit -m "M3-fleet T3: MemoryLimiter — cgroup v2 impl + fake, host-only gate"
```

---

## Task 4 — `oom_score_adj` + OOM reporter tự chẩn đoán `browser_oom`

**Files:**
- Create: `apps/runner/src/memory/oom-score.ts`
- Create: `apps/runner/src/memory/oom-reporter.ts`
- Test: `apps/runner/test/memory/oom-reporter.test.ts`

**Interfaces:**
- Consumes: `MemoryLimiter`, `MemorySnapshot` (Task 3); `RetryableInfraError` từ `@testkite/contract`.
- Produces:
  ```ts
  const OOM_SCORE_NODE = -500; const OOM_SCORE_CHROMIUM = 500;
  function setOomScoreAdj(pid: number, value: number, procRoot?: string): "applied" | "denied"
  interface OomFinding { readonly killed: boolean; readonly oomKillDelta: number; readonly peakRssBytes: number; readonly blamedContextId: string | null; }
  class OomReporter { constructor(limiter: MemoryLimiter); baseline(): void; check(largestContext: { contextId: string; rssBytes: number } | null): OomFinding; toInfraError(f: OomFinding): RetryableInfraError }
  ```

**Ranh giới spike:** trong sandbox `oom_score_adj` **nâng được (+500) nhưng không hạ được (−500)** vì thiếu CAP_SYS_RESOURCE. Vì vậy `setOomScoreAdj` trả `"applied" | "denied"` thay vì ném — worker log cảnh báo và chạy tiếp, host thật (có CAP_SYS_RESOURCE) mới bắt buộc `applied`.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/memory/oom-reporter.test.ts`**

```ts
import { mkdtempSync, mkdirSync, readFileSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { RetryableInfraError } from "@testkite/contract";
import { describe, expect, it } from "vitest";
import { FakeMemoryLimiter } from "../../src/memory/limiter.js";
import { OOM_SCORE_CHROMIUM, OOM_SCORE_NODE, setOomScoreAdj } from "../../src/memory/oom-score.js";
import { OomReporter } from "../../src/memory/oom-reporter.js";

const MB = 1024 * 1024;

function fakeProc(pid: number): string {
  const root = mkdtempSync(join(tmpdir(), "tk-oomproc-"));
  mkdirSync(join(root, String(pid)));
  writeFileSync(join(root, String(pid), "oom_score_adj"), "0\n");
  return root;
}

describe("setOomScoreAdj", () => {
  it("writes the chromium value so the kernel prefers killing the browser", () => {
    const root = fakeProc(7);
    expect(setOomScoreAdj(7, OOM_SCORE_CHROMIUM, root)).toBe("applied");
    expect(readFileSync(join(root, "7", "oom_score_adj"), "utf8").trim()).toBe("500");
  });

  it("writes the node value so the kernel spares the reporter", () => {
    const root = fakeProc(8);
    expect(setOomScoreAdj(8, OOM_SCORE_NODE, root)).toBe("applied");
    expect(readFileSync(join(root, "8", "oom_score_adj"), "utf8").trim()).toBe("-500");
  });

  it("reports denied instead of throwing when the capability is missing", () => {
    // No such pid ⇒ the write fails exactly like an EACCES from a missing CAP_SYS_RESOURCE.
    expect(setOomScoreAdj(999_999, OOM_SCORE_NODE, mkdtempSync(join(tmpdir(), "tk-empty-")))).toBe("denied");
  });

  it("keeps the two values as separate constants (node negative, chromium positive)", () => {
    expect(OOM_SCORE_NODE).toBe(-500);
    expect(OOM_SCORE_CHROMIUM).toBe(500);
  });
});

describe("OomReporter", () => {
  it("finds no kill when the oom_kill counter did not move", () => {
    const limiter = new FakeMemoryLimiter();
    const reporter = new OomReporter(limiter);
    reporter.baseline();
    limiter.setCurrent(900 * MB);
    expect(reporter.check(null).killed).toBe(false);
  });

  it("detects a kernel kill from the counter delta and carries the cgroup peak", () => {
    const limiter = new FakeMemoryLimiter();
    const reporter = new OomReporter(limiter);
    reporter.baseline();
    limiter.setPeak(1_728_053_248);
    limiter.raiseOomKill();
    const finding = reporter.check({ contextId: "ctx-3", rssBytes: 520 * MB });
    expect(finding.killed).toBe(true);
    expect(finding.oomKillDelta).toBe(1);
    expect(finding.peakRssBytes).toBe(1_728_053_248);
    expect(finding.blamedContextId).toBe("ctx-3");
  });

  it("re-baselines after a check so the same kill is never reported twice", () => {
    const limiter = new FakeMemoryLimiter();
    const reporter = new OomReporter(limiter);
    reporter.baseline();
    limiter.raiseOomKill();
    expect(reporter.check(null).killed).toBe(true);
    expect(reporter.check(null).killed).toBe(false);
  });

  it("maps a finding to a RETRYABLE browser_oom infra error carrying peakRss", () => {
    const limiter = new FakeMemoryLimiter();
    const reporter = new OomReporter(limiter);
    reporter.baseline();
    limiter.setPeak(1_728_053_248);
    limiter.raiseOomKill();
    const err = reporter.toInfraError(reporter.check({ contextId: "ctx-3", rssBytes: 520 * MB }));
    expect(err).toBeInstanceOf(RetryableInfraError);
    expect(err.code).toBe("browser_oom");
    expect(err.retryable).toBe(true);
    expect(err.message).toContain("1728053248");
    expect(err.message).toContain("ctx-3");
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/memory/oom-reporter.test.ts`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/memory/oom-score.ts`**

```ts
/**
 * oom_score_adj is how the container TELLS THE KERNEL who to kill first (§5 L2): Chromium +500
 * so it is picked, Node −500 so the reporter survives to say what happened.
 *
 * Two separate calls on purpose, never one "do both" helper: raising the score needs no
 * privilege, LOWERING it needs CAP_SYS_RESOURCE. The 2026-08-29 spike measured exactly that on
 * the dev sandbox — `+500` succeeded, `-500` returned EACCES even as uid 0, because
 * CAP_SYS_RESOURCE is dropped. A worker missing the capability must still run (chromium is
 * still the preferred victim); it just logs that Node is not protected.
 */
import { writeFileSync } from "node:fs";
import { join } from "node:path";

export const OOM_SCORE_NODE = -500;
export const OOM_SCORE_CHROMIUM = 500;

export function setOomScoreAdj(pid: number, value: number, procRoot = "/proc"): "applied" | "denied" {
  try {
    writeFileSync(join(procRoot, String(pid), "oom_score_adj"), String(value));
    return "applied";
  } catch {
    return "denied";
  }
}
```

- [x] **Step 4: Implement `apps/runner/src/memory/oom-reporter.ts`**

```ts
/**
 * The container DIAGNOSES ITSELF (docs/SYSTEM_DESIGN.md §5). When the nested browser cgroup
 * blows its ceiling, the kernel kills Chromium and Node stays alive — so Node is the one that
 * must turn a silent kill into a first-class, RETRYABLE infra error rather than letting the
 * job look like a mysterious crash or, worse, a product failure.
 *
 * The counter is read as a DELTA against a baseline taken when the chain started: an absolute
 * count would re-report every earlier kill on the same long-lived browser forever.
 */
import { RetryableInfraError } from "@testkite/contract";
import type { MemoryLimiter } from "./limiter.js";

export interface OomFinding {
  readonly killed: boolean;
  readonly oomKillDelta: number;
  /** Peak of the browser cgroup — the number the incident report is actually judged on. */
  readonly peakRssBytes: number;
  /** The context with the highest RSS at kill time: the one we blame. */
  readonly blamedContextId: string | null;
}

export class OomReporter {
  readonly #limiter: MemoryLimiter;
  #baselineOomKill = 0;

  constructor(limiter: MemoryLimiter) {
    this.#limiter = limiter;
  }

  baseline(): void {
    this.#baselineOomKill = this.#limiter.read().oomKillCount;
  }

  check(largestContext: { readonly contextId: string; readonly rssBytes: number } | null): OomFinding {
    const snap = this.#limiter.read();
    const delta = snap.oomKillCount - this.#baselineOomKill;
    this.#baselineOomKill = snap.oomKillCount; // re-baseline: one kill is reported exactly once
    return {
      killed: delta > 0,
      oomKillDelta: delta,
      peakRssBytes: snap.peakBytes,
      blamedContextId: delta > 0 ? (largestContext?.contextId ?? null) : null,
    };
  }

  toInfraError(finding: OomFinding): RetryableInfraError {
    const who = finding.blamedContextId ?? "unknown-context";
    return new RetryableInfraError(
      "browser_oom",
      `chromium was killed by the nested cgroup (oom_kill delta=${finding.oomKillDelta}); ` +
        `peakRss=${finding.peakRssBytes} bytes; largest context at kill time=${who}`,
    );
  }
}
```

- [x] **Step 5: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/memory/oom-reporter.test.ts`
Expected: PASS (9 test).

- [x] **Step 6: Commit**

```bash
git add apps/runner/src/memory/oom-score.ts apps/runner/src/memory/oom-reporter.ts \
        apps/runner/test/memory/oom-reporter.test.ts
git commit -m "M3-fleet T4: oom_score_adj split + self-diagnosing browser_oom reporter"
```

---

## Task 5 — L3 per-context monitor: soft 350 / hard 500 MB, poll 5s

**Files:**
- Create: `apps/runner/src/memory/context-monitor.ts`
- Test: `apps/runner/test/memory/context-monitor.test.ts`

**Interfaces:**
- Consumes: `MEMORY.contextSoftMb/contextHardMb/pollIntervalMs`, `sumRssBytes` (Task 2).
- Produces:
  ```ts
  type ContextMemoryAction = "ok" | "soft-warn" | "hard-abort";
  function classifyContextRss(rssBytes: number): ContextMemoryAction
  interface ContextSample { readonly contextId: string; readonly rssBytes: number; readonly action: ContextMemoryAction }
  interface ContextMonitorDeps { readonly sampleRss: (contextId: string) => number | null; readonly onAction: (s: ContextSample) => void }
  class ContextMemoryMonitor {
    constructor(deps: ContextMonitorDeps)
    register(contextId: string): void
    unregister(contextId: string): void
    tick(): readonly ContextSample[]
    largest(): ContextSample | null
    start(): void; stop(): void
  }
  ```

Vòng lặp thời gian dùng `setInterval(…, MEMORY.pollIntervalMs)` nhưng **logic nằm hết trong `tick()`** — test gọi `tick()` trực tiếp, không cần fake timer.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/memory/context-monitor.test.ts`**

```ts
import { describe, expect, it, vi } from "vitest";
import { MEMORY } from "../../src/memory-governance.js";
import { classifyContextRss, ContextMemoryMonitor, type ContextSample } from "../../src/memory/context-monitor.js";

const MB = 1024 * 1024;

describe("classifyContextRss", () => {
  it("is ok below the soft ceiling", () => {
    expect(classifyContextRss(349 * MB)).toBe("ok");
  });

  it("warns at exactly the soft ceiling (350MB)", () => {
    expect(MEMORY.contextSoftMb).toBe(350);
    expect(classifyContextRss(350 * MB)).toBe("soft-warn");
  });

  it("aborts at exactly the hard ceiling (500MB)", () => {
    expect(MEMORY.contextHardMb).toBe(500);
    expect(classifyContextRss(500 * MB)).toBe("hard-abort");
  });

  it("aborts above the hard ceiling", () => {
    expect(classifyContextRss(2_000 * MB)).toBe("hard-abort");
  });
});

describe("ContextMemoryMonitor", () => {
  it("polls every registered context and reports each sample", () => {
    const seen: ContextSample[] = [];
    const rss: Record<string, number> = { a: 100 * MB, b: 400 * MB };
    const m = new ContextMemoryMonitor({
      sampleRss: (id) => rss[id] ?? null,
      onAction: (s) => seen.push(s),
    });
    m.register("a");
    m.register("b");
    const samples = m.tick();
    expect(samples.map((s) => s.contextId).sort()).toEqual(["a", "b"]);
    expect(seen.find((s) => s.contextId === "b")?.action).toBe("soft-warn");
    expect(seen.find((s) => s.contextId === "a")?.action).toBe("ok");
  });

  it("emits hard-abort exactly once per crossing, not on every tick", () => {
    const onAction = vi.fn();
    const m = new ContextMemoryMonitor({ sampleRss: () => 600 * MB, onAction });
    m.register("a");
    m.tick();
    m.tick();
    m.tick();
    expect(onAction.mock.calls.filter((c) => (c[0] as ContextSample).action === "hard-abort")).toHaveLength(1);
  });

  it("names the largest context — this is who the OOM reporter blames", () => {
    const rss: Record<string, number> = { a: 120 * MB, b: 480 * MB, c: 300 * MB };
    const m = new ContextMemoryMonitor({ sampleRss: (id) => rss[id] ?? null, onAction: () => {} });
    for (const id of ["a", "b", "c"]) m.register(id);
    m.tick();
    expect(m.largest()?.contextId).toBe("b");
  });

  it("drops a context that disappeared (renderer already gone) instead of counting it as 0", () => {
    const m = new ContextMemoryMonitor({ sampleRss: () => null, onAction: () => {} });
    m.register("a");
    expect(m.tick()).toEqual([]);
    expect(m.largest()).toBeNull();
  });

  it("stops reporting a context after unregister", () => {
    const onAction = vi.fn();
    const m = new ContextMemoryMonitor({ sampleRss: () => 100 * MB, onAction });
    m.register("a");
    m.unregister("a");
    expect(m.tick()).toEqual([]);
    expect(onAction).not.toHaveBeenCalled();
  });

  it("uses MEMORY.pollIntervalMs (5s) for the timer, not a hand-picked number", () => {
    expect(MEMORY.pollIntervalMs).toBe(5_000);
    const spy = vi.spyOn(globalThis, "setInterval");
    const m = new ContextMemoryMonitor({ sampleRss: () => null, onAction: () => {} });
    m.start();
    expect(spy.mock.calls[0]?.[1]).toBe(MEMORY.pollIntervalMs);
    m.stop();
    spy.mockRestore();
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/memory/context-monitor.test.ts`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/memory/context-monitor.ts`**

```ts
/**
 * L3 of the four ceilings (docs/SYSTEM_DESIGN.md §5): 350MB soft / 500MB hard per BrowserContext,
 * polled every 5s. One context = one chain, so a context over its ceiling is one tenant's chain
 * misbehaving — the blast radius stops there, and the fleet never has to choose between killing
 * the host and killing a stranger's run.
 *
 * RSS comes from the CONTEXT'S RENDERER PROCESSES, not from the JS heap: the 2026-08-29 spike
 * measured a renderer at 84MB RSS while CDP reported JSHeapUsedSize=866KB. Attribution is
 * verified there too — ballooning one context moved only that context's renderer (84MB → 332MB)
 * and left its neighbour untouched.
 *
 * The timing loop is a thin wrapper over `tick()` so tests drive time explicitly.
 */
import { MEMORY } from "../memory-governance.js";

export type ContextMemoryAction = "ok" | "soft-warn" | "hard-abort";

const SOFT_BYTES = MEMORY.contextSoftMb * 1024 * 1024;
const HARD_BYTES = MEMORY.contextHardMb * 1024 * 1024;

export function classifyContextRss(rssBytes: number): ContextMemoryAction {
  if (rssBytes >= HARD_BYTES) return "hard-abort";
  if (rssBytes >= SOFT_BYTES) return "soft-warn";
  return "ok";
}

export interface ContextSample {
  readonly contextId: string;
  readonly rssBytes: number;
  readonly action: ContextMemoryAction;
}

export interface ContextMonitorDeps {
  /** null = the context's processes are gone; the monitor forgets it rather than scoring it 0. */
  readonly sampleRss: (contextId: string) => number | null;
  readonly onAction: (sample: ContextSample) => void;
}

export class ContextMemoryMonitor {
  readonly #deps: ContextMonitorDeps;
  readonly #registered = new Set<string>();
  readonly #lastAction = new Map<string, ContextMemoryAction>();
  #last: readonly ContextSample[] = [];
  #timer: ReturnType<typeof setInterval> | null = null;

  constructor(deps: ContextMonitorDeps) {
    this.#deps = deps;
  }

  register(contextId: string): void {
    this.#registered.add(contextId);
  }

  unregister(contextId: string): void {
    this.#registered.delete(contextId);
    this.#lastAction.delete(contextId);
    this.#last = this.#last.filter((s) => s.contextId !== contextId);
  }

  tick(): readonly ContextSample[] {
    const samples: ContextSample[] = [];
    for (const contextId of this.#registered) {
      const rssBytes = this.#deps.sampleRss(contextId);
      if (rssBytes === null) {
        this.#lastAction.delete(contextId);
        continue;
      }
      const action = classifyContextRss(rssBytes);
      const sample: ContextSample = { contextId, rssBytes, action };
      samples.push(sample);
      // EDGE-triggered, not level-triggered: a context parked above the hard ceiling for a
      // minute must not fire 12 aborts — the first crossing already started the teardown.
      if (this.#lastAction.get(contextId) !== action) {
        this.#lastAction.set(contextId, action);
        this.#deps.onAction(sample);
      }
    }
    this.#last = samples;
    return samples;
  }

  largest(): ContextSample | null {
    let best: ContextSample | null = null;
    for (const s of this.#last) if (best === null || s.rssBytes > best.rssBytes) best = s;
    return best;
  }

  start(): void {
    if (this.#timer !== null) return;
    this.#timer = setInterval(() => this.tick(), MEMORY.pollIntervalMs);
    this.#timer.unref?.();
  }

  stop(): void {
    if (this.#timer === null) return;
    clearInterval(this.#timer);
    this.#timer = null;
  }
}
```

- [x] **Step 4: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/memory/context-monitor.test.ts`
Expected: PASS (11 test).

- [x] **Step 5: Commit**

```bash
git add apps/runner/src/memory/context-monitor.ts apps/runner/test/memory/context-monitor.test.ts
git commit -m "M3-fleet T5: L3 per-context monitor — 350/500MB, 5s poll, edge-triggered"
```

---

## Task 6 — Shedding 75/85/92%

**Files:**
- Create: `apps/runner/src/memory/shedder.ts`
- Test: `apps/runner/test/memory/shedder.test.ts`

**Interfaces:**
- Consumes: `MEMORY.shedThresholdsPct`, `ContextSample` (Task 5).
- Produces:
  ```ts
  type ShedLevel = "green" | "stop-admitting" | "abort-largest" | "fail-youngest";
  function shedLevel(usedBytes: number, limitBytes: number): ShedLevel
  interface ShedAction { readonly level: ShedLevel; readonly admit: boolean; readonly abortContextIds: readonly string[] }
  function planShedding(usedBytes: number, limitBytes: number, contexts: readonly ShedCandidate[]): ShedAction
  interface ShedCandidate { readonly contextId: string; readonly rssBytes: number; readonly startedAtMs: number }
  ```

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/memory/shedder.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { MEMORY } from "../../src/memory-governance.js";
import { planShedding, shedLevel, type ShedCandidate } from "../../src/memory/shedder.js";

const LIMIT = 3072 * 1024 * 1024; // batch container
const pct = (p: number) => Math.floor((LIMIT * p) / 100);

describe("shedLevel", () => {
  it("uses the thresholds frozen in memory-governance (75/85/92)", () => {
    expect(MEMORY.shedThresholdsPct).toEqual([75, 85, 92]);
  });

  it("is green below 75%", () => {
    expect(shedLevel(pct(74), LIMIT)).toBe("green");
  });

  it("stops admitting at 75%", () => {
    expect(shedLevel(pct(75), LIMIT)).toBe("stop-admitting");
  });

  it("aborts the largest context at 85%", () => {
    expect(shedLevel(pct(85), LIMIT)).toBe("abort-largest");
  });

  it("fails the youngest context at 92%", () => {
    expect(shedLevel(pct(92), LIMIT)).toBe("fail-youngest");
  });

  it("treats an unlimited container as green rather than dividing by Infinity into NaN", () => {
    expect(shedLevel(pct(99), Number.POSITIVE_INFINITY)).toBe("green");
  });
});

describe("planShedding", () => {
  const contexts: readonly ShedCandidate[] = [
    { contextId: "old-big", rssBytes: 500 * 1024 * 1024, startedAtMs: 1_000 },
    { contextId: "young-small", rssBytes: 80 * 1024 * 1024, startedAtMs: 9_000 },
  ];

  it("keeps admitting while green", () => {
    const a = planShedding(pct(10), LIMIT, contexts);
    expect(a).toEqual({ level: "green", admit: true, abortContextIds: [] });
  });

  it("stops admitting but aborts nobody at 75%", () => {
    const a = planShedding(pct(76), LIMIT, contexts);
    expect(a.admit).toBe(false);
    expect(a.abortContextIds).toEqual([]);
  });

  it("aborts the LARGEST context at 85% — most memory freed per casualty", () => {
    expect(planShedding(pct(86), LIMIT, contexts).abortContextIds).toEqual(["old-big"]);
  });

  it("fails the YOUNGEST context at 92% — least work thrown away", () => {
    expect(planShedding(pct(93), LIMIT, contexts).abortContextIds).toEqual(["young-small"]);
  });

  it("aborts nothing when there is no context to sacrifice", () => {
    expect(planShedding(pct(99), LIMIT, []).abortContextIds).toEqual([]);
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/memory/shedder.test.ts`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/memory/shedder.ts`**

```ts
/**
 * Shedding (docs/SYSTEM_DESIGN.md §5): 75% stop admitting, 85% abort the largest context,
 * 92% fail the youngest. A pure function of (used, limit, contexts) — the decision is testable
 * without a browser, and the worker only executes what this returns.
 *
 * Why largest at 85 and youngest at 92: at 85 the goal is to free the most memory per casualty,
 * at 92 the goal is to survive, so we throw away the least amount of already-done work.
 */
import { MEMORY } from "../memory-governance.js";

const [STOP_PCT, ABORT_PCT, FAIL_PCT] = MEMORY.shedThresholdsPct;

export type ShedLevel = "green" | "stop-admitting" | "abort-largest" | "fail-youngest";

export interface ShedCandidate {
  readonly contextId: string;
  readonly rssBytes: number;
  readonly startedAtMs: number;
}

export interface ShedAction {
  readonly level: ShedLevel;
  readonly admit: boolean;
  readonly abortContextIds: readonly string[];
}

export function shedLevel(usedBytes: number, limitBytes: number): ShedLevel {
  if (!Number.isFinite(limitBytes) || limitBytes <= 0) return "green";
  const used = (usedBytes / limitBytes) * 100;
  if (used >= FAIL_PCT) return "fail-youngest";
  if (used >= ABORT_PCT) return "abort-largest";
  if (used >= STOP_PCT) return "stop-admitting";
  return "green";
}

export function planShedding(
  usedBytes: number,
  limitBytes: number,
  contexts: readonly ShedCandidate[],
): ShedAction {
  const level = shedLevel(usedBytes, limitBytes);
  if (level === "green") return { level, admit: true, abortContextIds: [] };
  if (level === "stop-admitting") return { level, admit: false, abortContextIds: [] };

  const pick =
    level === "abort-largest"
      ? [...contexts].sort((a, b) => b.rssBytes - a.rssBytes)[0]
      : [...contexts].sort((a, b) => b.startedAtMs - a.startedAtMs)[0];

  return { level, admit: false, abortContextIds: pick === undefined ? [] : [pick.contextId] };
}
```

- [x] **Step 4: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/memory/shedder.test.ts`
Expected: PASS (11 test).

- [x] **Step 5: Commit**

```bash
git add apps/runner/src/memory/shedder.ts apps/runner/test/memory/shedder.test.ts
git commit -m "M3-fleet T6: shedding 75/85/92 — stop-admitting / abort-largest / fail-youngest"
```

---

## Task 7 — Recycle browser + container

**Files:**
- Create: `apps/runner/src/memory/recycler.ts`
- Test: `apps/runner/test/memory/recycler.test.ts`

**Interfaces:**
- Consumes: `MEMORY.recycle`.
- Produces:
  ```ts
  type BrowserRecycleReason = "contexts" | "age" | "rss" | "crash" | null;
  interface BrowserLifetime { readonly contextsServed: number; readonly startedAtMs: number; readonly rssBytes: number; readonly crashed: boolean }
  function browserRecycleReason(l: BrowserLifetime, nowMs: number): BrowserRecycleReason
  type ContainerRecycleReason = "jobs" | "age" | "rss-floor-growth" | null;
  interface ContainerLifetime { readonly jobsDone: number; readonly startedAtMs: number; readonly rssFloorBytes: number; readonly baselineRssFloorBytes: number }
  function containerRecycleReason(l: ContainerLifetime, nowMs: number): ContainerRecycleReason
  ```

**Số thật từ spike làm chứng cho ngưỡng `containerRssFloorGrowthPct = 130`:** node RSS 87.0 → 95.3MB qua 30 chain rồi **phẳng** (+9.5%) ⇒ nhiễu bình thường không chạm 130%.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/memory/recycler.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { MEMORY } from "../../src/memory-governance.js";
import { browserRecycleReason, containerRecycleReason } from "../../src/memory/recycler.js";

const MB = 1024 * 1024;
const MIN = 60_000;
const HOUR = 60 * MIN;
const healthyBrowser = { contextsServed: 1, startedAtMs: 0, rssBytes: 200 * MB, crashed: false };

describe("browserRecycleReason", () => {
  it("keeps a healthy browser", () => {
    expect(browserRecycleReason(healthyBrowser, 60_000)).toBeNull();
  });

  it("recycles after MEMORY.recycle.browserAfterContexts (50) contexts", () => {
    expect(MEMORY.recycle.browserAfterContexts).toBe(50);
    expect(browserRecycleReason({ ...healthyBrowser, contextsServed: 50 }, 1_000)).toBe("contexts");
  });

  it("recycles after 45 minutes", () => {
    expect(MEMORY.recycle.browserAfterMinutes).toBe(45);
    expect(browserRecycleReason(healthyBrowser, 45 * MIN)).toBe("age");
  });

  it("recycles above 1400MB RSS", () => {
    expect(MEMORY.recycle.browserAboveRssMb).toBe(1_400);
    expect(browserRecycleReason({ ...healthyBrowser, rssBytes: 1_400 * MB }, 1_000)).toBe("rss");
  });

  it("recycles a crashed browser before anything else", () => {
    expect(browserRecycleReason({ ...healthyBrowser, crashed: true }, 1_000)).toBe("crash");
  });

  it("does not recycle a browser measured at the spike's steady state (212MB after 30 contexts)", () => {
    expect(browserRecycleReason({ contextsServed: 30, startedAtMs: 0, rssBytes: 213 * MB, crashed: false }, 10 * MIN)).toBeNull();
  });
});

describe("containerRecycleReason", () => {
  const healthy = { jobsDone: 10, startedAtMs: 0, rssFloorBytes: 95 * MB, baselineRssFloorBytes: 87 * MB };

  it("keeps a healthy container", () => {
    expect(containerRecycleReason(healthy, HOUR)).toBeNull();
  });

  it("recycles after 500 jobs", () => {
    expect(MEMORY.recycle.containerAfterJobs).toBe(500);
    expect(containerRecycleReason({ ...healthy, jobsDone: 500 }, HOUR)).toBe("jobs");
  });

  it("recycles after 12 hours", () => {
    expect(MEMORY.recycle.containerAfterHours).toBe(12);
    expect(containerRecycleReason(healthy, 12 * HOUR)).toBe("age");
  });

  it("recycles when the RSS floor grew past 130% of baseline (leak detector)", () => {
    expect(MEMORY.recycle.containerRssFloorGrowthPct).toBe(130);
    expect(containerRecycleReason({ ...healthy, rssFloorBytes: 114 * MB }, HOUR)).toBe("rss-floor-growth");
  });

  it("does NOT fire the leak detector on the spike's real drift (87.0MB -> 95.3MB = 110%)", () => {
    expect(
      containerRecycleReason({ jobsDone: 30, startedAtMs: 0, rssFloorBytes: 95.3 * MB, baselineRssFloorBytes: 87 * MB }, HOUR),
    ).toBeNull();
  });

  it("ignores a zero baseline instead of dividing by zero", () => {
    expect(containerRecycleReason({ ...healthy, baselineRssFloorBytes: 0 }, HOUR)).toBeNull();
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/memory/recycler.test.ts`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/memory/recycler.ts`**

```ts
/**
 * Recycling (docs/SYSTEM_DESIGN.md §5): browser every 50 contexts / 45 min / 1.4GB / crash;
 * container every 500 jobs / 12h / RSS floor above 130% of baseline.
 *
 * This is the answer to the old system's slow death: a process that lives forever accumulates
 * forever. Nothing here is a heuristic invented on the spot — every number comes from
 * MEMORY.recycle, which is also what the container manifest is generated from.
 *
 * The RSS-floor rule is a LEAK DETECTOR, not a ceiling: it compares the floor between jobs
 * (when nothing should be held) against the floor at boot. The 2026-08-29 soak measured the
 * real drift at 87.0MB → 95.3MB over 30 chains and then flat — 110%, comfortably under 130%.
 */
import { MEMORY } from "../memory-governance.js";

export type BrowserRecycleReason = "contexts" | "age" | "rss" | "crash" | null;

export interface BrowserLifetime {
  readonly contextsServed: number;
  readonly startedAtMs: number;
  readonly rssBytes: number;
  readonly crashed: boolean;
}

export function browserRecycleReason(lifetime: BrowserLifetime, nowMs: number): BrowserRecycleReason {
  if (lifetime.crashed) return "crash";
  if (lifetime.contextsServed >= MEMORY.recycle.browserAfterContexts) return "contexts";
  if (nowMs - lifetime.startedAtMs >= MEMORY.recycle.browserAfterMinutes * 60_000) return "age";
  if (lifetime.rssBytes >= MEMORY.recycle.browserAboveRssMb * 1024 * 1024) return "rss";
  return null;
}

export type ContainerRecycleReason = "jobs" | "age" | "rss-floor-growth" | null;

export interface ContainerLifetime {
  readonly jobsDone: number;
  readonly startedAtMs: number;
  /** RSS measured BETWEEN jobs, when nothing should still be held. */
  readonly rssFloorBytes: number;
  readonly baselineRssFloorBytes: number;
}

export function containerRecycleReason(lifetime: ContainerLifetime, nowMs: number): ContainerRecycleReason {
  if (lifetime.jobsDone >= MEMORY.recycle.containerAfterJobs) return "jobs";
  if (nowMs - lifetime.startedAtMs >= MEMORY.recycle.containerAfterHours * 3_600_000) return "age";
  if (lifetime.baselineRssFloorBytes > 0) {
    const growthPct = (lifetime.rssFloorBytes / lifetime.baselineRssFloorBytes) * 100;
    if (growthPct >= MEMORY.recycle.containerRssFloorGrowthPct) return "rss-floor-growth";
  }
  return null;
}
```

- [x] **Step 4: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/memory/recycler.test.ts`
Expected: PASS (13 test).

- [x] **Step 5: Commit**

```bash
git add apps/runner/src/memory/recycler.ts apps/runner/test/memory/recycler.test.ts
git commit -m "M3-fleet T7: recycle rules for browser + container, leak detector"
```

---

## Task 8 — Quarantine chain sau 2 OOM + fleet breaker + poison-chain alert

**Files:**
- Create: `apps/runner/src/quarantine.ts`
- Test: `apps/runner/test/quarantine.test.ts`

**Interfaces:**
- Consumes: `MEMORY.quarantineAfterOomCount`.
- Produces:
  ```ts
  type QuarantineOutcome = { readonly quarantined: boolean; readonly oomCount: number; readonly alert: "poison-chain" | "fleet-unhealthy" | null }
  class QuarantineLedger { recordOom(chainKey: string): number; recordSuccess(chainKey: string): void; oomCount(chainKey: string): number; isQuarantined(chainKey: string): boolean }
  class FleetBreaker { constructor(o: { windowMs: number; minSamples: number; oomRatePct: number; now: () => number }); record(kind: "ok" | "oom"): void; isOpen(): boolean }
  class QuarantineDecider { constructor(ledger: QuarantineLedger, breaker: FleetBreaker); onChainOom(chainKey: string): QuarantineOutcome; onChainOk(chainKey: string): void }
  ```

**Vì sao cần breaker:** nếu cả fleet ốm (host lỗi, image hỏng), mọi chain đều OOM ⇒ quarantine sạch backlog của khách hàng vì lỗi của ta. Breaker mở ⇒ **ngừng quarantine**, chỉ alert `fleet-unhealthy`.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/quarantine.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { MEMORY } from "../src/memory-governance.js";
import { FleetBreaker, QuarantineDecider, QuarantineLedger } from "../src/quarantine.js";

function fixedClock(start = 0) {
  let t = start;
  return { now: () => t, advance: (ms: number) => (t += ms) };
}

describe("QuarantineLedger", () => {
  it("quarantines a chain after MEMORY.quarantineAfterOomCount (2) consecutive OOMs", () => {
    expect(MEMORY.quarantineAfterOomCount).toBe(2);
    const led = new QuarantineLedger();
    expect(led.recordOom("chain-a")).toBe(1);
    expect(led.isQuarantined("chain-a")).toBe(false);
    expect(led.recordOom("chain-a")).toBe(2);
    expect(led.isQuarantined("chain-a")).toBe(true);
  });

  it("resets the counter on a successful run — OOMs must be CONSECUTIVE", () => {
    const led = new QuarantineLedger();
    led.recordOom("chain-a");
    led.recordSuccess("chain-a");
    led.recordOom("chain-a");
    expect(led.isQuarantined("chain-a")).toBe(false);
    expect(led.oomCount("chain-a")).toBe(1);
  });

  it("keeps chains independent — one poison chain does not quarantine its neighbour", () => {
    const led = new QuarantineLedger();
    led.recordOom("chain-a");
    led.recordOom("chain-a");
    expect(led.isQuarantined("chain-b")).toBe(false);
  });
});

describe("FleetBreaker", () => {
  it("stays closed below the minimum sample count even at a 100% OOM rate", () => {
    const clock = fixedClock();
    const b = new FleetBreaker({ windowMs: 600_000, minSamples: 10, oomRatePct: 50, now: clock.now });
    for (let i = 0; i < 9; i++) b.record("oom");
    expect(b.isOpen()).toBe(false);
  });

  it("opens once the OOM rate crosses the threshold with enough samples", () => {
    const clock = fixedClock();
    const b = new FleetBreaker({ windowMs: 600_000, minSamples: 10, oomRatePct: 50, now: clock.now });
    for (let i = 0; i < 10; i++) b.record("oom");
    expect(b.isOpen()).toBe(true);
  });

  it("stays closed when the OOM rate is below the threshold", () => {
    const clock = fixedClock();
    const b = new FleetBreaker({ windowMs: 600_000, minSamples: 10, oomRatePct: 50, now: clock.now });
    for (let i = 0; i < 3; i++) b.record("oom");
    for (let i = 0; i < 17; i++) b.record("ok");
    expect(b.isOpen()).toBe(false);
  });

  it("forgets samples older than the window so a fixed incident closes the breaker again", () => {
    const clock = fixedClock();
    const b = new FleetBreaker({ windowMs: 600_000, minSamples: 10, oomRatePct: 50, now: clock.now });
    for (let i = 0; i < 10; i++) b.record("oom");
    expect(b.isOpen()).toBe(true);
    clock.advance(600_001);
    for (let i = 0; i < 10; i++) b.record("ok");
    expect(b.isOpen()).toBe(false);
  });
});

describe("QuarantineDecider", () => {
  it("quarantines a genuine poison chain and raises the poison-chain alert", () => {
    const clock = fixedClock();
    const d = new QuarantineDecider(
      new QuarantineLedger(),
      new FleetBreaker({ windowMs: 600_000, minSamples: 10, oomRatePct: 50, now: clock.now }),
    );
    expect(d.onChainOom("chain-a")).toMatchObject({ quarantined: false, oomCount: 1, alert: null });
    expect(d.onChainOom("chain-a")).toMatchObject({ quarantined: true, oomCount: 2, alert: "poison-chain" });
  });

  it("refuses to quarantine while the fleet breaker is open — the fleet is the suspect, not the chain", () => {
    const clock = fixedClock();
    const breaker = new FleetBreaker({ windowMs: 600_000, minSamples: 4, oomRatePct: 50, now: clock.now });
    const d = new QuarantineDecider(new QuarantineLedger(), breaker);
    for (const key of ["c1", "c2", "c3", "c4"]) d.onChainOom(key);
    const outcome = d.onChainOom("chain-a");
    expect(breaker.isOpen()).toBe(true);
    expect(outcome.quarantined).toBe(false);
    expect(outcome.alert).toBe("fleet-unhealthy");
  });

  it("feeds successes to the breaker so a healthy fleet keeps quarantining poison chains", () => {
    const clock = fixedClock();
    const breaker = new FleetBreaker({ windowMs: 600_000, minSamples: 4, oomRatePct: 50, now: clock.now });
    const d = new QuarantineDecider(new QuarantineLedger(), breaker);
    for (let i = 0; i < 20; i++) d.onChainOk(`ok-${i}`);
    d.onChainOom("chain-a");
    expect(d.onChainOom("chain-a")).toMatchObject({ quarantined: true, alert: "poison-chain" });
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/quarantine.test.ts`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/quarantine.ts`**

```ts
/**
 * A chain that OOMs twice in a row is a POISON CHAIN: retrying it burns a browser slot every
 * time and never converges. MEMORY.quarantineAfterOomCount = 2 is the frozen threshold.
 *
 * The breaker exists because that rule inverts under a fleet-wide fault: a bad image or a sick
 * host makes EVERY chain OOM, and a naive ledger would quarantine a customer's whole backlog
 * for OUR failure. So when the fleet-wide OOM rate crosses the threshold, the breaker opens and
 * quarantining stops — the alert changes from "your chain is poison" to "our fleet is sick".
 */
import { MEMORY } from "./memory-governance.js";

export class QuarantineLedger {
  readonly #consecutiveOom = new Map<string, number>();

  recordOom(chainKey: string): number {
    const next = (this.#consecutiveOom.get(chainKey) ?? 0) + 1;
    this.#consecutiveOom.set(chainKey, next);
    return next;
  }

  recordSuccess(chainKey: string): void {
    this.#consecutiveOom.delete(chainKey);
  }

  oomCount(chainKey: string): number {
    return this.#consecutiveOom.get(chainKey) ?? 0;
  }

  isQuarantined(chainKey: string): boolean {
    return this.oomCount(chainKey) >= MEMORY.quarantineAfterOomCount;
  }
}

export interface FleetBreakerOptions {
  readonly windowMs: number;
  readonly minSamples: number;
  readonly oomRatePct: number;
  readonly now: () => number;
}

export class FleetBreaker {
  readonly #opts: FleetBreakerOptions;
  #samples: { at: number; oom: boolean }[] = [];

  constructor(opts: FleetBreakerOptions) {
    this.#opts = opts;
  }

  record(kind: "ok" | "oom"): void {
    this.#samples.push({ at: this.#opts.now(), oom: kind === "oom" });
    this.#evict();
  }

  isOpen(): boolean {
    this.#evict();
    if (this.#samples.length < this.#opts.minSamples) return false;
    const ooms = this.#samples.filter((s) => s.oom).length;
    return (ooms / this.#samples.length) * 100 >= this.#opts.oomRatePct;
  }

  #evict(): void {
    const cutoff = this.#opts.now() - this.#opts.windowMs;
    this.#samples = this.#samples.filter((s) => s.at > cutoff);
  }
}

export interface QuarantineOutcome {
  readonly quarantined: boolean;
  readonly oomCount: number;
  readonly alert: "poison-chain" | "fleet-unhealthy" | null;
}

export class QuarantineDecider {
  readonly #ledger: QuarantineLedger;
  readonly #breaker: FleetBreaker;

  constructor(ledger: QuarantineLedger, breaker: FleetBreaker) {
    this.#ledger = ledger;
    this.#breaker = breaker;
  }

  onChainOom(chainKey: string): QuarantineOutcome {
    this.#breaker.record("oom");
    const oomCount = this.#ledger.recordOom(chainKey);
    if (this.#breaker.isOpen()) {
      return { quarantined: false, oomCount, alert: "fleet-unhealthy" };
    }
    const quarantined = this.#ledger.isQuarantined(chainKey);
    return { quarantined, oomCount, alert: quarantined ? "poison-chain" : null };
  }

  onChainOk(chainKey: string): void {
    this.#breaker.record("ok");
    this.#ledger.recordSuccess(chainKey);
  }
}
```

- [x] **Step 4: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/quarantine.test.ts`
Expected: PASS (10 test).

- [x] **Step 5: Commit**

```bash
git add apps/runner/src/quarantine.ts apps/runner/test/quarantine.test.ts
git commit -m "M3-fleet T8: quarantine after 2 OOMs + fleet breaker + poison-chain alert"
```

---

## Task 9 — Timeout lồng + phân loại lỗi thành verdict

**Files:**
- Create: `apps/runner/src/executor/timeouts.ts`
- Create: `apps/runner/src/executor/verdict.ts`
- Test: `apps/runner/test/executor/timeouts.test.ts`
- Test: `apps/runner/test/executor/verdict.test.ts`

**Interfaces:**
- Consumes: `MEMORY.timeoutsSec`; `chainTimeoutSeconds` từ `@testkite/run-compiler`; `AppError`, `AssertionFailure`, `RetryableInfraError`, `FatalInfraError` từ `@testkite/contract`.
- Produces:
  ```ts
  interface TimeoutBudget { readonly actionMs: number; readonly navigationMs: number; readonly stepMs: number; readonly chainMs: number }
  function budgetForChain(stepCount: number): TimeoutBudget
  function assertNested(b: TimeoutBudget): void
  class StepTimeoutError extends Error { readonly stepOrdinal: number }
  type ErrorClass = { readonly kind: "assertion"; readonly message: string } | { readonly kind: "retryable-infra"; readonly code: string; readonly message: string } | { readonly kind: "fatal-infra"; readonly code: string; readonly message: string }
  function classifyError(err: unknown): ErrorClass
  ```

**Điểm mấu chốt (spike §10):** `Promise.race` với ngân sách step **KHÔNG huỷ** action Playwright đang chạy — chỉ `context.close()` mới thật sự huỷ. Vì thế `run-chain.ts` (Task 10) đóng context trong `finally` **không ngoại lệ**.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/executor/timeouts.test.ts`**

```ts
import { chainTimeoutSeconds } from "@testkite/run-compiler";
import { describe, expect, it } from "vitest";
import { MEMORY } from "../../src/memory-governance.js";
import { assertNested, budgetForChain } from "../../src/executor/timeouts.js";

describe("budgetForChain", () => {
  it("takes action/navigation/step straight from MEMORY.timeoutsSec", () => {
    const b = budgetForChain(10);
    expect(b.actionMs).toBe(MEMORY.timeoutsSec.action * 1000);
    expect(b.navigationMs).toBe(MEMORY.timeoutsSec.nav * 1000);
    expect(b.stepMs).toBe(MEMORY.timeoutsSec.step * 1000);
    expect([b.actionMs, b.navigationMs, b.stepMs]).toEqual([15_000, 30_000, 60_000]);
  });

  it("reuses the compiler's chain formula rather than recomputing it", () => {
    for (const steps of [0, 1, 7, 50, 200, 1_000]) {
      expect(budgetForChain(steps).chainMs).toBe(chainTimeoutSeconds(steps) * 1000);
    }
  });

  it("honours the floor of 180s for a tiny chain", () => {
    expect(budgetForChain(1).chainMs).toBe(MEMORY.timeoutsSec.chainMin * 1000);
  });

  it("honours the cap of 900s for a huge chain", () => {
    expect(budgetForChain(10_000).chainMs).toBe(MEMORY.timeoutsSec.chainMax * 1000);
  });

  it("produces a strictly nested budget at every size", () => {
    for (const steps of [1, 5, 25, 100, 500]) expect(() => assertNested(budgetForChain(steps))).not.toThrow();
  });
});

describe("assertNested", () => {
  it("rejects a budget where an action could outlive its step", () => {
    expect(() => assertNested({ actionMs: 70_000, navigationMs: 30_000, stepMs: 60_000, chainMs: 180_000 })).toThrow(
      /action/,
    );
  });

  it("rejects a budget where a step could outlive its chain", () => {
    expect(() => assertNested({ actionMs: 15_000, navigationMs: 30_000, stepMs: 200_000, chainMs: 180_000 })).toThrow(
      /step/,
    );
  });
});
```

- [x] **Step 2: Viết test ĐỎ `apps/runner/test/executor/verdict.test.ts`**

```ts
import { AssertionFailure, FatalInfraError, RetryableInfraError } from "@testkite/contract";
import { describe, expect, it } from "vitest";
import { classifyError, StepTimeoutError } from "../../src/executor/verdict.js";

describe("classifyError", () => {
  it("classifies AssertionFailure as a VERDICT, never as an incident", () => {
    const c = classifyError(new AssertionFailure("expected 'Welcome', saw 'Error'"));
    expect(c.kind).toBe("assertion");
    expect(c.message).toContain("Welcome");
  });

  it("classifies RetryableInfraError as retryable infra and keeps its code", () => {
    const c = classifyError(new RetryableInfraError("browser_oom", "killed"));
    expect(c).toMatchObject({ kind: "retryable-infra", code: "browser_oom" });
  });

  it("classifies FatalInfraError as non-retryable infra", () => {
    expect(classifyError(new FatalInfraError("unknown plan format")).kind).toBe("fatal-infra");
  });

  it("treats a step timeout as an ASSERTION (the app hanging is a product signal, §4 taxonomy)", () => {
    const c = classifyError(new StepTimeoutError(3, 60_000));
    expect(c.kind).toBe("assertion");
    expect(c.message).toContain("step 3");
  });

  it("treats a Playwright TimeoutError as an assertion too, not an infra incident", () => {
    const err = new Error("locator.click: Timeout 15000ms exceeded.");
    err.name = "TimeoutError";
    expect(classifyError(err).kind).toBe("assertion");
  });

  it("treats an unknown non-Error throw as FATAL infra rather than guessing a verdict", () => {
    expect(classifyError("boom")).toMatchObject({ kind: "fatal-infra", code: "fatal_infra" });
  });

  it("uses the ONE retry predicate — retryable===true — and nothing else", () => {
    class Weird extends RetryableInfraError {}
    expect(classifyError(new Weird("network", "reset")).kind).toBe("retryable-infra");
  });
});
```

- [x] **Step 3: Chạy cả hai test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/executor/`
Expected: FAIL — hai module không tồn tại.

- [x] **Step 4: Implement `apps/runner/src/executor/timeouts.ts`**

```ts
/**
 * Nested timeouts (docs/SYSTEM_DESIGN.md §5): action 15s < navigation 30s < step 60s <
 * chain clamp(90 + 12×steps, 180..900)s. Nesting is what stops a hung page from holding a
 * browser slot all night — every layer is bounded by the one above it.
 *
 * The chain formula is NOT reimplemented here: it is imported from the compiler, which already
 * stamps `timeoutSeconds` into every ChainPlan. Two copies of one formula is two chances to
 * disagree about how long a chain may live.
 */
import { chainTimeoutSeconds } from "@testkite/run-compiler";
import { MEMORY } from "../memory-governance.js";

export interface TimeoutBudget {
  readonly actionMs: number;
  readonly navigationMs: number;
  readonly stepMs: number;
  readonly chainMs: number;
}

export function budgetForChain(stepCount: number): TimeoutBudget {
  return {
    actionMs: MEMORY.timeoutsSec.action * 1000,
    navigationMs: MEMORY.timeoutsSec.nav * 1000,
    stepMs: MEMORY.timeoutsSec.step * 1000,
    chainMs: chainTimeoutSeconds(stepCount) * 1000,
  };
}

/** Fails loudly if a future edit breaks the nesting — a budget that is not nested silently un-bounds a layer. */
export function assertNested(b: TimeoutBudget): void {
  if (!(b.actionMs < b.navigationMs)) throw new Error(`timeout nesting broken: action ${b.actionMs}ms >= navigation ${b.navigationMs}ms`);
  if (!(b.navigationMs < b.stepMs)) throw new Error(`timeout nesting broken: navigation ${b.navigationMs}ms >= step ${b.stepMs}ms`);
  if (!(b.stepMs < b.chainMs)) throw new Error(`timeout nesting broken: step ${b.stepMs}ms >= chain ${b.chainMs}ms`);
}
```

- [x] **Step 5: Implement `apps/runner/src/executor/verdict.ts`**

```ts
/**
 * ONE predicate gates every retry in the system: `err instanceof AppError && err.retryable`.
 * This file is the single place raw throws are turned into that decision.
 *
 * The load-bearing rule (docs/SYSTEM_DESIGN.md §4): an AssertionFailure is a VERDICT, not an
 * incident. Retrying a verdict poisons result data — the run would flip from failed to passed
 * because a flaky retry got lucky, and the team would stop trusting the whole product. A hanging
 * app is the same thing: `failed(timeout)` is a product signal, not a fleet incident.
 */
import { AppError, AssertionFailure } from "@testkite/contract";

export class StepTimeoutError extends Error {
  readonly stepOrdinal: number;
  constructor(stepOrdinal: number, budgetMs: number) {
    super(`step ${stepOrdinal} exceeded its ${budgetMs}ms budget`);
    this.name = "StepTimeoutError";
    this.stepOrdinal = stepOrdinal;
  }
}

export type ErrorClass =
  | { readonly kind: "assertion"; readonly message: string }
  | { readonly kind: "retryable-infra"; readonly code: string; readonly message: string }
  | { readonly kind: "fatal-infra"; readonly code: string; readonly message: string };

export function classifyError(err: unknown): ErrorClass {
  if (err instanceof AssertionFailure) return { kind: "assertion", message: err.message };
  if (err instanceof StepTimeoutError) return { kind: "assertion", message: err.message };
  if (err instanceof Error && err.name === "TimeoutError") return { kind: "assertion", message: err.message };
  if (err instanceof AppError) {
    return err.retryable
      ? { kind: "retryable-infra", code: err.code, message: err.message }
      : { kind: "fatal-infra", code: err.code, message: err.message };
  }
  return {
    kind: "fatal-infra",
    code: "fatal_infra",
    message: err instanceof Error ? err.message : `non-Error thrown: ${String(err)}`,
  };
}
```

- [x] **Step 6: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/executor/`
Expected: PASS (14 test).

- [x] **Step 7: Commit**

```bash
git add apps/runner/src/executor/timeouts.ts apps/runner/src/executor/verdict.ts apps/runner/test/executor/
git commit -m "M3-fleet T9: nested timeout budget + error taxonomy to verdict mapping"
```

---

## Task 10 — `BrowserEngine` interface + `FakeBrowserEngine`

**Files:**
- Create: `apps/runner/src/browser/engine.ts`
- Create: `apps/runner/src/browser/fake-engine.ts`
- Test: `apps/runner/test/browser/fake-engine.test.ts`

**Interfaces:**
- Consumes: `TimeoutBudget` (Task 9), `OpContext` từ `@testkite/verb-kit`.
- Produces:
  ```ts
  interface ChainContextOptions { readonly contextId: string; readonly baseUrl: string; readonly timeouts: TimeoutBudget; readonly trace: boolean }
  interface EngineContextHandle {
    readonly contextId: string;
    opContext(stepTimeoutMs: number, log: (m: string) => void): OpContext;
    rendererPids(): readonly number[];
    screenshotWebp(): Promise<Buffer>;
    stopTracing(destPath: string | null): Promise<void>;
    close(): Promise<void>;
    readonly closed: boolean;
  }
  interface BrowserEngine {
    readonly kind: "playwright" | "fake";
    newChainContext(o: ChainContextOptions): Promise<EngineContextHandle>;
    browserPid(): number | null;
    contextsServed(): number;
    treeRssBytes(): number;
    crashed(): boolean;
    close(): Promise<void>;
  }
  class FakeBrowserEngine implements BrowserEngine  // scriptable: setScreenshot, setRendererRss, failNextContext…
  ```

**Vì sao có interface:** toàn bộ executor/worker test được **không cần chromium** (CI nào cũng xanh), còn impl thật (Task 11) chỉ phải chứng minh nó *là* một `BrowserEngine`.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/browser/fake-engine.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { FakeBrowserEngine } from "../../src/browser/fake-engine.js";
import { budgetForChain } from "../../src/executor/timeouts.js";

const opts = { contextId: "ctx-1", baseUrl: "https://staging.example.test", timeouts: budgetForChain(5), trace: false };

describe("FakeBrowserEngine", () => {
  it("hands out a context that reports itself open, then closed", async () => {
    const engine = new FakeBrowserEngine();
    const ctx = await engine.newChainContext(opts);
    expect(ctx.closed).toBe(false);
    await ctx.close();
    expect(ctx.closed).toBe(true);
  });

  it("counts contexts served — the input to the recycle rule", async () => {
    const engine = new FakeBrowserEngine();
    for (let i = 0; i < 3; i++) await (await engine.newChainContext({ ...opts, contextId: `c${i}` })).close();
    expect(engine.contextsServed()).toBe(3);
  });

  it("exposes an OpContext carrying the step timeout the executor chose", async () => {
    const engine = new FakeBrowserEngine();
    const ctx = await engine.newChainContext(opts);
    expect(ctx.opContext(60_000, () => {}).stepTimeoutMs).toBe(60_000);
  });

  it("returns scripted screenshot bytes so ring-buffer tests are deterministic", async () => {
    const engine = new FakeBrowserEngine();
    engine.setScreenshot(Buffer.from("webp-a"));
    const ctx = await engine.newChainContext(opts);
    expect((await ctx.screenshotWebp()).toString()).toBe("webp-a");
  });

  it("reports scripted renderer RSS so the L3 monitor can be driven without a browser", async () => {
    const engine = new FakeBrowserEngine();
    const ctx = await engine.newChainContext(opts);
    engine.setRendererRss("ctx-1", 480 * 1024 * 1024);
    expect(engine.rendererRssFor("ctx-1")).toBe(480 * 1024 * 1024);
    expect(ctx.rendererPids().length).toBeGreaterThan(0);
  });

  it("can be scripted to fail context creation, so infra-error paths are testable", async () => {
    const engine = new FakeBrowserEngine();
    engine.failNextContext(new Error("browser has crashed"));
    await expect(engine.newChainContext(opts)).rejects.toThrow(/crashed/);
  });

  it("records whether tracing was stopped with a path (kept) or null (discarded)", async () => {
    const engine = new FakeBrowserEngine();
    const ctx = await engine.newChainContext({ ...opts, trace: true });
    await ctx.stopTracing("/scratch/trace.zip");
    expect(engine.tracesKept).toEqual(["/scratch/trace.zip"]);
    const ctx2 = await engine.newChainContext({ ...opts, contextId: "ctx-2", trace: true });
    await ctx2.stopTracing(null);
    expect(engine.tracesDiscarded).toBe(1);
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/browser/`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/browser/engine.ts`**

```ts
/**
 * The browser boundary. Everything above it (executor, worker, governance) is testable with
 * NO chromium at all; everything below it is one file (playwright-engine.ts) whose job is to
 * satisfy exactly this interface.
 *
 * ONE CONTEXT PER CHAIN is not an optimisation, it is the isolation model (§5): a chain's
 * cookies, storage and logged-in session live and die with its context, so a leaked session can
 * never reach the next tenant's chain. The handle therefore has no "reset" — only `close()`.
 */
import type { OpContext } from "@testkite/verb-kit";
import type { TimeoutBudget } from "../executor/timeouts.js";

export interface ChainContextOptions {
  readonly contextId: string;
  readonly baseUrl: string;
  readonly timeouts: TimeoutBudget;
  /** retain-on-failure: start the trace, then keep it only when the chain fails (§5.1). */
  readonly trace: boolean;
}

export interface EngineContextHandle {
  readonly contextId: string;
  readonly closed: boolean;
  /** The OpContext a verb-kit op runs against. `page` stays `unknown` in the verb contract. */
  opContext(stepTimeoutMs: number, log: (message: string) => void): OpContext;
  /** Renderer pids of THIS context — the L3 monitor sums their RSS. */
  rendererPids(): readonly number[];
  /** WebP q70 via CDP: the Playwright screenshot API cannot emit WebP (§5.2). */
  screenshotWebp(): Promise<Buffer>;
  /** destPath = keep the trace there; null = discard it (the chain passed). */
  stopTracing(destPath: string | null): Promise<void>;
  close(): Promise<void>;
}

export interface BrowserEngine {
  readonly kind: "playwright" | "fake";
  newChainContext(options: ChainContextOptions): Promise<EngineContextHandle>;
  /** The browser process pid — needed to place it in the nested cgroup and set oom_score_adj. */
  browserPid(): number | null;
  contextsServed(): number;
  treeRssBytes(): number;
  crashed(): boolean;
  close(): Promise<void>;
}
```

- [x] **Step 4: Implement `apps/runner/src/browser/fake-engine.ts`**

```ts
/**
 * A scriptable BrowserEngine. Every executor/worker/governance test runs on this: the logic
 * being proved (verdicts, timeouts, shedding, epoch handling) has nothing to do with chromium,
 * and a browser in those tests would only add flake and seconds.
 */
import type { OpContext } from "@testkite/verb-kit";
import type { BrowserEngine, ChainContextOptions, EngineContextHandle } from "./engine.js";

class FakeHandle implements EngineContextHandle {
  readonly contextId: string;
  closed = false;
  readonly #engine: FakeBrowserEngine;
  readonly #pid: number;

  constructor(engine: FakeBrowserEngine, contextId: string, pid: number) {
    this.#engine = engine;
    this.contextId = contextId;
    this.#pid = pid;
  }

  opContext(stepTimeoutMs: number, log: (message: string) => void): OpContext {
    return { page: { contextId: this.contextId }, stepTimeoutMs, log };
  }

  rendererPids(): readonly number[] {
    return [this.#pid];
  }

  async screenshotWebp(): Promise<Buffer> {
    return this.#engine.nextScreenshot();
  }

  async stopTracing(destPath: string | null): Promise<void> {
    if (destPath === null) this.#engine.tracesDiscarded += 1;
    else this.#engine.tracesKept.push(destPath);
  }

  async close(): Promise<void> {
    this.closed = true;
    this.#engine.noteClosed(this.contextId);
  }
}

export class FakeBrowserEngine implements BrowserEngine {
  readonly kind = "fake" as const;
  readonly tracesKept: string[] = [];
  tracesDiscarded = 0;
  readonly openContextIds = new Set<string>();
  #served = 0;
  #nextPid = 9000;
  #screenshot = Buffer.from("fake-webp");
  #failNext: Error | null = null;
  #crashed = false;
  readonly #rendererRss = new Map<string, number>();

  async newChainContext(options: ChainContextOptions): Promise<EngineContextHandle> {
    if (this.#failNext !== null) {
      const err = this.#failNext;
      this.#failNext = null;
      throw err;
    }
    this.#served += 1;
    this.openContextIds.add(options.contextId);
    return new FakeHandle(this, options.contextId, this.#nextPid++);
  }

  browserPid(): number | null {
    return 8000;
  }

  contextsServed(): number {
    return this.#served;
  }

  treeRssBytes(): number {
    let total = 0;
    for (const v of this.#rendererRss.values()) total += v;
    return total;
  }

  crashed(): boolean {
    return this.#crashed;
  }

  async close(): Promise<void> {
    this.openContextIds.clear();
  }

  // --- test scripting surface ---
  setScreenshot(bytes: Buffer): void {
    this.#screenshot = bytes;
  }

  nextScreenshot(): Buffer {
    return this.#screenshot;
  }

  setRendererRss(contextId: string, bytes: number): void {
    this.#rendererRss.set(contextId, bytes);
  }

  rendererRssFor(contextId: string): number | null {
    return this.#rendererRss.get(contextId) ?? null;
  }

  failNextContext(err: Error): void {
    this.#failNext = err;
  }

  setCrashed(value: boolean): void {
    this.#crashed = value;
  }

  noteClosed(contextId: string): void {
    this.openContextIds.delete(contextId);
    this.#rendererRss.delete(contextId);
  }
}
```

- [x] **Step 5: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/browser/`
Expected: PASS (7 test).

- [x] **Step 6: Commit**

```bash
git add apps/runner/src/browser/engine.ts apps/runner/src/browser/fake-engine.ts apps/runner/test/browser/
git commit -m "M3-fleet T10: BrowserEngine boundary + scriptable fake engine"
```

---

## Task 11 — Executor: 1 context/chain, `finally` đóng, verdict đúng taxonomy

**Files:**
- Create: `apps/runner/src/executor/step-runner.ts`
- Create: `apps/runner/src/executor/run-chain.ts`
- Test: `apps/runner/test/executor/run-chain.test.ts`

**Interfaces:**
- Consumes: `BrowserEngine`/`EngineContextHandle` (T10), `TimeoutBudget`/`classifyError`/`StepTimeoutError` (T9), `ChainPlan`/`CasePlan`/`StepPlan`/`RunPolicy` từ `@testkite/run-compiler`, `VerbDefinition`/`OpResult` từ `@testkite/verb-kit`.
- Produces:
  ```ts
  type VerbResolver = (opKey: string) => VerbDefinition | undefined
  interface StepOutcome { readonly caseId: string; readonly ordinal: number; readonly status: "passed" | "failed"; readonly durationMs: number; readonly message?: string; readonly screenshotSha256?: string }
  interface ChainOutcome { readonly chainKey: string; readonly verdict: "passed" | "failed" | "infra_error"; readonly steps: readonly StepOutcome[]; readonly infra?: { readonly code: string; readonly retryable: boolean; readonly message: string } }
  interface RunChainDeps { readonly engine: BrowserEngine; readonly resolveVerb: VerbResolver; readonly now: () => number; readonly onStep: (o: StepOutcome) => void; readonly screenshot: (handle: EngineContextHandle, step: StepOutcome) => Promise<string | null>; readonly log: (m: string) => void }
  function runChain(chain: ChainPlan, policy: RunPolicy, deps: RunChainDeps): Promise<ChainOutcome>
  ```

**Phạm vi M3 có chủ đích:** executor chạy step `kind: "action"` (qua verb registry) và `kind: "for"` (lặp theo `loopRows` đã đóng băng trong plan). `if` / `while` / `rest` cần verb điều kiện + verb HTTP **chưa port** (verb-kit hiện chỉ có `web.click`, `web.enter`, cả hai còn `TODO(M4)`) ⇒ M3 ném `FatalInfraError` gọi đúng tên kind. Đó là biên giới M4 tường minh, không phải bỏ sót: im lặng bỏ qua một `if` sẽ đổi ngữ nghĩa test mà không ai biết.

**Sửa sau review (30-08-2026):** `for` phải BIND dữ liệu, không chỉ đếm vòng. Compiler CỐ Ý giữ
nguyên literal `$data:<column>` trong args của step con nằm trong `for` (`phase45-resolve.ts`:
"the data column belongs to the loop row, which only the worker knows"), nên worker phải thay
`$data:<column>` bằng `loopRows[i].values[column]` ở MỖI vòng — nếu không, một `for` qua 3 hàng
sẽ gọi verb 3 lần với đúng một bộ args chứa chuỗi thô `"$data:user"`, tức là không hề
data-driven. Luật thay thế sao y `mergeArgs`: chỉ ref nguyên chuỗi, ĐÚNG MỘT LƯỢT (giá trị đã
thay không bao giờ được quét lại ⇒ dữ liệu test không thể tự biến thành `$secret:`), `$secret:`
/ `$env:` không đụng tới, cột không tồn tại thì GIỮ NGUYÊN. `for` lồng nhau: hàng trong đè hàng
ngoài theo từng cột. Xem `bindLoopArgs` trong `step-runner.ts`.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/executor/run-chain.test.ts`**

```ts
import { AssertionFailure, RetryableInfraError } from "@testkite/contract";
import type { CasePlan, ChainPlan, RunPolicy, StepPlan } from "@testkite/run-compiler";
import type { VerbDefinition } from "@testkite/verb-kit";
import { describe, expect, it, vi } from "vitest";
import { FakeBrowserEngine } from "../../src/browser/fake-engine.js";
import { runChain, type RunChainDeps } from "../../src/executor/run-chain.js";

const policy: RunPolicy = {
  lane: "batch",
  engine: "chromium-headless-shell",
  retry: "infra-only",
  screenshots: "failure",
  baseUrl: "https://staging.example.test",
};

function actionStep(ordinal: number, opKey = "web.click"): StepPlan {
  return { kind: "action", ordinal, renderedSentence: `Click on button ${ordinal}`, groupPath: [], args: { element: "btn" }, opKey };
}

function chainOf(steps: readonly StepPlan[]): ChainPlan {
  const kase: CasePlan = { caseId: "case-1", revisionId: "rev-1", expectedToFail: false, steps };
  return { chainKey: "login>checkout", cases: [kase], stepCount: steps.length, timeoutSeconds: 180 };
}

function verb(execute: VerbDefinition["execute"]): VerbDefinition {
  return { opKey: "web.click", sentence: "Click on {element}", params: [], needsRendering: true, execute };
}

function deps(engine: FakeBrowserEngine, execute: VerbDefinition["execute"], over: Partial<RunChainDeps> = {}): RunChainDeps {
  return {
    engine,
    resolveVerb: () => verb(execute),
    now: () => Date.now(),
    onStep: () => {},
    screenshot: async () => null,
    log: () => {},
    ...over,
  };
}

describe("runChain", () => {
  it("passes a chain whose every step returns ok", async () => {
    const engine = new FakeBrowserEngine();
    const out = await runChain(chainOf([actionStep(1), actionStep(2)]), policy, deps(engine, async () => ({ ok: true })));
    expect(out.verdict).toBe("passed");
    expect(out.steps.map((s) => s.status)).toEqual(["passed", "passed"]);
  });

  it("uses exactly ONE context for the whole chain", async () => {
    const engine = new FakeBrowserEngine();
    await runChain(chainOf([actionStep(1), actionStep(2), actionStep(3)]), policy, deps(engine, async () => ({ ok: true })));
    expect(engine.contextsServed()).toBe(1);
  });

  it("closes the context in finally even when a step throws", async () => {
    const engine = new FakeBrowserEngine();
    await runChain(chainOf([actionStep(1)]), policy, deps(engine, async () => { throw new Error("kaboom"); }));
    expect(engine.openContextIds.size).toBe(0);
  });

  it("closes the context in finally even when the chain times out", async () => {
    const engine = new FakeBrowserEngine();
    const slow = chainOf([actionStep(1)]);
    const out = await runChain({ ...slow, timeoutSeconds: 0 }, policy, deps(engine, async () => {
      await new Promise((r) => setTimeout(r, 50));
      return { ok: true };
    }));
    expect(engine.openContextIds.size).toBe(0);
    expect(out.verdict).toBe("failed");
  });

  it("turns an ok=false OpResult into verdict=failed and stops the chain", async () => {
    const engine = new FakeBrowserEngine();
    const execute = vi.fn(async () => ({ ok: false, failureMessage: "button not found" }));
    const out = await runChain(chainOf([actionStep(1), actionStep(2)]), policy, deps(engine, execute));
    expect(out.verdict).toBe("failed");
    expect(out.steps).toHaveLength(1);
    expect(out.steps[0]?.message).toContain("button not found");
    expect(execute).toHaveBeenCalledTimes(1);
  });

  it("turns a thrown AssertionFailure into verdict=failed, NEVER infra_error", async () => {
    const engine = new FakeBrowserEngine();
    const out = await runChain(chainOf([actionStep(1)]), policy, deps(engine, async () => { throw new AssertionFailure("text mismatch"); }));
    expect(out.verdict).toBe("failed");
    expect(out.infra).toBeUndefined();
  });

  it("reports a RetryableInfraError as infra_error, not as a verdict", async () => {
    const engine = new FakeBrowserEngine();
    const out = await runChain(chainOf([actionStep(1)]), policy, deps(engine, async () => { throw new RetryableInfraError("browser_oom", "killed"); }));
    expect(out.verdict).toBe("infra_error");
    expect(out.infra).toMatchObject({ code: "browser_oom", retryable: true });
  });

  it("reports a failure to create the context as infra_error and never as failed", async () => {
    const engine = new FakeBrowserEngine();
    engine.failNextContext(new RetryableInfraError("context_crash", "no context"));
    const out = await runChain(chainOf([actionStep(1)]), policy, deps(engine, async () => ({ ok: true })));
    expect(out.verdict).toBe("infra_error");
    expect(out.infra?.code).toBe("context_crash");
  });

  it("fails fatally when a step's opKey is not in the registry", async () => {
    const engine = new FakeBrowserEngine();
    const out = await runChain(chainOf([actionStep(1, "web.nope")]), policy, {
      ...deps(engine, async () => ({ ok: true })),
      resolveVerb: () => undefined,
    });
    expect(out.verdict).toBe("infra_error");
    expect(out.infra).toMatchObject({ retryable: false });
    expect(out.infra?.message).toContain("web.nope");
  });

  it("runs a for-block once per loop row from the frozen plan", async () => {
    const engine = new FakeBrowserEngine();
    const forStep: StepPlan = {
      kind: "for", ordinal: 1, renderedSentence: "For each row", groupPath: [], args: {},
      children: [actionStep(2)],
      loopRows: [{ user: "a" }, { user: "b" }, { user: "c" }],
    };
    const execute = vi.fn(async () => ({ ok: true }));
    const out = await runChain(chainOf([forStep]), policy, deps(engine, execute));
    expect(out.verdict).toBe("passed");
    expect(execute).toHaveBeenCalledTimes(3);
  });

  it("refuses an if/while/rest step with a NAMED fatal error (the explicit M4 boundary)", async () => {
    const engine = new FakeBrowserEngine();
    const ifStep: StepPlan = { kind: "if", ordinal: 1, renderedSentence: "If visible", groupPath: [], args: {}, children: [actionStep(2)] };
    const out = await runChain(chainOf([ifStep]), policy, deps(engine, async () => ({ ok: true })));
    expect(out.verdict).toBe("infra_error");
    expect(out.infra?.message).toContain("if");
    expect(out.infra?.retryable).toBe(false);
  });

  it("emits a step event for every finished step so progress is reportable live", async () => {
    const engine = new FakeBrowserEngine();
    const onStep = vi.fn();
    await runChain(chainOf([actionStep(1), actionStep(2)]), policy, deps(engine, async () => ({ ok: true }), { onStep }));
    expect(onStep).toHaveBeenCalledTimes(2);
  });

  it("attaches the screenshot hash the artifact layer returned", async () => {
    const engine = new FakeBrowserEngine();
    const out = await runChain(chainOf([actionStep(1)]), policy, deps(engine, async () => ({ ok: true }), {
      screenshot: async () => "abc123",
    }));
    expect(out.steps[0]?.screenshotSha256).toBe("abc123");
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/executor/run-chain.test.ts`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/executor/step-runner.ts`**

```ts
/**
 * Walks a CasePlan's steps. The plan is already FROZEN by the compiler — every locator, every
 * argument and every loop row is baked in — so this file never looks anything up: no DB, no
 * element table, no authoring read. That is what makes a run reproducible.
 *
 * M3 executes `action` and `for`. `if` / `while` / `rest` need condition verbs and an HTTP verb
 * that are not ported until M4 (verb-kit currently registers web.click and web.enter, both still
 * TODO(M4)). Rather than silently skipping them — which would change what a test MEANS without
 * anyone noticing — an unsupported kind raises a NAMED fatal infra error.
 */
import { FatalInfraError } from "@testkite/contract";
import type { CasePlan, StepPlan } from "@testkite/run-compiler";
import type { VerbDefinition } from "@testkite/verb-kit";
import type { EngineContextHandle } from "../browser/engine.js";
import { StepTimeoutError } from "./verdict.js";

export type VerbResolver = (opKey: string) => VerbDefinition | undefined;

export interface StepOutcome {
  readonly caseId: string;
  readonly ordinal: number;
  readonly status: "passed" | "failed";
  readonly durationMs: number;
  readonly message?: string;
  readonly screenshotSha256?: string;
}

export interface StepRunnerDeps {
  readonly handle: EngineContextHandle;
  readonly resolveVerb: VerbResolver;
  readonly stepTimeoutMs: number;
  readonly now: () => number;
  readonly onStep: (outcome: StepOutcome) => void;
  readonly screenshot: (handle: EngineContextHandle, outcome: StepOutcome) => Promise<string | null>;
  readonly log: (message: string) => void;
}

/** Thrown when a step legitimately fails — the chain stops here with verdict=failed. */
export class StepFailed extends Error {
  readonly outcome: StepOutcome;
  constructor(outcome: StepOutcome) {
    super(outcome.message ?? "step failed");
    this.name = "StepFailed";
    this.outcome = outcome;
  }
}

export async function runCase(kase: CasePlan, deps: StepRunnerDeps): Promise<readonly StepOutcome[]> {
  const outcomes: StepOutcome[] = [];
  await runSteps(kase.steps, kase.caseId, deps, outcomes);
  return outcomes;
}

async function runSteps(
  steps: readonly StepPlan[],
  caseId: string,
  deps: StepRunnerDeps,
  out: StepOutcome[],
): Promise<void> {
  for (const step of steps) {
    if (step.kind === "action") {
      await runAction(step, caseId, deps, out);
      continue;
    }
    if (step.kind === "for") {
      // Loop rows were resolved at compile time; the body runs once per row, in order, with that
      // row's columns bound into the body's `$data:` args (a nested `for` shadows column by column).
      const rows: readonly DataRow[] = step.loopRows ?? [];
      for (const [i, current] of rows.entries()) {
        deps.log(`for-loop iteration ${i + 1}/${rows.length} (row "${current.label}") at ordinal ${step.ordinal}`);
        await runSteps(step.children, caseId, deps, out, { ...loopRow, ...current.values });
      }
      continue;
    }
    throw new FatalInfraError(
      `step kind "${step.kind}" at ordinal ${step.ordinal} is not executable yet — condition and REST verbs land in M4`,
    );
  }
}

async function runAction(
  step: Extract<StepPlan, { kind: "action" }>,
  caseId: string,
  deps: StepRunnerDeps,
  out: StepOutcome[],
): Promise<void> {
  const verb = deps.resolveVerb(step.opKey);
  if (verb === undefined) {
    throw new FatalInfraError(`opKey "${step.opKey}" is not in the verb registry — the plan was compiled against a different build`);
  }

  const startedAt = deps.now();
  const ctx = verb.execute(deps.handle.opContext(deps.stepTimeoutMs, deps.log), step.args);

  // The step budget bounds the WAIT, not the work: Playwright keeps running after a lost race
  // (spike 2026-08-29), which is exactly why run-chain.ts closes the context in `finally`.
  const result = await Promise.race([
    ctx,
    new Promise<never>((_resolve, reject) =>
      setTimeout(() => reject(new StepTimeoutError(step.ordinal, deps.stepTimeoutMs)), deps.stepTimeoutMs).unref?.(),
    ),
  ]);

  const base: StepOutcome = {
    caseId,
    ordinal: step.ordinal,
    status: result.ok ? "passed" : "failed",
    durationMs: deps.now() - startedAt,
    ...(result.ok ? {} : { message: result.failureMessage ?? "the op reported a failure without a message" }),
  };

  const sha = await deps.screenshot(deps.handle, base);
  const outcome: StepOutcome = sha === null ? base : { ...base, screenshotSha256: sha };
  out.push(outcome);
  deps.onStep(outcome);

  if (!result.ok) throw new StepFailed(outcome);
}
```

- [x] **Step 4: Implement `apps/runner/src/executor/run-chain.ts`**

```ts
/**
 * One chain, one BrowserContext, closed in `finally` — NO exceptions (docs/SYSTEM_DESIGN.md §5).
 *
 * `finally` is load-bearing twice over. (1) Isolation: the context holds the chain's session, so
 * leaving it open would leak one tenant's login into the next chain. (2) Cancellation: the
 * 2026-08-29 spike showed a lost `Promise.race` does NOT cancel an in-flight Playwright action —
 * the only thing that truly stops it is closing the context. A chain that times out and leaves
 * its context open is exactly how the old system leaked browsers until the host died.
 */
import { FatalInfraError } from "@testkite/contract";
import type { ChainPlan, RunPolicy } from "@testkite/run-compiler";
import type { BrowserEngine, EngineContextHandle } from "../browser/engine.js";
import { runCase, StepFailed, type StepOutcome, type VerbResolver } from "./step-runner.js";
import { assertNested, budgetForChain } from "./timeouts.js";
import { classifyError } from "./verdict.js";

export interface ChainOutcome {
  readonly chainKey: string;
  readonly verdict: "passed" | "failed" | "infra_error";
  readonly steps: readonly StepOutcome[];
  readonly infra?: { readonly code: string; readonly retryable: boolean; readonly message: string };
}

export interface RunChainDeps {
  readonly engine: BrowserEngine;
  readonly resolveVerb: VerbResolver;
  readonly now: () => number;
  readonly onStep: (outcome: StepOutcome) => void;
  readonly screenshot: (handle: EngineContextHandle, outcome: StepOutcome) => Promise<string | null>;
  readonly log: (message: string) => void;
}

class ChainTimeout extends Error {
  constructor(ms: number) {
    super(`chain exceeded its ${ms}ms budget`);
    this.name = "TimeoutError"; // classified as an assertion: a hanging app is a product signal
  }
}

export async function runChain(chain: ChainPlan, policy: RunPolicy, deps: RunChainDeps): Promise<ChainOutcome> {
  const budget = budgetForChain(chain.stepCount);
  const chainMs = chain.timeoutSeconds * 1000;
  // The plan's own stamped timeout wins; assertNested still guards the layers below it.
  assertNested({ ...budget, chainMs: Math.max(chainMs, budget.stepMs + 1) });

  const contextId = `${chain.chainKey}#${deps.now()}`;
  const steps: StepOutcome[] = [];

  let handle: EngineContextHandle | null = null;
  try {
    handle = await deps.engine.newChainContext({
      contextId,
      baseUrl: policy.baseUrl,
      timeouts: { ...budget, chainMs },
      trace: true, // always start; retain-on-failure decides whether to KEEP it
    });

    const work = (async () => {
      for (const kase of chain.cases) {
        steps.push(...(await runCase(kase, {
          handle: handle as EngineContextHandle,
          resolveVerb: deps.resolveVerb,
          stepTimeoutMs: budget.stepMs,
          now: deps.now,
          onStep: deps.onStep,
          screenshot: deps.screenshot,
          log: deps.log,
        })));
      }
    })();

    await Promise.race([
      work,
      new Promise<never>((_resolve, reject) => setTimeout(() => reject(new ChainTimeout(chainMs)), chainMs).unref?.()),
    ]);

    return { chainKey: chain.chainKey, verdict: "passed", steps };
  } catch (err) {
    if (err instanceof StepFailed) {
      return { chainKey: chain.chainKey, verdict: "failed", steps };
    }
    const cls = classifyError(err);
    if (cls.kind === "assertion") {
      return { chainKey: chain.chainKey, verdict: "failed", steps };
    }
    return {
      chainKey: chain.chainKey,
      verdict: "infra_error",
      steps,
      infra: { code: cls.code, retryable: cls.kind === "retryable-infra", message: cls.message },
    };
  } finally {
    // NOTHING may skip this — not a throw, not a timeout, not a cancellation.
    if (handle !== null) {
      try {
        await handle.close();
      } catch (closeErr) {
        deps.log(`context ${contextId} failed to close: ${String(closeErr)}`);
      }
    }
  }
}

/** Re-exported so callers do not need to know which file inside executor/ defines it. */
export { FatalInfraError };
export type { StepOutcome, VerbResolver };
```

- [x] **Step 5: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/executor/run-chain.test.ts`
Expected: PASS (13 test).

- [x] **Step 6: Commit**

```bash
git add apps/runner/src/executor/step-runner.ts apps/runner/src/executor/run-chain.ts \
        apps/runner/test/executor/run-chain.test.ts
git commit -m "M3-fleet T11: executor — one context per chain, finally close, verdict taxonomy"
```

---

## Task 12 — `PlaywrightBrowserEngine` thật + quy tội renderer theo context

**Files:**
- Create: `apps/runner/src/browser/playwright-engine.ts`
- Test: `apps/runner/test/browser/playwright-engine.test.ts`
- Test (chỉ host non-root, sửa review 30-08): `apps/runner/test/host/chromium-sandbox.test.ts`

**Interfaces:**
- Consumes: `BrowserEngine`/`EngineContextHandle` (T10), `readRssBytes`/`sumRssBytes` (T2), `playwright-core`.
- Produces: `class PlaywrightBrowserEngine implements BrowserEngine`, `launchPlaywrightEngine(opts: { headlessShellPath?: string; traceDir: string; sandbox?: "on" | "off-root-dev-only" }): Promise<PlaywrightBrowserEngine>`, `resolveChromiumSandbox(mode, uid)`.

**Sửa sau review (30-08-2026) — sandbox:** bản đầu KHÔNG truyền `chromiumSandbox` nên playwright tự thêm `--no-sandbox`: code/comment/commit nói "sandbox BẬT" trong khi `/proc/<pid>/cmdline` nói ngược lại. Nay: mặc định `chromiumSandbox: true`; opt-out `sandbox: "off-root-dev-only"` chỉ chấp nhận uid 0 (ném lỗi ở uid khác ⇒ worker uid 10001 không thể tụt sandbox), mỗi lần opt-out in cảnh báo. Chứng minh: 4 test thuần (policy, không cần browser) + 1 test đọc `/proc/<pid>/cmdline` thật + 1 test root chứng minh launch mặc định BỊ chromium từ chối; sandbox BẬT thật sự chỉ chứng minh được ở `test/host/chromium-sandbox.test.ts` (host non-root).

**Cơ chế quy tội (đã chứng minh trong spike §2):** chụp `SystemInfo.getProcessInfo` **trước và sau** `newContext()+newPage()`; pid mới xuất hiện chính là renderer của context đó; RSS đọc từ `/proc/<pid>/statm`.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/browser/playwright-engine.test.ts`**

```ts
import { mkdtempSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { afterAll, beforeAll, describe, expect, it } from "vitest";
import { launchPlaywrightEngine, type PlaywrightBrowserEngine } from "../../src/browser/playwright-engine.js";
import { budgetForChain } from "../../src/executor/timeouts.js";

/**
 * Real chromium-headless-shell. Verified available in this environment on 2026-08-29
 * (PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers, launch 1118ms). This dev/CI box is ROOT, where
 * chromium refuses to sandbox, so this file asks for the opt-out EXPLICITLY and everything it
 * measures is unsandboxed; the sandboxed shape is proven in `test/host/chromium-sandbox.test.ts`.
 * If a machine has no browser, the whole file skips: every logic test already runs on the fake.
 */
let available = true;
let engine: PlaywrightBrowserEngine;
const traceDir = mkdtempSync(join(tmpdir(), "tk-trace-"));
const rootHere = process.getuid?.() === 0;

beforeAll(async () => {
  try {
    engine = await launchPlaywrightEngine(rootHere ? { traceDir, sandbox: "off-root-dev-only" } : { traceDir });
  } catch {
    available = false;
  }
});

afterAll(async () => {
  if (available) await engine.close();
});

const opts = { baseUrl: "https://example.invalid", timeouts: budgetForChain(3), trace: false };

describe.skipIf(!available)("PlaywrightBrowserEngine", () => {
  it("reports a browser pid so the worker can place it in the nested cgroup", () => {
    expect(engine.browserPid()).toBeGreaterThan(0);
  });

  it("gives each context its OWN renderer pids (this is what per-context RSS attribution needs)", async () => {
    const a = await engine.newChainContext({ ...opts, contextId: "a" });
    const b = await engine.newChainContext({ ...opts, contextId: "b" });
    const pidsA = a.rendererPids();
    const pidsB = b.rendererPids();
    expect(pidsA.length).toBeGreaterThan(0);
    expect(pidsB.length).toBeGreaterThan(0);
    expect(pidsA.some((p) => pidsB.includes(p))).toBe(false);
    await a.close();
    await b.close();
  });

  it("attributes a memory balloon to the guilty context only", async () => {
    const a = await engine.newChainContext({ ...opts, contextId: "a" });
    const b = await engine.newChainContext({ ...opts, contextId: "b" });
    const before = engine.contextRssBytes("a");
    await engine.evaluateForTest("b", "globalThis.__b = []; for (let i=0;i<25;i++) globalThis.__b.push(new Uint8Array(8*1024*1024).fill(1));");
    await new Promise((r) => setTimeout(r, 400));
    expect(engine.contextRssBytes("b")).toBeGreaterThan(before + 150 * 1024 * 1024);
    expect(engine.contextRssBytes("a")).toBeLessThan(before + 50 * 1024 * 1024);
    await a.close();
    await b.close();
  });

  it("emits WebP screenshots (the Playwright API cannot, CDP can)", async () => {
    const ctx = await engine.newChainContext({ ...opts, contextId: "shot" });
    const bytes = await ctx.screenshotWebp();
    // RIFF....WEBP magic
    expect(bytes.subarray(0, 4).toString("ascii")).toBe("RIFF");
    expect(bytes.subarray(8, 12).toString("ascii")).toBe("WEBP");
    await ctx.close();
  });

  it("writes a trace.zip when the trace is kept and no file when it is discarded", async () => {
    const kept = await engine.newChainContext({ ...opts, contextId: "kept", trace: true });
    const path = join(traceDir, "kept.zip");
    await kept.stopTracing(path);
    await kept.close();
    const { existsSync } = await import("node:fs");
    expect(existsSync(path)).toBe(true);

    const dropped = await engine.newChainContext({ ...opts, contextId: "dropped", trace: true });
    await dropped.stopTracing(null);
    await dropped.close();
    expect(existsSync(join(traceDir, "dropped.zip"))).toBe(false);
  });

  it("leaves NO renderer behind after close — this is the anti-leak assertion", async () => {
    const ctx = await engine.newChainContext({ ...opts, contextId: "leak" });
    const pids = [...ctx.rendererPids()];
    await ctx.close();
    await new Promise((r) => setTimeout(r, 500)); // the spike showed reaping is not instant
    const { readRssBytes } = await import("../../src/memory/rss.js");
    expect(pids.map((p) => readRssBytes(p))).toEqual(pids.map(() => null));
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/browser/playwright-engine.test.ts`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/browser/playwright-engine.ts`**

```ts
/**
 * The ONLY file in the runner that touches Playwright — and Playwright exists ONLY in the runner
 * image. `apps/core` never imports it (CI gate since M1): the browser lives in the fleet, not in
 * the API. That single boundary is the reason the old system's OOM class cannot be rewritten
 * here (docs/SYSTEM_DESIGN.md §1).
 *
 * PER-CONTEXT MEMORY ATTRIBUTION, verified on 2026-08-29: CDP `SystemInfo.getProcessInfo`
 * returns {type,id} for every browser process. Diffing that set around newContext()+newPage()
 * yields exactly the renderer pids of the new context — ballooning one context moved only its
 * own renderer (84MB → 332MB) and left the neighbour at 84MB. RSS then comes from
 * /proc/<pid>/statm. `Performance.getMetrics` is NOT a substitute: it reported JSHeapUsedSize
 * of 866KB while the same renderer held 84MB of RSS.
 *
 * WebP: Playwright's screenshot API has no WebP option; CDP `Page.captureScreenshot` does
 * (§5.2). Measured on the same viewport: WebP q70 2 362B vs JPEG q70 6 392B.
 *
 * The Chromium sandbox stays ON — never `--no-sandbox` (§5). playwright-core defaults
 * `chromiumSandbox` to FALSE, so the option is passed explicitly; the only opt-out is
 * `sandbox: "off-root-dev-only"`, accepted for uid 0 (root dev/CI, where chromium's zygote
 * refuses to sandbox) and refused everywhere else, the worker container included.
 */
import type { Browser, BrowserContext, CDPSession, Page } from "playwright-core";
import { chromium } from "playwright-core";
import { sumRssBytes } from "../memory/rss.js";
import type { BrowserEngine, ChainContextOptions, EngineContextHandle } from "./engine.js";

interface ContextRecord {
  readonly context: BrowserContext;
  readonly page: Page;
  readonly cdp: CDPSession;
  readonly rendererPids: readonly number[];
  readonly tracing: boolean;
}

class PlaywrightHandle implements EngineContextHandle {
  readonly contextId: string;
  closed = false;
  readonly #engine: PlaywrightBrowserEngine;
  readonly #record: ContextRecord;

  constructor(engine: PlaywrightBrowserEngine, contextId: string, record: ContextRecord) {
    this.#engine = engine;
    this.contextId = contextId;
    this.#record = record;
  }

  opContext(stepTimeoutMs: number, log: (message: string) => void) {
    return { page: this.#record.page as unknown, stepTimeoutMs, log };
  }

  rendererPids(): readonly number[] {
    return this.#record.rendererPids;
  }

  async screenshotWebp(): Promise<Buffer> {
    const shot = await this.#record.cdp.send("Page.captureScreenshot", { format: "webp", quality: 70 });
    return Buffer.from(shot.data, "base64");
  }

  async stopTracing(destPath: string | null): Promise<void> {
    if (!this.#record.tracing) return;
    // No path ⇒ the trace is dropped: retain-on-failure means a green chain costs nothing (§5.1).
    await (destPath === null
      ? this.#record.context.tracing.stop()
      : this.#record.context.tracing.stop({ path: destPath }));
  }

  async close(): Promise<void> {
    if (this.closed) return;
    this.closed = true;
    await this.#record.context.close();
    this.#engine.forget(this.contextId);
  }
}

export interface LaunchOptions {
  readonly traceDir: string;
  /** Overrides the bundled chromium-headless-shell; used by the container image. */
  readonly headlessShellPath?: string;
  /** Defaults to `"on"`. Dropping the OS sandbox is never implicit. */
  readonly sandbox?: SandboxMode;
}

/** Printed on every unsandboxed launch: dropping OS isolation must never be a silent event. */
export const UNSANDBOXED_LAUNCH_WARNING =
  "[playwright-engine] chromium is launching WITHOUT its OS sandbox (--no-sandbox) because this process is root";

export class PlaywrightBrowserEngine implements BrowserEngine {
  readonly kind = "playwright" as const;
  readonly #browser: Browser;
  readonly #browserCdp: CDPSession;
  readonly #contexts = new Map<string, ContextRecord>();
  #served = 0;
  #crashed = false;

  constructor(browser: Browser, browserCdp: CDPSession) {
    this.#browser = browser;
    this.#browserCdp = browserCdp;
    this.#browser.on("disconnected", () => {
      this.#crashed = true;
    });
  }

  async newChainContext(options: ChainContextOptions): Promise<EngineContextHandle> {
    const before = new Set(await this.#processPids());

    const context = await this.#browser.newContext({ viewport: { width: 1280, height: 720 } });
    context.setDefaultTimeout(options.timeouts.actionMs);
    context.setDefaultNavigationTimeout(options.timeouts.navigationMs);
    if (options.trace) await context.tracing.start({ screenshots: true, snapshots: true, sources: false });

    const page = await context.newPage();
    const cdp = await context.newCDPSession(page);

    const rendererPids = (await this.#processPids()).filter((pid) => !before.has(pid));
    this.#contexts.set(options.contextId, { context, page, cdp, rendererPids, tracing: options.trace });
    this.#served += 1;
    return new PlaywrightHandle(this, options.contextId, this.#contexts.get(options.contextId) as ContextRecord);
  }

  browserPid(): number | null {
    // Playwright does not expose the pid; CDP does, tagged type="browser".
    return this.#lastBrowserPid;
  }

  contextsServed(): number {
    return this.#served;
  }

  treeRssBytes(): number {
    return sumRssBytes(this.#lastKnownPids);
  }

  crashed(): boolean {
    return this.#crashed;
  }

  /** RSS of one context = the sum over its own renderer processes. */
  contextRssBytes(contextId: string): number {
    const rec = this.#contexts.get(contextId);
    return rec === undefined ? 0 : sumRssBytes(rec.rendererPids);
  }

  forget(contextId: string): void {
    this.#contexts.delete(contextId);
  }

  /** Test-only hook: runs JS inside one context's page so attribution can be proved. */
  async evaluateForTest(contextId: string, script: string): Promise<void> {
    const rec = this.#contexts.get(contextId);
    if (rec === undefined) throw new Error(`no such context: ${contextId}`);
    await rec.page.evaluate(script);
  }

  async close(): Promise<void> {
    for (const [, rec] of this.#contexts) await rec.context.close().catch(() => undefined);
    this.#contexts.clear();
    await this.#browser.close();
  }

  #lastKnownPids: number[] = [];
  #lastBrowserPid: number | null = null;

  async #processPids(): Promise<number[]> {
    const info = (await this.#browserCdp.send("SystemInfo.getProcessInfo")) as {
      processInfo: { type: string; id: number }[];
    };
    this.#lastKnownPids = info.processInfo.map((p) => p.id);
    this.#lastBrowserPid = info.processInfo.find((p) => p.type === "browser")?.id ?? this.#lastBrowserPid;
    // Only renderers are attributable to a context; GPU/network are shared infrastructure.
    return info.processInfo.filter((p) => p.type === "renderer").map((p) => p.id);
  }
}

export type SandboxMode = "on" | "off-root-dev-only";

/** Pure, so the policy is provable on the very box that cannot launch a sandboxed chromium. */
export function resolveChromiumSandbox(mode: SandboxMode | undefined, uid: number): boolean {
  if (mode === undefined || mode === "on") return true;
  if (uid !== 0) throw new Error(`opt-out is for uid 0 only, this process is uid ${String(uid)}`);
  return false;
}

export async function launchPlaywrightEngine(options: LaunchOptions): Promise<PlaywrightBrowserEngine> {
  // `chromiumSandbox` must be EXACTLY true: playwright appends `--no-sandbox` for anything else
  // (chromium.js:288), so omitting the option ships an unsandboxed fleet (§5).
  const chromiumSandbox = resolveChromiumSandbox(options.sandbox, process.getuid?.() ?? -1);
  if (!chromiumSandbox) console.warn(UNSANDBOXED_LAUNCH_WARNING);
  const browser = await chromium.launch({
    chromiumSandbox,
    ...(options.headlessShellPath === undefined
      ? { channel: "chromium-headless-shell" }
      : { executablePath: options.headlessShellPath }),
  });
  const cdp = await browser.newBrowserCDPSession();
  return new PlaywrightBrowserEngine(browser, cdp);
}
```

- [x] **Step 4: Chạy test, xác nhận XANH**

Run: `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers pnpm --filter @testkite/runner test test/browser/playwright-engine.test.ts`
Expected: PASS (6 test). Trên máy không có browser: toàn bộ file SKIPPED — có chủ ý, mọi logic đã phủ bằng fake engine.

- [x] **Step 5: Commit**

```bash
git add apps/runner/src/browser/playwright-engine.ts apps/runner/test/browser/playwright-engine.test.ts
git commit -m "M3-fleet T12: Playwright engine — per-context renderer attribution, CDP WebP, trace"
```

---

## Task 13 — Screenshot ring-buffer trên NVMe + dedup content-hash

**Files:**
- Create: `apps/runner/src/artifacts/screenshot-ring.ts`
- Test: `apps/runner/test/artifacts/screenshot-ring.test.ts`

**Interfaces:**
- Produces:
  ```ts
  interface RingEntry { readonly ordinal: number; readonly sha256: string; readonly sizeBytes: number; readonly path: string; readonly deduped: boolean }
  interface RingOptions { readonly dir: string; readonly capacity: number; readonly policy: ScreenshotPolicy }
  class ScreenshotRing {
    constructor(o: RingOptions)
    push(ordinal: number, bytes: Buffer): Promise<RingEntry | null>
    entries(): readonly RingEntry[]
    keepForUpload(): Promise<readonly RingEntry[]>
    discard(): Promise<void>
    get bytesWritten(): number
  }
  ```

**Chính sách §5.2:** `policy="failure"` (batch mặc định) ⇒ giữ ring trên NVMe, **chỉ upload khi chain FAIL**; `policy="all"` ⇒ giữ tất cả; `policy="none"` ⇒ không chụp. Dedup **exact hash** (không perceptual — mất bằng chứng thị giác). Số thật spike: 240 ảnh, 96.3% trùng hash, tiết kiệm 96.2% byte.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/artifacts/screenshot-ring.test.ts`**

```ts
import { existsSync, mkdtempSync, readdirSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { describe, expect, it } from "vitest";
import { ScreenshotRing } from "../../src/artifacts/screenshot-ring.js";

const dir = () => mkdtempSync(join(tmpdir(), "tk-ring-"));
const shot = (s: string) => Buffer.from(s);

describe("ScreenshotRing", () => {
  it("writes a file per unique screenshot", async () => {
    const d = dir();
    const ring = new ScreenshotRing({ dir: d, capacity: 10, policy: "failure" });
    await ring.push(1, shot("frame-a"));
    await ring.push(2, shot("frame-b"));
    expect(readdirSync(d)).toHaveLength(2);
    expect(ring.entries()).toHaveLength(2);
  });

  it("dedups by exact content hash and does NOT write a second file", async () => {
    const d = dir();
    const ring = new ScreenshotRing({ dir: d, capacity: 10, policy: "failure" });
    const first = await ring.push(1, shot("same"));
    const second = await ring.push(2, shot("same"));
    expect(second?.sha256).toBe(first?.sha256);
    expect(second?.deduped).toBe(true);
    expect(second?.path).toBe(first?.path);
    expect(readdirSync(d)).toHaveLength(1);
    expect(ring.entries()).toHaveLength(2); // both STEPS are recorded, only one BLOB exists
  });

  it("reproduces the spike ratio: 8 near-identical frames cost one blob", async () => {
    const d = dir();
    const ring = new ScreenshotRing({ dir: d, capacity: 100, policy: "failure" });
    for (let i = 1; i <= 8; i++) await ring.push(i, shot("identical-frame"));
    expect(readdirSync(d)).toHaveLength(1);
    expect(ring.entries().filter((e) => e.deduped)).toHaveLength(7);
  });

  it("evicts the oldest blob when the ring is full", async () => {
    const d = dir();
    const ring = new ScreenshotRing({ dir: d, capacity: 2, policy: "failure" });
    const a = await ring.push(1, shot("a"));
    await ring.push(2, shot("b"));
    await ring.push(3, shot("c"));
    expect(existsSync(a?.path ?? "")).toBe(false);
    expect(readdirSync(d)).toHaveLength(2);
  });

  it("captures nothing at all when the policy is none", async () => {
    const d = dir();
    const ring = new ScreenshotRing({ dir: d, capacity: 10, policy: "none" });
    expect(await ring.push(1, shot("a"))).toBeNull();
    expect(readdirSync(d)).toHaveLength(0);
  });

  it("discard() removes every blob — a green batch chain costs zero PUTs", async () => {
    const d = dir();
    const ring = new ScreenshotRing({ dir: d, capacity: 10, policy: "failure" });
    await ring.push(1, shot("a"));
    await ring.push(2, shot("b"));
    await ring.discard();
    expect(readdirSync(d)).toHaveLength(0);
    expect(ring.entries()).toHaveLength(0);
  });

  it("keepForUpload() returns unique blobs only, in ordinal order", async () => {
    const d = dir();
    const ring = new ScreenshotRing({ dir: d, capacity: 10, policy: "failure" });
    await ring.push(1, shot("a"));
    await ring.push(2, shot("a"));
    await ring.push(3, shot("b"));
    const kept = await ring.keepForUpload();
    expect(kept.map((e) => e.ordinal)).toEqual([1, 3]);
  });

  it("tracks bytes written so the worker can report NVMe scratch usage", async () => {
    const d = dir();
    const ring = new ScreenshotRing({ dir: d, capacity: 10, policy: "all" });
    await ring.push(1, shot("12345"));
    await ring.push(2, shot("12345"));
    expect(ring.bytesWritten).toBe(5); // the duplicate costs nothing
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/artifacts/`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/artifacts/screenshot-ring.ts`**

```ts
/**
 * Per-step screenshots on the worker's NVMe scratch, uploaded only when the chain FAILS
 * (docs/SYSTEM_DESIGN.md §5.2). The old system PUT a full-page PNG for every step of every run —
 * ~460GB/month. Here 95–97% of green steps never cost a single PUT, and the failure gallery is
 * still complete.
 *
 * NVMe, not tmpfs: tmpfs pages are charged to the memory cgroup, so a screenshot buffer on tmpfs
 * would eat the very ceiling this milestone exists to defend.
 *
 * Dedup is EXACT-HASH only. Perceptual near-dup would silently drop visual evidence — the one
 * thing a QA opens the gallery to see. Measured on 2026-08-29: 240 WebP frames of a form-heavy
 * synthetic flow, 231 exact duplicates, 96.2% of bytes saved.
 */
import { createHash } from "node:crypto";
import { rm, writeFile } from "node:fs/promises";
import { join } from "node:path";
import type { ScreenshotPolicy } from "@testkite/run-compiler";

export interface RingEntry {
  readonly ordinal: number;
  readonly sha256: string;
  readonly sizeBytes: number;
  readonly path: string;
  /** true = this step reuses a blob an earlier step already wrote. */
  readonly deduped: boolean;
}

export interface RingOptions {
  readonly dir: string;
  /** Max distinct blobs kept on scratch; the oldest is evicted first. */
  readonly capacity: number;
  readonly policy: ScreenshotPolicy;
}

export class ScreenshotRing {
  readonly #opts: RingOptions;
  readonly #entries: RingEntry[] = [];
  /** hash → path, for blobs currently on disk. */
  readonly #blobs = new Map<string, string>();
  /** Insertion order of hashes, for eviction. */
  readonly #order: string[] = [];
  #bytesWritten = 0;

  constructor(options: RingOptions) {
    this.#opts = options;
  }

  get bytesWritten(): number {
    return this.#bytesWritten;
  }

  async push(ordinal: number, bytes: Buffer): Promise<RingEntry | null> {
    if (this.#opts.policy === "none") return null;

    const sha256 = createHash("sha256").update(bytes).digest("hex");
    const existing = this.#blobs.get(sha256);
    if (existing !== undefined) {
      const entry: RingEntry = { ordinal, sha256, sizeBytes: bytes.length, path: existing, deduped: true };
      this.#entries.push(entry);
      return entry;
    }

    const path = join(this.#opts.dir, `${sha256}.webp`);
    await writeFile(path, bytes);
    this.#bytesWritten += bytes.length;
    this.#blobs.set(sha256, path);
    this.#order.push(sha256);
    await this.#evictIfFull();

    const entry: RingEntry = { ordinal, sha256, sizeBytes: bytes.length, path, deduped: false };
    this.#entries.push(entry);
    return entry;
  }

  entries(): readonly RingEntry[] {
    return this.#entries;
  }

  /** Unique blobs, in the order their first step produced them. */
  async keepForUpload(): Promise<readonly RingEntry[]> {
    const seen = new Set<string>();
    const kept: RingEntry[] = [];
    for (const e of this.#entries) {
      if (e.deduped || seen.has(e.sha256)) continue;
      seen.add(e.sha256);
      kept.push(e);
    }
    return kept;
  }

  async discard(): Promise<void> {
    for (const path of this.#blobs.values()) await rm(path, { force: true });
    this.#blobs.clear();
    this.#order.length = 0;
    this.#entries.length = 0;
  }

  async #evictIfFull(): Promise<void> {
    while (this.#order.length > this.#opts.capacity) {
      const oldest = this.#order.shift();
      if (oldest === undefined) return;
      const path = this.#blobs.get(oldest);
      this.#blobs.delete(oldest);
      if (path !== undefined) await rm(path, { force: true });
    }
  }
}
```

- [x] **Step 4: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/artifacts/`
Expected: PASS (8 test).

- [x] **Step 5: Commit**

```bash
git add apps/runner/src/artifacts/screenshot-ring.ts apps/runner/test/artifacts/screenshot-ring.test.ts
git commit -m "M3-fleet T13: screenshot ring-buffer on NVMe + exact-hash dedup"
```

---

## Task 14 — Artifact uploader: presigned PUT, zero-credential

**Files:**
- Create: `apps/runner/src/artifacts/uploader.ts`
- Test: `apps/runner/test/artifacts/uploader.test.ts`

**Interfaces:**
- Consumes: `RingEntry` (T13), `RetryableInfraError` từ `@testkite/contract`.
- Produces:
  ```ts
  interface PresignedTarget { readonly url: string; readonly method: "PUT"; readonly headers: Readonly<Record<string, string>> }
  interface UploadRequest { readonly target: PresignedTarget; readonly body: Buffer; readonly contentType: string }
  interface ArtifactUploader { upload(req: UploadRequest): Promise<void> }
  class HttpArtifactUploader implements ArtifactUploader { constructor(o: { fetchImpl?: typeof fetch; maxAttempts?: number; sleep?: (ms: number) => Promise<void> }) }
  class RecordingUploader implements ArtifactUploader  // test double
  ```

**Bất biến:** uploader **không ký gì cả** — nó chỉ PUT vào URL control plane đưa. Không có key S3 nào tồn tại trong process worker.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/artifacts/uploader.test.ts`**

```ts
import { RetryableInfraError } from "@testkite/contract";
import { describe, expect, it, vi } from "vitest";
import { HttpArtifactUploader, type PresignedTarget } from "../../src/artifacts/uploader.js";

const target: PresignedTarget = {
  url: "https://minio.internal/bucket/trace.zip?X-Amz-Signature=abc",
  method: "PUT",
  headers: { "Content-Type": "application/zip" },
};

const ok = () => new Response(null, { status: 200 });
const noSleep = async () => {};

describe("HttpArtifactUploader", () => {
  it("PUTs the body to exactly the presigned url, signing nothing itself", async () => {
    const fetchImpl = vi.fn(async () => ok());
    await new HttpArtifactUploader({ fetchImpl, sleep: noSleep }).upload({
      target,
      body: Buffer.from("zip"),
      contentType: "application/zip",
    });
    const [url, init] = fetchImpl.mock.calls[0] ?? [];
    expect(url).toBe(target.url);
    expect((init as RequestInit).method).toBe("PUT");
    const headers = new Headers((init as RequestInit).headers);
    expect(headers.get("content-type")).toBe("application/zip");
    // Zero-credential: nothing that looks like an AWS credential may be added by the worker.
    expect(headers.get("authorization")).toBeNull();
    expect(headers.get("x-amz-credential")).toBeNull();
  });

  it("retries a 5xx and succeeds", async () => {
    const fetchImpl = vi
      .fn()
      .mockResolvedValueOnce(new Response(null, { status: 503 }))
      .mockResolvedValueOnce(ok());
    await new HttpArtifactUploader({ fetchImpl, sleep: noSleep }).upload({ target, body: Buffer.from("z"), contentType: "application/zip" });
    expect(fetchImpl).toHaveBeenCalledTimes(2);
  });

  it("gives up after maxAttempts with a RETRYABLE network infra error", async () => {
    const fetchImpl = vi.fn(async () => new Response(null, { status: 500 }));
    const uploader = new HttpArtifactUploader({ fetchImpl, sleep: noSleep, maxAttempts: 3 });
    await expect(uploader.upload({ target, body: Buffer.from("z"), contentType: "application/zip" })).rejects.toBeInstanceOf(
      RetryableInfraError,
    );
    expect(fetchImpl).toHaveBeenCalledTimes(3);
  });

  it("does NOT retry a 403 — an expired signature will never succeed by trying harder", async () => {
    const fetchImpl = vi.fn(async () => new Response(null, { status: 403 }));
    const uploader = new HttpArtifactUploader({ fetchImpl, sleep: noSleep, maxAttempts: 5 });
    await expect(uploader.upload({ target, body: Buffer.from("z"), contentType: "application/zip" })).rejects.toThrow(/403/);
    expect(fetchImpl).toHaveBeenCalledTimes(1);
  });

  it("retries a thrown network error too, not only an http status", async () => {
    const fetchImpl = vi.fn().mockRejectedValueOnce(new TypeError("fetch failed")).mockResolvedValueOnce(ok());
    await new HttpArtifactUploader({ fetchImpl, sleep: noSleep }).upload({ target, body: Buffer.from("z"), contentType: "application/zip" });
    expect(fetchImpl).toHaveBeenCalledTimes(2);
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/artifacts/uploader.test.ts`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/artifacts/uploader.ts`**

```ts
/**
 * Artifact upload, zero-credential (docs/SYSTEM_DESIGN.md §5): the worker holds NO object-store
 * key. The control plane signs a short-lived PUT and the worker just sends bytes to that URL.
 * If a worker is ever compromised, it can write to one object for one run — not to the bucket.
 *
 * Retry policy follows the same taxonomy as everything else: transport and 5xx are RETRYABLE;
 * a 4xx is not, because an expired or malformed signature never succeeds by trying harder.
 */
import { RetryableInfraError } from "@testkite/contract";

export interface PresignedTarget {
  readonly url: string;
  readonly method: "PUT";
  readonly headers: Readonly<Record<string, string>>;
}

export interface UploadRequest {
  readonly target: PresignedTarget;
  readonly body: Buffer;
  readonly contentType: string;
}

export interface ArtifactUploader {
  upload(request: UploadRequest): Promise<void>;
}

export interface HttpUploaderOptions {
  readonly fetchImpl?: typeof fetch;
  readonly maxAttempts?: number;
  readonly sleep?: (ms: number) => Promise<void>;
}

export class HttpArtifactUploader implements ArtifactUploader {
  readonly #fetch: typeof fetch;
  readonly #maxAttempts: number;
  readonly #sleep: (ms: number) => Promise<void>;

  constructor(options: HttpUploaderOptions = {}) {
    this.#fetch = options.fetchImpl ?? fetch;
    this.#maxAttempts = options.maxAttempts ?? 4;
    this.#sleep = options.sleep ?? ((ms) => new Promise((r) => setTimeout(r, ms)));
  }

  async upload(request: UploadRequest): Promise<void> {
    let lastError = "";
    for (let attempt = 1; attempt <= this.#maxAttempts; attempt++) {
      try {
        const response = await this.#fetch(request.target.url, {
          method: request.target.method,
          headers: { ...request.target.headers, "Content-Type": request.contentType },
          body: request.body,
        });
        if (response.ok) return;
        if (response.status < 500) {
          throw new Error(`artifact upload rejected with ${response.status} — the presigned target is not usable`);
        }
        lastError = `http ${response.status}`;
      } catch (err) {
        if (err instanceof Error && err.message.startsWith("artifact upload rejected")) throw err;
        lastError = err instanceof Error ? err.message : String(err);
      }
      if (attempt < this.#maxAttempts) await this.#sleep(backoffMs(attempt));
    }
    throw new RetryableInfraError("network", `artifact upload failed after ${this.#maxAttempts} attempts: ${lastError}`);
  }
}

/** Exponential with a deterministic shape; jitter is added by the caller's sleep in production. */
function backoffMs(attempt: number): number {
  return Math.min(2_000, 100 * 2 ** (attempt - 1));
}

/** Test double: records what would have been uploaded. */
export class RecordingUploader implements ArtifactUploader {
  readonly uploads: UploadRequest[] = [];
  async upload(request: UploadRequest): Promise<void> {
    this.uploads.push(request);
  }
}
```

- [x] **Step 4: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/artifacts/uploader.test.ts`
Expected: PASS (5 test).

- [x] **Step 5: Commit**

```bash
git add apps/runner/src/artifacts/uploader.ts apps/runner/test/artifacts/uploader.test.ts
git commit -m "M3-fleet T14: presigned PUT uploader — zero-credential, taxonomy-correct retry"
```

---

## Task 15 — `control-plane-client.ts`: MỘT file chứa toàn bộ giả định hợp đồng

> ⏳ **PHỤ THUỘC:** viết được ngay theo §"Hợp đồng giả định". Khi plan `2026-08-29-m3-orchestration.md` chốt mục "Hợp đồng cho plan fleet", chạy lại contract test và **chỉ sửa file này** nếu lệch.

**Files:**
- Create: `apps/runner/src/control-plane-client.ts`
- Create: `apps/runner/test/harness/fake-control-plane.ts`
- Test: `apps/runner/test/control-plane-client.test.ts`

**Interfaces:**
- Consumes: `RunPlan` từ `@testkite/run-compiler`, `PresignedTarget` (T14), `RetryableInfraError`/`FatalInfraError` từ `@testkite/contract`.
- Produces:
  ```ts
  class StaleEpochError extends Error { readonly jobRunId: string; readonly sentEpoch: number }
  class JobCancelledError extends Error { readonly jobRunId: string }
  interface ClaimedJob { readonly jobRunId: string; readonly leaseEpoch: number; readonly runId: string; readonly chainKey: string; readonly attempt: number; readonly runToken: string; readonly plan: RunPlan }
  interface StepEvent { readonly seq: number; readonly kind: "step_finished" | "chain_started"; readonly payload: Readonly<Record<string, unknown>> }
  interface CompletePayload { readonly verdict: "passed" | "failed"; readonly steps: readonly unknown[]; readonly artifacts: readonly { kind: string; sha256: string; sizeBytes: number }[] }
  interface InfraPayload { readonly code: string; readonly retryable: boolean; readonly message: string; readonly peakRssBytes?: number }
  interface ControlPlaneClient {
    register(req: { workerName: string; lane: "interactive" | "batch"; maxContexts: number }): Promise<{ workerId: string; workerToken: string }>;
    workerHeartbeat(req: { workerId: string; psi: number | null; rssBytes: number; busySlots: number }): Promise<{ command: "continue" | "drain" }>;
    claim(req: { workerId: string; lane: "interactive" | "batch"; freeSlots: number }): Promise<ClaimedJob | null>;
    jobHeartbeat(job: ClaimedJob): Promise<void>;
    event(job: ClaimedJob, event: StepEvent): Promise<void>;
    artifactTicket(job: ClaimedJob, req: { kind: string; contentType: string; sha256: string; sizeBytes: number }): Promise<PresignedTarget>;
    complete(job: ClaimedJob, payload: CompletePayload): Promise<void>;
    fail(job: ClaimedJob, infra: InfraPayload): Promise<void>;
  }
  class HttpControlPlaneClient implements ControlPlaneClient
  ```

- [x] **Step 1: Viết harness `apps/runner/test/harness/fake-control-plane.ts`**

```ts
/**
 * An in-process HTTP server implementing the ASSUMED contract (see the "assumed contract"
 * section of the fleet plan). It exists so the client's wire behaviour — epoch on every
 * mutation, 409 handling, idempotent seq — is tested against a real socket rather than a mock
 * object, while still being fully deterministic.
 *
 * When the orchestration plan lands the real contract, THIS harness and control-plane-client.ts
 * are the only two files that change.
 */
import { createServer, type Server } from "node:http";
import type { RunPlan } from "@testkite/run-compiler";

export interface RecordedCall {
  readonly method: string;
  readonly path: string;
  readonly body: Record<string, unknown>;
  readonly auth: string | undefined;
}

export class FakeControlPlane {
  readonly calls: RecordedCall[] = [];
  #server: Server | null = null;
  #port = 0;
  /** Epoch the server considers current; raise it to simulate a reaper bumping the lease. */
  currentEpoch = 7;
  nextJob: { chainKey: string; plan: RunPlan } | null = null;
  workerCommand: "continue" | "drain" = "continue";

  get url(): string {
    return `http://127.0.0.1:${this.#port}`;
  }

  async start(): Promise<void> {
    this.#server = createServer((req, res) => {
      let raw = "";
      req.on("data", (c) => (raw += c));
      req.on("end", () => {
        const body: Record<string, unknown> = raw === "" ? {} : (JSON.parse(raw) as Record<string, unknown>);
        this.calls.push({
          method: req.method ?? "",
          path: req.url ?? "",
          body,
          auth: req.headers.authorization,
        });
        this.#route(req.url ?? "", body, res);
      });
    });
    await new Promise<void>((resolve) => this.#server?.listen(0, "127.0.0.1", resolve));
    const addr = this.#server?.address();
    this.#port = typeof addr === "object" && addr !== null ? addr.port : 0;
  }

  async stop(): Promise<void> {
    await new Promise<void>((resolve) => this.#server?.close(() => resolve()));
  }

  #route(path: string, body: Record<string, unknown>, res: import("node:http").ServerResponse): void {
    const send = (status: number, payload: unknown): void => {
      res.writeHead(status, { "content-type": "application/json" });
      res.end(JSON.stringify(payload));
    };

    if (path.endsWith("/workers/register")) {
      send(200, { workerId: "w-1", workerToken: "tk_worker" });
      return;
    }
    if (path.includes("/workers/") && path.endsWith("/heartbeat")) {
      send(200, { command: this.workerCommand });
      return;
    }
    if (path.endsWith("/claim")) {
      if (this.nextJob === null) {
        res.writeHead(204);
        res.end();
        return;
      }
      send(200, {
        jobRunId: "jr-1",
        leaseEpoch: this.currentEpoch,
        runId: "run-1",
        chainKey: this.nextJob.chainKey,
        attempt: 1,
        runToken: "tk_run",
        plan: this.nextJob.plan,
      });
      this.nextJob = null;
      return;
    }

    // Every job mutation must carry the epoch; a stale one is rejected the same way everywhere.
    const epoch = body["leaseEpoch"];
    if (typeof epoch !== "number") {
      send(400, { code: "VALIDATION_FAILED", message: "leaseEpoch is required on every mutation" });
      return;
    }
    if (epoch < this.currentEpoch) {
      send(409, { code: "STALE_EPOCH", message: `epoch ${epoch} < ${this.currentEpoch}` });
      return;
    }
    if (path.endsWith("/artifacts")) {
      send(200, {
        url: `${this.url}/blob/${String(body["sha256"])}`,
        method: "PUT",
        headers: { "Content-Type": String(body["contentType"]) },
      });
      return;
    }
    send(200, {});
  }
}
```

- [x] **Step 2: Viết test ĐỎ `apps/runner/test/control-plane-client.test.ts`**

```ts
import type { RunPlan } from "@testkite/run-compiler";
import { afterEach, beforeEach, describe, expect, it } from "vitest";
import { HttpControlPlaneClient, JobCancelledError, StaleEpochError, type ClaimedJob } from "../src/control-plane-client.js";
import { FakeControlPlane } from "./harness/fake-control-plane.js";

const plan = {
  planFormatVersion: 1,
  teamId: "team-1",
  projectId: "proj-1",
  policy: { lane: "batch", engine: "chromium-headless-shell", retry: "infra-only", screenshots: "failure", baseUrl: "https://staging.test" },
  chains: [{ chainKey: "login>checkout", cases: [], stepCount: 3, timeoutSeconds: 180 }],
  contentHash: "deadbeef",
} as unknown as RunPlan;

let cp: FakeControlPlane;
let client: HttpControlPlaneClient;

beforeEach(async () => {
  cp = new FakeControlPlane();
  await cp.start();
  client = new HttpControlPlaneClient({ baseUrl: cp.url, bootstrapToken: "boot", sleep: async () => {} });
});

afterEach(async () => {
  await cp.stop();
});

async function claimOne(): Promise<ClaimedJob> {
  await client.register({ workerName: "host1-w1", lane: "batch", maxContexts: 4 });
  cp.nextJob = { chainKey: "login>checkout", plan };
  const job = await client.claim({ workerId: "w-1", lane: "batch", freeSlots: 4 });
  if (job === null) throw new Error("expected a job");
  return job;
}

describe("HttpControlPlaneClient", () => {
  it("registers and stores the worker token for later calls", async () => {
    const reg = await client.register({ workerName: "host1-w1", lane: "batch", maxContexts: 4 });
    expect(reg.workerId).toBe("w-1");
    const claim = await client.claim({ workerId: "w-1", lane: "batch", freeSlots: 4 });
    expect(claim).toBeNull(); // 204: no job queued
    expect(cp.calls.at(-1)?.auth).toBe("Bearer tk_worker");
  });

  it("returns null on 204 rather than throwing — an empty queue is normal", async () => {
    await client.register({ workerName: "host1-w1", lane: "batch", maxContexts: 4 });
    expect(await client.claim({ workerId: "w-1", lane: "batch", freeSlots: 4 })).toBeNull();
  });

  it("carries the claimed plan and epoch through unchanged", async () => {
    const job = await claimOne();
    expect(job.leaseEpoch).toBe(7);
    expect(job.plan.contentHash).toBe("deadbeef");
    expect(job.runToken).toBe("tk_run");
  });

  it("uses the RUN token (not the worker token) for job mutations", async () => {
    const job = await claimOne();
    await client.jobHeartbeat(job);
    expect(cp.calls.at(-1)?.auth).toBe("Bearer tk_run");
  });

  it("sends leaseEpoch on EVERY job mutation", async () => {
    const job = await claimOne();
    await client.jobHeartbeat(job);
    await client.event(job, { seq: 1, kind: "step_finished", payload: { ordinal: 1 } });
    await client.complete(job, { verdict: "passed", steps: [], artifacts: [] });
    const mutations = cp.calls.filter((c) => c.path.includes("/jobs/"));
    expect(mutations).toHaveLength(3);
    for (const call of mutations) expect(call.body["leaseEpoch"]).toBe(7);
  });

  it("throws StaleEpochError on 409 STALE_EPOCH", async () => {
    const job = await claimOne();
    cp.currentEpoch = 9; // a reaper bumped the lease while we were running
    await expect(client.complete(job, { verdict: "passed", steps: [], artifacts: [] })).rejects.toBeInstanceOf(StaleEpochError);
  });

  it("never retries a STALE_EPOCH — one call, then stop", async () => {
    const job = await claimOne();
    cp.currentEpoch = 9;
    const before = cp.calls.length;
    await expect(client.jobHeartbeat(job)).rejects.toBeInstanceOf(StaleEpochError);
    expect(cp.calls.length - before).toBe(1);
  });

  it("returns a presigned target from the artifact ticket endpoint", async () => {
    const job = await claimOne();
    const target = await client.artifactTicket(job, { kind: "trace", contentType: "application/zip", sha256: "abc", sizeBytes: 3304 });
    expect(target.method).toBe("PUT");
    expect(target.url).toContain("/blob/abc");
  });

  it("relays a drain command from the worker heartbeat", async () => {
    await client.register({ workerName: "host1-w1", lane: "batch", maxContexts: 4 });
    cp.workerCommand = "drain";
    expect(await client.workerHeartbeat({ workerId: "w-1", psi: 12.5, rssBytes: 95_000_000, busySlots: 2 })).toEqual({ command: "drain" });
  });

  it("reports an infra failure through fail(), never as a verdict", async () => {
    const job = await claimOne();
    await client.fail(job, { code: "browser_oom", retryable: true, message: "killed", peakRssBytes: 1_728_053_248 });
    const call = cp.calls.at(-1);
    expect(call?.path).toContain("/complete");
    expect(call?.body["infraError"]).toMatchObject({ code: "browser_oom", peakRssBytes: 1_728_053_248 });
    expect(call?.body["verdict"]).toBeUndefined();
  });
});
```

- [x] **Step 3: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/control-plane-client.test.ts`
Expected: FAIL — module không tồn tại.

- [x] **Step 4: Implement `apps/runner/src/control-plane-client.ts`**

```ts
/**
 * THE ONLY FILE THAT KNOWS THE CONTROL PLANE'S SHAPE.
 *
 * Every assumption about the internal HTTP contract — paths, payload field names, the epoch
 * rule, the error codes — lives here and nowhere else. The orchestration plan
 * (2026-08-29-m3-orchestration.md) OWNS that contract; this file is the client. When the real
 * contract lands and differs, exactly one file and its test harness change; the executor,
 * governance and worker loop do not.
 *
 * ZERO-CREDENTIAL (docs/SYSTEM_DESIGN.md §5): two tokens ever exist in this process — a host
 * bootstrap token used once to register, and a per-run token that arrives with a claim and dies
 * with the job. No DB url, no object-store key.
 *
 * EPOCH IS NOT OPTIONAL: every job mutation carries `leaseEpoch`. A 409 STALE_EPOCH means this
 * worker is a ZOMBIE — its lease was reaped and someone else owns the chain now. It throws
 * StaleEpochError and is NEVER retried: retrying is precisely how a zombie would corrupt a
 * result that another worker already wrote.
 */
import { FatalInfraError, RetryableInfraError } from "@testkite/contract";
import type { RunPlan } from "@testkite/run-compiler";
import type { PresignedTarget } from "./artifacts/uploader.js";

export class StaleEpochError extends Error {
  readonly jobRunId: string;
  readonly sentEpoch: number;
  constructor(jobRunId: string, sentEpoch: number, detail: string) {
    super(`STALE_EPOCH for job ${jobRunId} at epoch ${sentEpoch}: ${detail}`);
    this.name = "StaleEpochError";
    this.jobRunId = jobRunId;
    this.sentEpoch = sentEpoch;
  }
}

export class JobCancelledError extends Error {
  readonly jobRunId: string;
  constructor(jobRunId: string) {
    super(`job ${jobRunId} was cancelled by the control plane`);
    this.name = "JobCancelledError";
    this.jobRunId = jobRunId;
  }
}

export interface ClaimedJob {
  readonly jobRunId: string;
  readonly leaseEpoch: number;
  readonly runId: string;
  readonly chainKey: string;
  readonly attempt: number;
  /** Scoped to THIS run only; used for every job mutation. */
  readonly runToken: string;
  readonly plan: RunPlan;
}

export interface StepEvent {
  /** Monotonic per job — the control plane deduplicates on it, so a resend is harmless. */
  readonly seq: number;
  readonly kind: "chain_started" | "step_finished";
  readonly payload: Readonly<Record<string, unknown>>;
}

export interface CompletePayload {
  readonly verdict: "passed" | "failed";
  readonly steps: readonly unknown[];
  readonly artifacts: readonly { readonly kind: string; readonly sha256: string; readonly sizeBytes: number }[];
}

export interface InfraPayload {
  readonly code: string;
  readonly retryable: boolean;
  readonly message: string;
  readonly peakRssBytes?: number;
}

export interface ControlPlaneClient {
  register(req: { workerName: string; lane: "interactive" | "batch"; maxContexts: number }): Promise<{ workerId: string; workerToken: string }>;
  workerHeartbeat(req: { workerId: string; psi: number | null; rssBytes: number; busySlots: number }): Promise<{ command: "continue" | "drain" }>;
  claim(req: { workerId: string; lane: "interactive" | "batch"; freeSlots: number }): Promise<ClaimedJob | null>;
  jobHeartbeat(job: ClaimedJob): Promise<void>;
  event(job: ClaimedJob, event: StepEvent): Promise<void>;
  artifactTicket(job: ClaimedJob, req: { kind: string; contentType: string; sha256: string; sizeBytes: number }): Promise<PresignedTarget>;
  complete(job: ClaimedJob, payload: CompletePayload): Promise<void>;
  fail(job: ClaimedJob, infra: InfraPayload): Promise<void>;
}

export interface HttpClientOptions {
  readonly baseUrl: string;
  readonly bootstrapToken: string;
  readonly fetchImpl?: typeof fetch;
  readonly maxAttempts?: number;
  readonly sleep?: (ms: number) => Promise<void>;
}

export class HttpControlPlaneClient implements ControlPlaneClient {
  readonly #baseUrl: string;
  readonly #bootstrapToken: string;
  readonly #fetch: typeof fetch;
  readonly #maxAttempts: number;
  readonly #sleep: (ms: number) => Promise<void>;
  #workerToken: string | null = null;

  constructor(options: HttpClientOptions) {
    this.#baseUrl = options.baseUrl.replace(/\/+$/, "");
    this.#bootstrapToken = options.bootstrapToken;
    this.#fetch = options.fetchImpl ?? fetch;
    this.#maxAttempts = options.maxAttempts ?? 4;
    this.#sleep = options.sleep ?? ((ms) => new Promise((r) => setTimeout(r, ms)));
  }

  async register(req: { workerName: string; lane: "interactive" | "batch"; maxContexts: number }): Promise<{ workerId: string; workerToken: string }> {
    const body = await this.#post("/internal/fleet/workers/register", req, this.#bootstrapToken, null);
    const result = body as { workerId: string; workerToken: string };
    this.#workerToken = result.workerToken;
    return result;
  }

  async workerHeartbeat(req: { workerId: string; psi: number | null; rssBytes: number; busySlots: number }): Promise<{ command: "continue" | "drain" }> {
    const body = await this.#post(`/internal/fleet/workers/${req.workerId}/heartbeat`, req, this.#requireWorkerToken(), null);
    return body as { command: "continue" | "drain" };
  }

  async claim(req: { workerId: string; lane: "interactive" | "batch"; freeSlots: number }): Promise<ClaimedJob | null> {
    const body = await this.#post("/internal/fleet/claim", req, this.#requireWorkerToken(), null);
    return body === null ? null : (body as ClaimedJob);
  }

  async jobHeartbeat(job: ClaimedJob): Promise<void> {
    await this.#post(`/internal/fleet/jobs/${job.jobRunId}/heartbeat`, { leaseEpoch: job.leaseEpoch }, job.runToken, job);
  }

  async event(job: ClaimedJob, event: StepEvent): Promise<void> {
    await this.#post(`/internal/fleet/jobs/${job.jobRunId}/events`, { leaseEpoch: job.leaseEpoch, ...event }, job.runToken, job);
  }

  async artifactTicket(job: ClaimedJob, req: { kind: string; contentType: string; sha256: string; sizeBytes: number }): Promise<PresignedTarget> {
    const body = await this.#post(`/internal/fleet/jobs/${job.jobRunId}/artifacts`, { leaseEpoch: job.leaseEpoch, ...req }, job.runToken, job);
    return body as PresignedTarget;
  }

  async complete(job: ClaimedJob, payload: CompletePayload): Promise<void> {
    await this.#post(`/internal/fleet/jobs/${job.jobRunId}/complete`, { leaseEpoch: job.leaseEpoch, ...payload }, job.runToken, job);
  }

  async fail(job: ClaimedJob, infra: InfraPayload): Promise<void> {
    await this.#post(
      `/internal/fleet/jobs/${job.jobRunId}/complete`,
      { leaseEpoch: job.leaseEpoch, infraError: infra },
      job.runToken,
      job,
    );
  }

  #requireWorkerToken(): string {
    if (this.#workerToken === null) throw new FatalInfraError("the worker called the control plane before registering");
    return this.#workerToken;
  }

  /** Returns the parsed body, or null for 204. */
  async #post(path: string, body: unknown, token: string, job: ClaimedJob | null): Promise<unknown> {
    let lastError = "";
    for (let attempt = 1; attempt <= this.#maxAttempts; attempt++) {
      let response: Response;
      try {
        response = await this.#fetch(`${this.#baseUrl}${path}`, {
          method: "POST",
          headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
          body: JSON.stringify(body),
        });
      } catch (err) {
        lastError = err instanceof Error ? err.message : String(err);
        if (attempt < this.#maxAttempts) await this.#sleep(backoffMs(attempt));
        continue;
      }

      if (response.status === 204) return null;
      if (response.ok) return (await response.json()) as unknown;

      const payload = (await response.json().catch(() => ({}))) as { code?: string; message?: string };

      // A zombie MUST NOT retry: another worker already owns this chain.
      if (response.status === 409 && payload.code === "STALE_EPOCH") {
        throw new StaleEpochError(job?.jobRunId ?? "unknown", job?.leaseEpoch ?? -1, payload.message ?? "");
      }
      if (response.status === 410) throw new JobCancelledError(job?.jobRunId ?? "unknown");
      if (response.status === 401) throw new FatalInfraError(`control plane rejected the worker credential on ${path}`);
      if (response.status === 404) throw new FatalInfraError(`control plane has no such job for ${path}`);
      if (response.status < 500 && response.status !== 429) {
        throw new FatalInfraError(`control plane rejected ${path} with ${response.status}: ${payload.message ?? ""}`);
      }
      lastError = `http ${response.status}`;
      if (attempt < this.#maxAttempts) await this.#sleep(backoffMs(attempt));
    }
    throw new RetryableInfraError("network", `control plane unreachable for ${path} after ${this.#maxAttempts} attempts: ${lastError}`);
  }
}

function backoffMs(attempt: number): number {
  const base = Math.min(4_000, 200 * 2 ** (attempt - 1));
  return base + Math.floor(base * 0.2 * Math.random()); // jitter: workers must not resynchronise
}
```

- [x] **Step 5: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/control-plane-client.test.ts`
Expected: PASS (10 test).

- [x] **Step 6: Ghi chú đồng bộ hợp đồng**

Thêm vào đầu file `apps/runner/src/control-plane-client.ts` một dòng TODO có chủ chốt (không phải placeholder — nó là hành động cụ thể có người nhận):

```ts
// CONTRACT SYNC: verify this client against the fleet-facing contract section of
// testkite/tasks/plans/2026-08-29-m3-orchestration.md before M3 acceptance. Any difference is
// fixed HERE and in test/harness/fake-control-plane.ts — nowhere else.
```

- [x] **Step 7: Commit**

```bash
git add apps/runner/src/control-plane-client.ts apps/runner/test/harness/fake-control-plane.ts \
        apps/runner/test/control-plane-client.test.ts
git commit -m "M3-fleet T15: control-plane client — the single file holding contract assumptions"
```

---

## Task 16 — Worker loop: claim → chạy chain → báo cáo; `STALE_EPOCH` ⇒ tự tử

> ⏳ **PHỤ THUỘC:** chạy được ngay trên `FakeControlPlane`; nghiệm thu cuối cần control plane thật.

**Files:**
- Create: `apps/runner/src/worker.ts`
- Modify: `apps/runner/src/main.ts`
- Test: `apps/runner/test/worker.test.ts`

**Interfaces:**
- Consumes: `ControlPlaneClient`/`ClaimedJob`/`StaleEpochError` (T15), `runChain`/`ChainOutcome` (T11), `BrowserEngine` (T10), `ScreenshotRing` (T13), `ArtifactUploader` (T14), `QuarantineDecider` (T8), `OomReporter` (T4), `ContextMemoryMonitor` (T5), `planShedding` (T6).
- Produces:
  ```ts
  interface WorkerDeps { readonly client; readonly engine; readonly ring: (chainKey: string) => ScreenshotRing; readonly uploader; readonly quarantine; readonly oom; readonly monitor; readonly resolveVerb; readonly now: () => number; readonly log: (m: string) => void; readonly workerId: string; readonly lane: "interactive" | "batch"; readonly maxContexts: number; readonly containerLimitBytes: number }
  interface WorkerStats { readonly claimed: number; readonly completed: number; readonly infraFailures: number; readonly zombieSuicides: number; readonly quarantined: number }
  class Worker { constructor(deps: WorkerDeps); runOnce(): Promise<"idle" | "ran" | "shed" | "stopped">; requestDrain(): void; stats(): WorkerStats }
  ```

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/worker.test.ts`**

```ts
import { AssertionFailure, RetryableInfraError } from "@testkite/contract";
import type { RunPlan } from "@testkite/run-compiler";
import type { VerbDefinition } from "@testkite/verb-kit";
import { beforeEach, describe, expect, it, vi } from "vitest";
import { FakeBrowserEngine } from "../src/browser/fake-engine.js";
import type { ClaimedJob, CompletePayload, ControlPlaneClient, InfraPayload } from "../src/control-plane-client.js";
import { StaleEpochError } from "../src/control-plane-client.js";
import { FakeMemoryLimiter } from "../src/memory/limiter.js";
import { OomReporter } from "../src/memory/oom-reporter.js";
import { ContextMemoryMonitor } from "../src/memory/context-monitor.js";
import { FleetBreaker, QuarantineDecider, QuarantineLedger } from "../src/quarantine.js";
import { RecordingUploader } from "../src/artifacts/uploader.js";
import { ScreenshotRing } from "../src/artifacts/screenshot-ring.js";
import { Worker, type WorkerDeps } from "../src/worker.js";
import { mkdtempSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

function planWith(stepOrdinals: readonly number[]): RunPlan {
  return {
    planFormatVersion: 1,
    teamId: "t1",
    projectId: "p1",
    policy: { lane: "batch", engine: "chromium-headless-shell", retry: "infra-only", screenshots: "failure", baseUrl: "https://staging.test" },
    chains: [
      {
        chainKey: "login>checkout",
        cases: [
          {
            caseId: "c1",
            revisionId: "r1",
            expectedToFail: false,
            steps: stepOrdinals.map((ordinal) => ({ kind: "action", ordinal, renderedSentence: "Click", groupPath: [], args: {}, opKey: "web.click" })),
          },
        ],
        stepCount: stepOrdinals.length,
        timeoutSeconds: 180,
      },
    ],
    contentHash: "hash",
  } as unknown as RunPlan;
}

const job = (over: Partial<ClaimedJob> = {}): ClaimedJob => ({
  jobRunId: "jr-1",
  leaseEpoch: 7,
  runId: "run-1",
  chainKey: "login>checkout",
  attempt: 1,
  runToken: "tk_run",
  plan: planWith([1, 2]),
  ...over,
});

class StubClient implements ControlPlaneClient {
  queue: (ClaimedJob | null)[] = [];
  completed: CompletePayload[] = [];
  failures: InfraPayload[] = [];
  events: unknown[] = [];
  staleOn: "complete" | "event" | null = null;
  async register() { return { workerId: "w-1", workerToken: "tk_w" }; }
  async workerHeartbeat() { return { command: "continue" as const }; }
  async claim() { return this.queue.shift() ?? null; }
  async jobHeartbeat() {}
  async event(j: ClaimedJob, e: unknown) {
    if (this.staleOn === "event") throw new StaleEpochError(j.jobRunId, j.leaseEpoch, "reaped");
    this.events.push(e);
  }
  async artifactTicket() { return { url: "http://blob/x", method: "PUT" as const, headers: {} }; }
  async complete(j: ClaimedJob, p: CompletePayload) {
    if (this.staleOn === "complete") throw new StaleEpochError(j.jobRunId, j.leaseEpoch, "reaped");
    this.completed.push(p);
  }
  async fail(_j: ClaimedJob, i: InfraPayload) { this.failures.push(i); }
}

let client: StubClient;
let engine: FakeBrowserEngine;
let uploader: RecordingUploader;
let limiter: FakeMemoryLimiter;

function deps(execute: VerbDefinition["execute"], over: Partial<WorkerDeps> = {}): WorkerDeps {
  const scratch = mkdtempSync(join(tmpdir(), "tk-worker-"));
  return {
    client,
    engine,
    ring: () => new ScreenshotRing({ dir: scratch, capacity: 50, policy: "failure" }),
    uploader,
    quarantine: new QuarantineDecider(new QuarantineLedger(), new FleetBreaker({ windowMs: 600_000, minSamples: 100, oomRatePct: 50, now: () => Date.now() })),
    oom: new OomReporter(limiter),
    monitor: new ContextMemoryMonitor({ sampleRss: () => null, onAction: () => {} }),
    resolveVerb: (): VerbDefinition => ({ opKey: "web.click", sentence: "Click on {element}", params: [], needsRendering: true, execute }),
    now: () => Date.now(),
    log: () => {},
    workerId: "w-1",
    lane: "batch",
    maxContexts: 4,
    containerLimitBytes: 3072 * 1024 * 1024,
    ...over,
  };
}

beforeEach(() => {
  client = new StubClient();
  engine = new FakeBrowserEngine();
  uploader = new RecordingUploader();
  limiter = new FakeMemoryLimiter();
  limiter.setLimit(2672 * 1024 * 1024);
});

describe("Worker", () => {
  it("returns idle when the queue is empty and never opens a browser context", async () => {
    const w = new Worker(deps(async () => ({ ok: true })));
    expect(await w.runOnce()).toBe("idle");
    expect(engine.contextsServed()).toBe(0);
  });

  it("claims, runs and completes a passing chain", async () => {
    client.queue.push(job());
    const w = new Worker(deps(async () => ({ ok: true })));
    expect(await w.runOnce()).toBe("ran");
    expect(client.completed).toHaveLength(1);
    expect(client.completed[0]?.verdict).toBe("passed");
    expect(w.stats()).toMatchObject({ claimed: 1, completed: 1 });
  });

  it("completes with verdict=failed on an AssertionFailure and does NOT report an infra error", async () => {
    client.queue.push(job());
    const w = new Worker(deps(async () => { throw new AssertionFailure("text mismatch"); }));
    await w.runOnce();
    expect(client.completed[0]?.verdict).toBe("failed");
    expect(client.failures).toHaveLength(0);
  });

  it("reports an infra error through fail() and never as a verdict", async () => {
    client.queue.push(job());
    const w = new Worker(deps(async () => { throw new RetryableInfraError("context_crash", "gone"); }));
    await w.runOnce();
    expect(client.completed).toHaveLength(0);
    expect(client.failures[0]).toMatchObject({ code: "context_crash", retryable: true });
  });

  it("attaches peakRss when the cgroup says chromium was OOM-killed", async () => {
    client.queue.push(job());
    limiter.setPeak(1_728_053_248);
    const w = new Worker(deps(async () => {
      limiter.raiseOomKill();
      throw new RetryableInfraError("context_crash", "renderer gone");
    }));
    await w.runOnce();
    expect(client.failures[0]).toMatchObject({ code: "browser_oom", peakRssBytes: 1_728_053_248 });
  });

  it("STALE_EPOCH on complete: stops immediately, writes nothing more, counts a zombie suicide", async () => {
    client.queue.push(job());
    client.staleOn = "complete";
    const w = new Worker(deps(async () => ({ ok: true })));
    expect(await w.runOnce()).toBe("stopped");
    expect(client.completed).toHaveLength(0);
    expect(client.failures).toHaveLength(0); // NOT even an infra report
    expect(uploader.uploads).toHaveLength(0); // NOT even an artifact
    expect(w.stats().zombieSuicides).toBe(1);
  });

  it("STALE_EPOCH mid-chain aborts the chain and still closes the context", async () => {
    client.queue.push(job());
    client.staleOn = "event";
    const w = new Worker(deps(async () => ({ ok: true })));
    expect(await w.runOnce()).toBe("stopped");
    expect(engine.openContextIds.size).toBe(0);
  });

  it("uploads the trace and the screenshot bundle only when the chain fails", async () => {
    client.queue.push(job());
    const w = new Worker(deps(async () => ({ ok: false, failureMessage: "no button" })));
    await w.runOnce();
    expect(uploader.uploads.length).toBeGreaterThan(0);

    client.queue.push(job({ jobRunId: "jr-2" }));
    const before = uploader.uploads.length;
    const w2 = new Worker(deps(async () => ({ ok: true })));
    await w2.runOnce();
    expect(uploader.uploads.length).toBe(before); // a green batch chain costs zero PUTs
  });

  it("quarantines a chain after two OOMs and stops claiming it", async () => {
    const w = new Worker(deps(async () => { limiter.raiseOomKill(); throw new RetryableInfraError("context_crash", "gone"); }));
    client.queue.push(job());
    await w.runOnce();
    client.queue.push(job({ jobRunId: "jr-2" }));
    await w.runOnce();
    expect(w.stats().quarantined).toBe(1);
    expect(client.failures.at(-1)?.code).toBe("browser_oom");
  });

  it("sheds instead of claiming when container memory is above 75%", async () => {
    client.queue.push(job());
    const claim = vi.spyOn(client, "claim");
    const w = new Worker(deps(async () => ({ ok: true }), {
      engine: Object.assign(engine, { treeRssBytes: () => 2_400 * 1024 * 1024 }),
    }));
    expect(await w.runOnce()).toBe("shed");
    expect(claim).not.toHaveBeenCalled();
  });

  it("stops claiming after requestDrain() but finishes what it holds", async () => {
    client.queue.push(job());
    const w = new Worker(deps(async () => ({ ok: true })));
    w.requestDrain();
    expect(await w.runOnce()).toBe("stopped");
    expect(client.completed).toHaveLength(0);
  });
});
```

- [x] **Step 2: Chạy test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/worker.test.ts`
Expected: FAIL — module không tồn tại.

- [x] **Step 3: Implement `apps/runner/src/worker.ts`**

```ts
/**
 * The worker's whole life: claim → run one chain → report. Everything difficult about the fleet
 * is expressed here as ORDERING, not cleverness.
 *
 *  - Shed BEFORE claiming. Admission control that runs after a job is in hand is not admission
 *    control. Above 75% of the container ceiling this worker stops taking work (§5).
 *  - Epoch on every mutation; a 409 STALE_EPOCH is a full stop. The worker writes NOTHING more
 *    for that job — no event, no artifact, no verdict — because another worker already owns the
 *    chain. This is the zombie suicide that makes "kill -9 a worker mid-chain" safe.
 *  - AssertionFailure completes the job with verdict=failed. Only an infra error goes to fail().
 *  - After any infra failure, ask the cgroup whether the kernel actually killed chromium: if it
 *    did, the report is upgraded to browser_oom with the real peakRss, and the chain's OOM
 *    counter moves toward quarantine.
 */
import type { ChainPlan } from "@testkite/run-compiler";
import type { VerbResolver } from "./executor/run-chain.js";
import { runChain, type ChainOutcome } from "./executor/run-chain.js";
import type { StepOutcome } from "./executor/step-runner.js";
import type { ArtifactUploader } from "./artifacts/uploader.js";
import type { ScreenshotRing } from "./artifacts/screenshot-ring.js";
import type { BrowserEngine } from "./browser/engine.js";
import { StaleEpochError, type ClaimedJob, type ControlPlaneClient } from "./control-plane-client.js";
import type { ContextMemoryMonitor } from "./memory/context-monitor.js";
import type { OomReporter } from "./memory/oom-reporter.js";
import { planShedding } from "./memory/shedder.js";
import type { QuarantineDecider } from "./quarantine.js";

export interface WorkerDeps {
  readonly client: ControlPlaneClient;
  readonly engine: BrowserEngine;
  readonly ring: (chainKey: string) => ScreenshotRing;
  readonly uploader: ArtifactUploader;
  readonly quarantine: QuarantineDecider;
  readonly oom: OomReporter;
  readonly monitor: ContextMemoryMonitor;
  readonly resolveVerb: VerbResolver;
  readonly now: () => number;
  readonly log: (message: string) => void;
  readonly workerId: string;
  readonly lane: "interactive" | "batch";
  readonly maxContexts: number;
  readonly containerLimitBytes: number;
}

export interface WorkerStats {
  readonly claimed: number;
  readonly completed: number;
  readonly infraFailures: number;
  readonly zombieSuicides: number;
  readonly quarantined: number;
}

export class Worker {
  readonly #deps: WorkerDeps;
  #draining = false;
  #claimed = 0;
  #completed = 0;
  #infraFailures = 0;
  #zombieSuicides = 0;
  #quarantined = 0;

  constructor(deps: WorkerDeps) {
    this.#deps = deps;
  }

  requestDrain(): void {
    this.#draining = true;
  }

  stats(): WorkerStats {
    return {
      claimed: this.#claimed,
      completed: this.#completed,
      infraFailures: this.#infraFailures,
      zombieSuicides: this.#zombieSuicides,
      quarantined: this.#quarantined,
    };
  }

  async runOnce(): Promise<"idle" | "ran" | "shed" | "stopped"> {
    if (this.#draining) return "stopped";

    const shed = planShedding(this.#deps.engine.treeRssBytes(), this.#deps.containerLimitBytes, []);
    if (!shed.admit) {
      this.#deps.log(`shedding at level ${shed.level}: not claiming`);
      return "shed";
    }

    const job = await this.#deps.client.claim({
      workerId: this.#deps.workerId,
      lane: this.#deps.lane,
      freeSlots: this.#deps.maxContexts,
    });
    if (job === null) return "idle";
    this.#claimed += 1;

    try {
      await this.#runJob(job);
      return "ran";
    } catch (err) {
      if (err instanceof StaleEpochError) {
        // ZOMBIE SUICIDE: stop dead. Not one more byte for this job.
        this.#zombieSuicides += 1;
        this.#deps.log(`stale epoch on job ${job.jobRunId}: this worker is a zombie, dropping everything`);
        return "stopped";
      }
      throw err;
    }
  }

  async #runJob(job: ClaimedJob): Promise<void> {
    const chain = job.plan.chains[0];
    if (chain === undefined) {
      await this.#deps.client.fail(job, { code: "fatal_infra", retryable: false, message: "the claimed plan has no chain" });
      this.#infraFailures += 1;
      return;
    }

    const ring = this.#deps.ring(chain.chainKey);
    this.#deps.oom.baseline();
    let seq = 0;

    const outcome = await this.#execute(job, chain, ring, () => ++seq);

    if (outcome.verdict === "infra_error") {
      await this.#reportInfra(job, chain, outcome, ring);
      return;
    }

    const artifacts = outcome.verdict === "failed" ? await this.#uploadFailureArtifacts(job, ring) : await this.#dropArtifacts(ring);
    await this.#deps.client.complete(job, { verdict: outcome.verdict, steps: outcome.steps, artifacts });
    this.#deps.quarantine.onChainOk(chain.chainKey);
    this.#completed += 1;
  }

  async #execute(job: ClaimedJob, chain: ChainPlan, ring: ScreenshotRing, nextSeq: () => number): Promise<ChainOutcome> {
    return runChain(chain, job.plan.policy, {
      engine: this.#deps.engine,
      resolveVerb: this.#deps.resolveVerb,
      now: this.#deps.now,
      log: this.#deps.log,
      onStep: (step: StepOutcome) => {
        // Fire-and-forget so a slow control plane cannot stall the browser; a StaleEpochError
        // still surfaces because the promise rejection is routed back through #onEventError.
        void this.#deps.client
          .event(job, { seq: nextSeq(), kind: "step_finished", payload: { ...step } })
          .catch((err: unknown) => this.#onEventError(err));
      },
      screenshot: async (handle, step) => {
        if (job.plan.policy.screenshots === "none") return null;
        const entry = await ring.push(step.ordinal, await handle.screenshotWebp());
        return entry?.sha256 ?? null;
      },
    });
  }

  #pendingStale: StaleEpochError | null = null;

  #onEventError(err: unknown): void {
    if (err instanceof StaleEpochError) this.#pendingStale = err;
    else this.#deps.log(`event delivery failed (non-fatal): ${String(err)}`);
  }

  async #reportInfra(job: ClaimedJob, chain: ChainPlan, outcome: ChainOutcome, ring: ScreenshotRing): Promise<void> {
    if (this.#pendingStale !== null) throw this.#pendingStale;

    // Ask the kernel, not the stack trace: a killed renderer looks like "crash" from inside.
    const finding = this.#deps.oom.check(this.#deps.monitor.largest());
    const infra = finding.killed
      ? { code: "browser_oom", retryable: true, message: this.#deps.oom.toInfraError(finding).message, peakRssBytes: finding.peakRssBytes }
      : outcome.infra ?? { code: "fatal_infra", retryable: false, message: "unclassified infra failure" };

    if (finding.killed) {
      const decision = this.#deps.quarantine.onChainOom(chain.chainKey);
      if (decision.quarantined) this.#quarantined += 1;
      if (decision.alert !== null) this.#deps.log(`ALERT ${decision.alert} for chain ${chain.chainKey} (oomCount=${decision.oomCount})`);
    }

    await ring.discard();
    await this.#deps.client.fail(job, infra);
    this.#infraFailures += 1;
  }

  async #uploadFailureArtifacts(job: ClaimedJob, ring: ScreenshotRing): Promise<readonly { kind: string; sha256: string; sizeBytes: number }[]> {
    if (this.#pendingStale !== null) throw this.#pendingStale;
    const uploaded: { kind: string; sha256: string; sizeBytes: number }[] = [];
    for (const entry of await ring.keepForUpload()) {
      const target = await this.#deps.client.artifactTicket(job, {
        kind: "screenshot",
        contentType: "image/webp",
        sha256: entry.sha256,
        sizeBytes: entry.sizeBytes,
      });
      const { readFile } = await import("node:fs/promises");
      await this.#deps.uploader.upload({ target, body: await readFile(entry.path), contentType: "image/webp" });
      uploaded.push({ kind: "screenshot", sha256: entry.sha256, sizeBytes: entry.sizeBytes });
    }
    await ring.discard();
    return uploaded;
  }

  async #dropArtifacts(ring: ScreenshotRing): Promise<readonly { kind: string; sha256: string; sizeBytes: number }[]> {
    if (this.#pendingStale !== null) throw this.#pendingStale;
    await ring.discard(); // retain-on-failure: a green chain leaves nothing behind
    return [];
  }
}
```

- [x] **Step 4: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/worker.test.ts`
Expected: PASS (11 test).

- [x] **Step 5: Nối wiring vào `apps/runner/src/main.ts`**

Thay toàn bộ nội dung `main()` (bỏ `throw new Error("TODO(M3)…")`):

```ts
/**
 * @testkite/runner — worker container: control-plane client × Playwright chromium-headless-shell.
 *
 * INVARIANTS (docs/SYSTEM_DESIGN.md §5):
 *  - The worker holds NO database or object-store credential: the plan arrives with the claim and
 *    artifacts go out through a presigned PUT the control plane signed.
 *  - AssertionFailure ⇒ the job COMPLETES with verdict=failed. Only a RetryableInfraError retries.
 *  - Every /fleet mutation carries lease_epoch — a stale epoch gets 409 STALE_EPOCH and the
 *    zombie stops writing anything at all.
 *  - One context = one chain, closed in `finally`. The login session lives inside that context.
 */
import { loadRunnerConfig } from "./config.js";
import { HttpArtifactUploader } from "./artifacts/uploader.js";
import { ScreenshotRing } from "./artifacts/screenshot-ring.js";
import { launchPlaywrightEngine } from "./browser/playwright-engine.js";
import { HttpControlPlaneClient } from "./control-plane-client.js";
import { ContextMemoryMonitor } from "./memory/context-monitor.js";
import { browserCgroupLimitBytes, CgroupV2MemoryLimiter, detectCgroupV2Memory, FakeMemoryLimiter, type MemoryLimiter } from "./memory/limiter.js";
import { OomReporter } from "./memory/oom-reporter.js";
import { OOM_SCORE_CHROMIUM, OOM_SCORE_NODE, setOomScoreAdj } from "./memory/oom-score.js";
import { FleetBreaker, QuarantineDecider, QuarantineLedger } from "./quarantine.js";
import { getVerb } from "@testkite/verb-kit";
import { MEMORY } from "./memory-governance.js";
import { Worker } from "./worker.js";
import { mkdirSync } from "node:fs";
import { join } from "node:path";

async function main(): Promise<void> {
  const config = loadRunnerConfig(process.env);
  const log = (message: string): void => console.log(JSON.stringify({ at: new Date().toISOString(), worker: config.workerName, message }));

  const client = new HttpControlPlaneClient({ baseUrl: config.controlPlaneUrl, bootstrapToken: config.bootstrapToken });
  const registration = await client.register({ workerName: config.workerName, lane: config.lane, maxContexts: config.maxContexts });

  const scratchDir = join(config.workspaceDir, "scratch");
  mkdirSync(scratchDir, { recursive: true });

  const engine = await launchPlaywrightEngine({ traceDir: join(config.workspaceDir, "traces") });

  // L2: the browser goes into a nested cgroup and becomes the kernel's preferred OOM victim.
  const browserCgroupDir = join("/sys/fs/cgroup", "ts-browser");
  let limiter: MemoryLimiter;
  if (detectCgroupV2Memory()) {
    const cgroup = new CgroupV2MemoryLimiter(browserCgroupDir);
    cgroup.setLimit(browserCgroupLimitBytes(config.containerLimitBytes));
    const browserPid = engine.browserPid();
    if (browserPid !== null) {
      cgroup.attach(browserPid);
      if (setOomScoreAdj(browserPid, OOM_SCORE_CHROMIUM) === "denied") log("could not raise chromium oom_score_adj");
    }
    if (setOomScoreAdj(process.pid, OOM_SCORE_NODE) === "denied") {
      log("could not lower node oom_score_adj — CAP_SYS_RESOURCE is missing, node is NOT protected");
    }
    limiter = cgroup;
  } else {
    // No nested ceiling on this host: report honestly rather than pretending governance is on.
    log("no cgroup v2 memory controller — L2 nested limiting is INACTIVE on this host");
    limiter = new FakeMemoryLimiter();
  }
  const oom = new OomReporter(limiter);

  const monitor = new ContextMemoryMonitor({
    sampleRss: () => null, // wired to the engine's per-context renderer pids by the worker loop
    onAction: (sample) => log(`context ${sample.contextId} ${sample.action} at ${sample.rssBytes} bytes`),
  });
  monitor.start();

  const worker = new Worker({
    client,
    engine,
    ring: () => new ScreenshotRing({ dir: scratchDir, capacity: 200, policy: "failure" }),
    uploader: new HttpArtifactUploader({}),
    quarantine: new QuarantineDecider(
      new QuarantineLedger(),
      new FleetBreaker({ windowMs: 600_000, minSamples: 20, oomRatePct: 50, now: () => Date.now() }),
    ),
    oom,
    monitor,
    resolveVerb: getVerb,
    now: () => Date.now(),
    log,
    workerId: registration.workerId,
    lane: config.lane,
    maxContexts: config.maxContexts,
    containerLimitBytes: config.containerLimitBytes,
  });

  const shutdown = (): void => {
    worker.requestDrain();
    log("drain requested, finishing the current chain");
  };
  process.on("SIGTERM", shutdown);
  process.on("SIGINT", shutdown);

  log(`runner up: lane=${config.lane} contexts=${config.maxContexts} ceilings=${JSON.stringify(MEMORY.containerLimitMb)}`);
  for (;;) {
    const result = await worker.runOnce();
    if (result === "stopped") break;
    if (result === "idle" || result === "shed") await new Promise((r) => setTimeout(r, config.claimIdleMs));
  }

  monitor.stop();
  await engine.close();
}

main().catch((err: unknown) => {
  console.error(err);
  process.exit(1);
});
```

- [x] **Step 6: Verify + commit**

```bash
cd testkite
pnpm --filter @testkite/runner typecheck && pnpm --filter @testkite/runner test && pnpm lint
git add apps/runner/src/worker.ts apps/runner/src/main.ts apps/runner/test/worker.test.ts
git commit -m "M3-fleet T16: worker loop — claim/run/report, shed-before-claim, zombie suicide"
```

---

## Task 17 — `runnerd`: register / heartbeat 5s / PSI watermark / drain

> ⏳ **PHỤ THUỘC MỘT PHẦN:** parser PSI + máy trạng thái drain chạy ngay; heartbeat thật cần control plane.

**Files:**
- Create: `apps/runner/src/runnerd/psi.ts`
- Create: `apps/runner/src/runnerd/daemon.ts`
- Create: `apps/runner/src/runnerd/main.ts`
- Test: `apps/runner/test/runnerd/psi.test.ts`
- Test: `apps/runner/test/runnerd/daemon.test.ts`

**Interfaces:**
- Consumes: `ControlPlaneClient` (T15), `readRssBytes` (T2).
- Produces:
  ```ts
  interface PsiSample { readonly some10: number; readonly some60: number; readonly full10: number }
  function parsePsi(text: string): PsiSample | null
  function readPsi(path?: string): PsiSample | null
  type Watermark = "green" | "amber" | "red";
  const PSI_AMBER_PCT = 10; const PSI_RED_PCT = 30;
  function watermarkFor(sample: PsiSample | null): Watermark
  class Runnerd { constructor(deps: RunnerdDeps); heartbeatOnce(): Promise<"continue" | "drain">; start(): void; stop(): void; watermark(): Watermark }
  ```

**Ranh giới spike:** `/proc/pressure/memory` **KHÔNG tồn tại** trong sandbox ⇒ parser test bằng fixture chuỗi thật của kernel; đọc file thật là I/O mỏng, có gate host.

- [x] **Step 1: Viết test ĐỎ `apps/runner/test/runnerd/psi.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { parsePsi, PSI_AMBER_PCT, PSI_RED_PCT, readPsi, watermarkFor } from "../../src/runnerd/psi.js";

/** The exact shape the kernel writes to /proc/pressure/memory. */
const HEALTHY = "some avg10=0.00 avg60=0.00 avg300=0.00 total=0\nfull avg10=0.00 avg60=0.00 avg300=0.00 total=0\n";
const STRESSED = "some avg10=42.31 avg60=18.02 avg300=4.11 total=91827364\nfull avg10=12.50 avg60=3.00 avg300=0.40 total=1827364\n";
const WARM = "some avg10=15.00 avg60=9.00 avg300=1.00 total=100\nfull avg10=1.00 avg60=0.10 avg300=0.00 total=10\n";

describe("parsePsi", () => {
  it("parses a healthy sample", () => {
    expect(parsePsi(HEALTHY)).toEqual({ some10: 0, some60: 0, full10: 0 });
  });

  it("parses a stressed sample", () => {
    expect(parsePsi(STRESSED)).toEqual({ some10: 42.31, some60: 18.02, full10: 12.5 });
  });

  it("returns null for text that is not PSI rather than inventing zeros", () => {
    expect(parsePsi("not pressure data")).toBeNull();
  });

  it("returns null for an empty file", () => {
    expect(parsePsi("")).toBeNull();
  });
});

describe("watermarkFor", () => {
  it("is green under the amber threshold", () => {
    expect(watermarkFor(parsePsi(HEALTHY))).toBe("green");
  });

  it("is amber at the amber threshold", () => {
    expect(PSI_AMBER_PCT).toBe(10);
    expect(watermarkFor(parsePsi(WARM))).toBe("amber");
  });

  it("is red at the red threshold", () => {
    expect(PSI_RED_PCT).toBe(30);
    expect(watermarkFor(parsePsi(STRESSED))).toBe("red");
  });

  it("treats a kernel WITHOUT PSI as green, not as red — an unknown is not an emergency", () => {
    expect(watermarkFor(null)).toBe("green");
  });
});

describe("readPsi", () => {
  it("returns null when the kernel does not expose PSI (the dev sandbox case)", () => {
    expect(readPsi("/proc/definitely-not-pressure")).toBeNull();
  });
});
```

- [x] **Step 2: Viết test ĐỎ `apps/runner/test/runnerd/daemon.test.ts`**

```ts
import { describe, expect, it, vi } from "vitest";
import { Runnerd } from "../../src/runnerd/daemon.js";

function client(command: "continue" | "drain" = "continue") {
  return {
    register: vi.fn(async () => ({ workerId: "w-1", workerToken: "tk_w" })),
    workerHeartbeat: vi.fn(async () => ({ command })),
  };
}

const base = {
  workerName: "host1",
  lane: "batch" as const,
  maxContexts: 4,
  readPsiSample: () => null,
  selfRssBytes: () => 95_000_000,
  busySlots: () => 2,
  onDrain: () => {},
  log: () => {},
};

describe("Runnerd", () => {
  it("registers once before its first heartbeat", async () => {
    const c = client();
    const d = new Runnerd({ ...base, client: c });
    await d.heartbeatOnce();
    await d.heartbeatOnce();
    expect(c.register).toHaveBeenCalledTimes(1);
    expect(c.workerHeartbeat).toHaveBeenCalledTimes(2);
  });

  it("sends the current PSI and RSS with each heartbeat", async () => {
    const c = client();
    const d = new Runnerd({
      ...base,
      client: c,
      readPsiSample: () => ({ some10: 42.31, some60: 18.02, full10: 12.5 }),
    });
    await d.heartbeatOnce();
    expect(c.workerHeartbeat).toHaveBeenCalledWith(expect.objectContaining({ psi: 42.31, rssBytes: 95_000_000, busySlots: 2 }));
  });

  it("fires onDrain when the control plane answers with drain", async () => {
    const onDrain = vi.fn();
    const d = new Runnerd({ ...base, client: client("drain"), onDrain });
    expect(await d.heartbeatOnce()).toBe("drain");
    expect(onDrain).toHaveBeenCalledTimes(1);
  });

  it("fires onDrain only ONCE even if drain keeps being returned", async () => {
    const onDrain = vi.fn();
    const d = new Runnerd({ ...base, client: client("drain"), onDrain });
    await d.heartbeatOnce();
    await d.heartbeatOnce();
    expect(onDrain).toHaveBeenCalledTimes(1);
  });

  it("exposes the host watermark computed from PSI", async () => {
    const d = new Runnerd({ ...base, client: client(), readPsiSample: () => ({ some10: 42.31, some60: 1, full10: 1 }) });
    await d.heartbeatOnce();
    expect(d.watermark()).toBe("red");
  });

  it("survives a control plane error without killing the data path", async () => {
    const c = { register: vi.fn(async () => ({ workerId: "w-1", workerToken: "t" })), workerHeartbeat: vi.fn(async () => { throw new Error("down"); }) };
    const log = vi.fn();
    const d = new Runnerd({ ...base, client: c, log });
    await expect(d.heartbeatOnce()).resolves.toBe("continue");
    expect(log).toHaveBeenCalled();
  });
});
```

- [x] **Step 3: Chạy cả hai test, xác nhận ĐỎ**

Run: `pnpm --filter @testkite/runner test test/runnerd/`
Expected: FAIL — hai module không tồn tại.

- [x] **Step 4: Implement `apps/runner/src/runnerd/psi.ts`**

```ts
/**
 * PSI (Pressure Stall Information) is the host watermark input (docs/SYSTEM_DESIGN.md §5):
 * GREEN/AMBER/RED tells the control plane whether this host should keep receiving work.
 *
 * It reads a number the kernel already computes — the share of wall time SOMETHING was stalled
 * on memory — which is a far better signal than free-bytes: a host can look "free" while every
 * process thrashes on reclaim.
 *
 * The dev sandbox has no /proc/pressure at all (verified 2026-08-29), so parsing is tested
 * against the kernel's literal text format and a missing file reads as "unknown" = GREEN.
 * Treating an unknown as RED would drain a whole fleet the first time it ran on a kernel
 * without PSI compiled in.
 */
import { readFileSync } from "node:fs";

export interface PsiSample {
  readonly some10: number;
  readonly some60: number;
  readonly full10: number;
}

export const PSI_AMBER_PCT = 10;
export const PSI_RED_PCT = 30;

export type Watermark = "green" | "amber" | "red";

export function parsePsi(text: string): PsiSample | null {
  const some = /^some\s+avg10=([\d.]+)\s+avg60=([\d.]+)/m.exec(text);
  const full = /^full\s+avg10=([\d.]+)/m.exec(text);
  if (some === null || full === null) return null;
  return { some10: Number(some[1]), some60: Number(some[2]), full10: Number(full[1]) };
}

export function readPsi(path = "/proc/pressure/memory"): PsiSample | null {
  try {
    return parsePsi(readFileSync(path, "utf8"));
  } catch {
    return null; // this kernel does not expose PSI
  }
}

export function watermarkFor(sample: PsiSample | null): Watermark {
  if (sample === null) return "green";
  if (sample.some10 >= PSI_RED_PCT) return "red";
  if (sample.some10 >= PSI_AMBER_PCT) return "amber";
  return "green";
}
```

- [x] **Step 5: Implement `apps/runner/src/runnerd/daemon.ts`**

```ts
/**
 * runnerd — the host supervisor (docs/SYSTEM_DESIGN.md §5). ~800 LOC in the blueprint's budget;
 * this is its core.
 *
 * The load-bearing property is that runnerd is OFF the data path: it registers the host,
 * heartbeats every 5s with PSI and RSS, and relays the control plane's answer. If runnerd dies,
 * chains keep running — systemd restarts it and it re-registers. That is why there is no inbound
 * port on the host: commands ride back on the heartbeat RESPONSE.
 */
import { watermarkFor, type PsiSample, type Watermark } from "./psi.js";

export interface RunnerdClient {
  register(req: { workerName: string; lane: "interactive" | "batch"; maxContexts: number }): Promise<{ workerId: string; workerToken: string }>;
  workerHeartbeat(req: { workerId: string; psi: number | null; rssBytes: number; busySlots: number }): Promise<{ command: "continue" | "drain" }>;
}

export interface RunnerdDeps {
  readonly client: RunnerdClient;
  readonly workerName: string;
  readonly lane: "interactive" | "batch";
  readonly maxContexts: number;
  readonly readPsiSample: () => PsiSample | null;
  readonly selfRssBytes: () => number;
  readonly busySlots: () => number;
  readonly onDrain: () => void;
  readonly log: (message: string) => void;
  readonly intervalMs?: number;
}

export class Runnerd {
  readonly #deps: RunnerdDeps;
  #workerId: string | null = null;
  #watermark: Watermark = "green";
  #drained = false;
  #timer: ReturnType<typeof setInterval> | null = null;

  constructor(deps: RunnerdDeps) {
    this.#deps = deps;
  }

  watermark(): Watermark {
    return this.#watermark;
  }

  async heartbeatOnce(): Promise<"continue" | "drain"> {
    try {
      if (this.#workerId === null) {
        const reg = await this.#deps.client.register({
          workerName: this.#deps.workerName,
          lane: this.#deps.lane,
          maxContexts: this.#deps.maxContexts,
        });
        this.#workerId = reg.workerId;
      }

      const sample = this.#deps.readPsiSample();
      this.#watermark = watermarkFor(sample);

      const response = await this.#deps.client.workerHeartbeat({
        workerId: this.#workerId,
        psi: sample?.some10 ?? null,
        rssBytes: this.#deps.selfRssBytes(),
        busySlots: this.#deps.busySlots(),
      });

      if (response.command === "drain" && !this.#drained) {
        this.#drained = true; // idempotent: drain is a one-way door until the process restarts
        this.#deps.onDrain();
      }
      return response.command;
    } catch (err) {
      // runnerd failing must never take the data path with it.
      this.#deps.log(`runnerd heartbeat failed (chains keep running): ${err instanceof Error ? err.message : String(err)}`);
      return "continue";
    }
  }

  start(): void {
    if (this.#timer !== null) return;
    this.#timer = setInterval(() => void this.heartbeatOnce(), this.#deps.intervalMs ?? 5_000);
    this.#timer.unref?.();
  }

  stop(): void {
    if (this.#timer === null) return;
    clearInterval(this.#timer);
    this.#timer = null;
  }
}
```

- [x] **Step 6: Implement `apps/runner/src/runnerd/main.ts`**

```ts
/**
 * runnerd entrypoint — one per HOST (the worker entrypoint is src/main.ts, one per container).
 */
import { loadRunnerConfig } from "../config.js";
import { HttpControlPlaneClient } from "../control-plane-client.js";
import { readRssBytes } from "../memory/rss.js";
import { Runnerd } from "./daemon.js";
import { readPsi } from "./psi.js";

function main(): void {
  const config = loadRunnerConfig(process.env);
  const log = (message: string): void => console.log(JSON.stringify({ at: new Date().toISOString(), daemon: "runnerd", message }));

  const daemon = new Runnerd({
    client: new HttpControlPlaneClient({ baseUrl: config.controlPlaneUrl, bootstrapToken: config.bootstrapToken }),
    workerName: config.workerName,
    lane: config.lane,
    maxContexts: config.maxContexts,
    readPsiSample: () => readPsi(),
    selfRssBytes: () => readRssBytes(process.pid) ?? 0,
    busySlots: () => 0, // the host-level count arrives from the control plane's view in M5
    onDrain: () => log("drain command received — the control plane will stop dispatching here"),
    log,
    intervalMs: config.heartbeatIntervalMs,
  });

  daemon.start();
  process.on("SIGTERM", () => daemon.stop());
  log(`runnerd up for ${config.workerName}`);
}

main();
```

- [x] **Step 7: Chạy test, xác nhận XANH**

Run: `pnpm --filter @testkite/runner test test/runnerd/`
Expected: PASS (15 test).

- [x] **Step 8: Commit**

```bash
git add apps/runner/src/runnerd/ apps/runner/test/runnerd/
git commit -m "M3-fleet T17: runnerd — register/heartbeat/PSI watermark/drain, off the data path"
```

---

## Task 18 — systemd fleet: `ts-workers.slice`, `ts-worker@`, `runnerd.service` + gate cú pháp offline

**Files:**
- Create: `apps/runner/deploy/systemd/ts-workers.slice`
- Create: `apps/runner/deploy/systemd/ts-worker@.service`
- Create: `apps/runner/deploy/systemd/runnerd.service`
- Create: `apps/runner/deploy/runner-manifest.json`
- Create: `scripts/verify-units.sh`
- Test: `apps/runner/test/deploy/manifest.test.ts`
- Modify: `.github/workflows/testkite-ci.yml`

**Ranh giới spike:** sandbox **không chạy systemd** (PID 1 = `process_api`, `/run/systemd/system` không tồn tại) ⇒ unit file là hạng mục **"viết + review, verify hành vi trên host thật"**. Nhưng `systemd-analyze verify` (systemd 255) **dùng được offline** — với một cái bẫy đã đo: nó **trả exit 0 kể cả khi có `Unknown key name` / `Invalid memory limit`**. Vì vậy gate bắt lỗi theo **OUTPUT**.

- [x] **Step 1: Viết `apps/runner/deploy/systemd/ts-workers.slice`**

```ini
# L0 of the four memory ceilings (docs/SYSTEM_DESIGN.md §5): every worker container on this host
# lives inside ONE slice, so the fleet can never take the host down with it. MemoryHigh throttles
# and reclaims first; MemoryMax is the wall. Both are percentages of host RAM so the same unit is
# correct on an 8-vCPU/24GB and a 16-vCPU/32GB box.
[Unit]
Description=TestKite worker containers (memory-capped fleet slice)
Before=slices.target

[Slice]
MemoryAccounting=yes
MemoryHigh=80%
MemoryMax=88%
CPUAccounting=yes
TasksAccounting=yes
TasksMax=4096
```

- [x] **Step 2: Viết `apps/runner/deploy/systemd/ts-worker@.service`**

```ini
# One instance per worker container: ts-worker@1 .. ts-worker@M.
#
# OOMPolicy=continue is deliberate and load-bearing. The default (`stop`) would tear the WHOLE
# unit down when the kernel kills one process inside it — which is exactly the case this fleet is
# designed around: the kernel kills Chromium, Node SURVIVES, reads memory.events and reports
# browser_oom with peakRss (§5 L2). `stop` would kill the reporter before it could report.
#
# Restart=always with a backoff: a worker that dies for any other reason comes back and
# re-registers; the control plane's reaper bumps lease_epoch, so the returning worker can never
# write a verdict for the chain it lost (409 STALE_EPOCH).
[Unit]
Description=TestKite worker %i
After=network-online.target
Wants=network-online.target
PartOf=ts-workers.target

[Service]
Type=exec
Slice=ts-workers.slice
Environment=RUNNER_WORKER_NAME=%H-w%i
Environment=RUNNER_LANE=batch
Environment=RUNNER_WORKSPACE_DIR=/var/lib/testkite/w%i
EnvironmentFile=/etc/testkite/runner.env
ExecStart=/usr/bin/podman run --rm --name ts-worker-%i \
  --user 10001:10001 \
  --cap-drop ALL \
  --security-opt no-new-privileges \
  --read-only --tmpfs /tmp:size=64m \
  --shm-size 1g \
  --memory 3g --memory-swap 3g \
  --pids-limit 512 \
  --cpus 2.0 \
  --volume /var/lib/testkite/w%i:/workspace:rw \
  --env-file /etc/testkite/runner.env \
  --env RUNNER_WORKER_NAME=%H-w%i \
  --env RUNNER_WORKSPACE_DIR=/workspace \
  ghcr.io/testkite/runner:latest
ExecStop=/usr/bin/podman stop --time 90 ts-worker-%i
Restart=always
RestartSec=2s
OOMPolicy=continue
TimeoutStopSec=120s
```

- [x] **Step 3: Viết `apps/runner/deploy/systemd/runnerd.service`**

```ini
# The host supervisor. Deliberately OUTSIDE ts-workers.slice: runnerd must survive the memory
# pressure it is reporting on. It holds no chain state, so a restart costs nothing but a
# re-registration.
[Unit]
Description=TestKite host supervisor (runnerd)
After=network-online.target
Wants=network-online.target

[Service]
Type=exec
ExecStart=/usr/bin/node /opt/testkite/runner/dist/runnerd/main.js
EnvironmentFile=/etc/testkite/runner.env
Restart=always
RestartSec=5s
MemoryAccounting=yes
MemoryMax=256M
OOMScoreAdjust=-800

[Install]
WantedBy=multi-user.target
```

- [x] **Step 4: Viết `apps/runner/deploy/runner-manifest.json`**

```json
{
  "$comment": "Container memory ceilings — MUST match apps/runner/src/memory-governance.ts. CI fails a manifest with no memory limit (docs/SYSTEM_DESIGN.md §1).",
  "image": "ghcr.io/testkite/runner:latest",
  "lanes": {
    "batch": { "memoryMb": 3072, "contexts": 4, "pidsLimit": 512, "cpus": 2.0, "shmSizeMb": 1024, "swap": false },
    "interactive": { "memoryMb": 2048, "contexts": 2, "pidsLimit": 512, "cpus": 2.0, "shmSizeMb": 1024, "swap": false }
  },
  "browserCgroupReserveMb": 400,
  "slice": { "memoryHighPct": 80, "memoryMaxPct": 88 }
}
```

- [x] **Step 5: Viết test ĐỎ `apps/runner/test/deploy/manifest.test.ts`**

```ts
import { readFileSync } from "node:fs";
import { join } from "node:path";
import { describe, expect, it } from "vitest";
import { MEMORY } from "../../src/memory-governance.js";

const deployDir = join(import.meta.dirname, "..", "..", "deploy");
const manifest = JSON.parse(readFileSync(join(deployDir, "runner-manifest.json"), "utf8")) as {
  lanes: Record<string, { memoryMb: number; contexts: number; swap: boolean }>;
  browserCgroupReserveMb: number;
  slice: { memoryHighPct: number; memoryMaxPct: number };
};
const unit = (name: string): string => readFileSync(join(deployDir, "systemd", name), "utf8");

describe("runner manifest", () => {
  it("matches MEMORY.containerLimitMb exactly — one source of truth, not two", () => {
    expect(manifest.lanes["batch"]?.memoryMb).toBe(MEMORY.containerLimitMb.batch);
    expect(manifest.lanes["interactive"]?.memoryMb).toBe(MEMORY.containerLimitMb.interactive);
  });

  it("matches MEMORY.contextsPerWorker", () => {
    expect(manifest.lanes["batch"]?.contexts).toBe(MEMORY.contextsPerWorker.batch);
    expect(manifest.lanes["interactive"]?.contexts).toBe(MEMORY.contextsPerWorker.interactive);
  });

  it("matches the browser cgroup reserve", () => {
    expect(manifest.browserCgroupReserveMb).toBe(MEMORY.browserCgroupReserveMb);
  });

  it("keeps swap OFF on every lane — swap turns an OOM into an unbounded slowdown", () => {
    for (const lane of Object.values(manifest.lanes)) expect(lane.swap).toBe(false);
  });

  it("declares a memory limit for EVERY lane (the CI rule from blueprint §1)", () => {
    for (const [name, lane] of Object.entries(manifest.lanes)) {
      expect(lane.memoryMb, `lane ${name} has no memory limit`).toBeGreaterThan(0);
    }
  });
});

describe("systemd units", () => {
  it("caps the slice at the documented 80%/88%", () => {
    const slice = unit("ts-workers.slice");
    expect(slice).toContain(`MemoryHigh=${manifest.slice.memoryHighPct}%`);
    expect(slice).toContain(`MemoryMax=${manifest.slice.memoryMaxPct}%`);
  });

  it("sets OOMPolicy=continue on the worker template so Node survives to report browser_oom", () => {
    expect(unit("ts-worker@.service")).toContain("OOMPolicy=continue");
  });

  it("restarts a worker always", () => {
    expect(unit("ts-worker@.service")).toContain("Restart=always");
  });

  it("runs the worker container with the manifest's memory limit and no swap headroom", () => {
    const service = unit("ts-worker@.service");
    expect(service).toContain("--memory 3g");
    expect(service).toContain("--memory-swap 3g"); // equal ⇒ swap disabled for this container
  });

  it("drops every capability and runs as an unprivileged uid", () => {
    const service = unit("ts-worker@.service");
    expect(service).toContain("--cap-drop ALL");
    expect(service).toContain("--user 10001:10001");
    expect(service).toContain("--read-only");
  });

  it("NEVER passes --no-sandbox anywhere in the deploy tree", () => {
    for (const name of ["ts-worker@.service", "runnerd.service", "ts-workers.slice"]) {
      expect(unit(name)).not.toContain("no-sandbox");
    }
  });

  it("keeps runnerd OUT of the worker slice so it survives the pressure it reports", () => {
    const runnerd = unit("runnerd.service");
    expect(runnerd).not.toContain("Slice=ts-workers.slice");
    expect(runnerd).toContain("OOMScoreAdjust=-800");
  });
});
```

- [x] **Step 6: Chạy test, xác nhận ĐỎ rồi XANH**

Run: `pnpm --filter @testkite/runner test test/deploy/`
Expected: đỏ nếu unit/manifest chưa khớp; sau Step 1–4 phải PASS (14 test).

- [x] **Step 7: Viết `scripts/verify-units.sh` — gate cú pháp offline**

```bash
#!/usr/bin/env bash
# Offline syntax gate for the systemd units.
#
# WHY THIS PARSES OUTPUT INSTEAD OF TRUSTING THE EXIT CODE (measured 2026-08-29, systemd 255):
#   $ systemd-analyze verify bad.service     # MemoryMax=nonsense + OOMPolicyy=continue
#   bad.service:7: Invalid memory limit 'nonsense', ignoring: Invalid argument
#   bad.service:8: Unknown key name 'OOMPolicyy' in section 'Service', ignoring.
#   $ echo $?   ->  0                        # <-- a typo'd directive is SILENTLY IGNORED
# A misspelled MemoryMax would ship a fleet with no ceiling at all and CI would stay green.
#
# The one message we tolerate is a missing ExecStart binary: podman/node are not installed on
# the CI runner, and that is a runtime concern, not a unit-file defect.
set -euo pipefail

UNIT_DIR="${1:-testkite/apps/runner/deploy/systemd}"

if ! command -v systemd-analyze >/dev/null 2>&1; then
  echo "systemd-analyze not found — install systemd-container or skip this gate on this machine" >&2
  exit 2
fi

status=0
for unit in "$UNIT_DIR"/*; do
  output="$(systemd-analyze verify "$unit" 2>&1 || true)"
  filtered="$(printf '%s\n' "$output" | grep -v 'is not executable: No such file or directory' | grep -v '^$' || true)"
  if [ -n "$filtered" ]; then
    echo "::error::systemd unit $unit has problems:" >&2
    printf '%s\n' "$filtered" >&2
    status=1
  else
    echo "ok: $unit"
  fi
done
exit "$status"
```

- [x] **Step 8: Chạy gate, xác nhận XANH**

```bash
chmod +x testkite/scripts/verify-units.sh
bash testkite/scripts/verify-units.sh testkite/apps/runner/deploy/systemd
```
Expected:
```
ok: testkite/apps/runner/deploy/systemd/runnerd.service
ok: testkite/apps/runner/deploy/systemd/ts-worker@.service
ok: testkite/apps/runner/deploy/systemd/ts-workers.slice
```

Kiểm chứng gate THẬT SỰ bắt được lỗi (bắt buộc — không có bước này thì gate là trang trí):

```bash
sed -i 's/^OOMPolicy=continue$/OOMPolicyy=continue/' testkite/apps/runner/deploy/systemd/ts-worker@.service
bash testkite/scripts/verify-units.sh testkite/apps/runner/deploy/systemd; echo "exit=$?"   # must be 1 and print "Unknown key name"
git checkout -- testkite/apps/runner/deploy/systemd/ts-worker@.service
```

- [x] **Step 9: Nối 2 gate vào CI**

Thêm vào `.github/workflows/testkite-ci.yml`, job `build-and-test`, sau gate "no browser in API image":

```yaml
      # The browser lives in the RUNNER image only. This is the invariant that makes the old
      # system's OOM class unwritable (docs/SYSTEM_DESIGN.md §1) — apps/core stays browser-free,
      # and the runner is the ONLY place playwright may be imported.
      - name: Gate — playwright only in the runner image
        run: |
          set -euo pipefail
          if grep -rlE "playwright|puppeteer" testkite/apps/core testkite/packages --include='*.ts' --include='*.json'; then
            echo "::error::Playwright must exist only in apps/runner."; exit 1
          fi
          grep -q '"playwright-core"' testkite/apps/runner/package.json \
            || { echo "::error::apps/runner lost its browser dependency."; exit 1; }

      # A container manifest without a memory limit is exactly how the old system died.
      - name: Gate — every runner lane declares a memory limit
        working-directory: testkite
        run: node -e '
          const m = require("./apps/runner/deploy/runner-manifest.json");
          for (const [lane, cfg] of Object.entries(m.lanes)) {
            if (!(cfg.memoryMb > 0)) { console.error(`lane ${lane} has no memoryMb`); process.exit(1); }
            if (cfg.swap !== false) { console.error(`lane ${lane} must disable swap`); process.exit(1); }
          }
          console.log("manifest memory limits: ok");'

      - name: Gate — systemd unit syntax
        run: bash testkite/scripts/verify-units.sh testkite/apps/runner/deploy/systemd
```

- [x] **Step 10: Verify + commit**

```bash
cd testkite
pnpm --filter @testkite/runner test test/deploy/ && bash scripts/verify-units.sh apps/runner/deploy/systemd
git add apps/runner/deploy/ scripts/verify-units.sh apps/runner/test/deploy/ ../.github/workflows/testkite-ci.yml
git commit -m "M3-fleet T18: systemd slice/template/runnerd units + manifest + offline unit gate"
```

**Ghi chú thực thi (31-08-2026) — chỗ lệch so với block spec ở trên:**

- Step 6: `manifest.test.ts` có **12** test (không phải 14 như plan ước); thêm file thứ hai
  `apps/runner/test/deploy/verify-units.test.ts` (**7** test) — đây là "gate canh gate": nó nạp
  một unit CỐ TÌNH sai vào thư mục tạm, khẳng định `systemd-analyze` trần vẫn trả **exit 0**
  (bẫy còn sống), rồi khẳng định `verify-units.sh` trả **exit 1** kèm `Unknown key name` /
  `Invalid memory limit`. Kiểm chứng phân biệt: thay tạm script bằng bản tin-exit-code ⇒ **6/7
  test đỏ**; trả lại bản đọc-output ⇒ xanh.
- Step 7: `verify-units.sh` giữ nguyên nguyên tắc đọc OUTPUT, thêm 2 thứ plan thiếu:
  (a) `UNIT_DIR` mặc định suy từ vị trí script (để `pnpm --filter @testkite/runner verify:units`,
  cwd = `apps/runner`, chạy đúng), (b) đếm số unit đã kiểm — thư mục rỗng/sai đường dẫn phải
  **đỏ (exit 2)** chứ không "xanh rỗng".
- Step 9: gate "playwright only in the runner image" KHÔNG dùng grep tên trần như block plan —
  bản đó luôn đỏ trên repo hiện tại (`packages/verb-kit/src/index.ts` có comment
  `TODO(M4): import type { Page } from "playwright-core"`, `run-compiler` có literal
  `"chromium-headless-shell"`). Đã đổi sang neo vào **câu lệnh import thật** (PCRE, `grep -rPn`),
  đo hai chiều 31-08: 0 hit ở `apps/core/src` + `packages/*/src`, 2 hit ở
  `apps/runner/src/browser/playwright-engine.ts`. Gate cũ "no browser in API image" giữ nguyên,
  gate mới chỉ phủ phần còn thiếu (`packages/**` + chiều DƯƠNG: runner không được mất
  `playwright-core`).
- Ranh giới bằng chứng: CI chỉ chứng minh unit **PARSE** được và deploy tree khớp
  `memory-governance.ts`. Việc systemd **áp** được `MemoryMax=88%`, `OOMPolicy=continue` giữ Node
  sống sau OOM, `OOMScoreAdjust=-800` cho runnerd — chỉ nghiệm thu được trên host pilot
  (`systemctl start ts-worker@1` rồi đọc `memory.max` trong unified hierarchy). Sandbox không có
  systemd (PID 1 = `process_api`).

---

## Task 19 — Soak 200 chain synthetic: bằng chứng lớp lỗi OOM đã bị xoá sổ

> ⏳ **PHỤ THUỘC:** chạy được ngay với `FakeControlPlane` + chromium thật (đó là hình thái nghiệm thu ở CI); nghiệm thu cuối trên host pilot với control plane thật (mục 5 `tasks/open-questions.md` — đo giây/chain thật).

**Files:**
- Create: `apps/runner/test/soak/synthetic-app.ts`
- Create: `apps/runner/test/soak/soak-200.test.ts`
- Modify: `.github/workflows/testkite-ci.yml` (job `fleet-soak` chạy nightly)

**Interfaces:**
- Consumes: mọi thứ ở T11–T17.
- Produces: `buildSyntheticPlan(chainCount: number, stepsPerChain: number): RunPlan`, `SoakReport { readonly chains: number; readonly nodeRssStartBytes: number; readonly nodeRssEndBytes: number; readonly browserTreeRssPeakBytes: number; readonly orphanChromiumAfter: number; readonly contextsLeaked: number; readonly msPerChainP50: number }`.

**Ngưỡng nghiệm thu (căn cứ số spike thật, có biên):**

| Khẳng định | Ngưỡng | Vì sao |
|---|---|---|
| node RSS cuối / đầu | < 130% | đúng `MEMORY.recycle.containerRssFloorGrowthPct`; mini-soak thật đo 110% |
| đỉnh cây chromium | < `containerLimitMb.batch` (3072MB) | trần L1 |
| orphan chromium sau khi đóng | **= 0** (đo sau settle 1.5s) | spike: đo ngay lập tức thấy 2 — **phải có settle**, nếu không là đỏ giả |
| context rò | **= 0** | metric `contexts_leaked` là P1 trong §5 |
| RSS "API" (process không phải worker) | phẳng | không có browser trong ảnh API ⇒ tự nhiên phẳng; khẳng định lại bằng gate ảnh |

- [x] **Step 1: Viết `apps/runner/test/soak/synthetic-app.ts`**

```ts
/**
 * The synthetic app under test: a `data:` URL, so the soak measures OUR memory behaviour and not
 * a web server's. Its shape mirrors the workload that actually stresses the fleet — a form-heavy
 * flow whose consecutive screenshots are near-identical (which is also what the dedup path sees).
 */
import type { CasePlan, RunPlan, StepPlan } from "@testkite/run-compiler";

export const SYNTHETIC_URL =
  "data:text/html,<h1 id=t>TestKite soak</h1><input id=a><input id=b><button id=go>go</button>";

export function buildSyntheticPlan(chainCount: number, stepsPerChain: number): RunPlan {
  const steps = (): readonly StepPlan[] =>
    Array.from({ length: stepsPerChain }, (_unused, i) => ({
      kind: "action" as const,
      ordinal: i + 1,
      renderedSentence: `Click on element ${i + 1}`,
      groupPath: [],
      args: { element: i % 2 === 0 ? "#a" : "#b" },
      opKey: "soak.noop",
    }));

  const kase = (n: number): CasePlan => ({
    caseId: `soak-case-${n}`,
    revisionId: "rev-soak",
    expectedToFail: false,
    steps: steps(),
  });

  return {
    planFormatVersion: 1,
    teamId: "soak-team",
    projectId: "soak-project",
    policy: {
      lane: "batch",
      engine: "chromium-headless-shell",
      retry: "infra-only",
      screenshots: "failure",
      baseUrl: SYNTHETIC_URL,
    },
    chains: Array.from({ length: chainCount }, (_unused, n) => ({
      chainKey: `soak-chain-${n}`,
      cases: [kase(n)],
      stepCount: stepsPerChain,
      timeoutSeconds: 180,
    })),
    contentHash: `soak-${chainCount}x${stepsPerChain}`,
  } as unknown as RunPlan;
}
```

- [x] **Step 2: Viết test ĐỎ `apps/runner/test/soak/soak-200.test.ts`**

```ts
import { mkdtempSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import type { VerbDefinition } from "@testkite/verb-kit";
import { afterAll, beforeAll, describe, expect, it } from "vitest";
import { launchPlaywrightEngine, type PlaywrightBrowserEngine } from "../../src/browser/playwright-engine.js";
import { runChain } from "../../src/executor/run-chain.js";
import { browserRecycleReason } from "../../src/memory/recycler.js";
import { readRssBytes } from "../../src/memory/rss.js";
import { MEMORY } from "../../src/memory-governance.js";
import { ScreenshotRing } from "../../src/artifacts/screenshot-ring.js";
import { buildSyntheticPlan, SYNTHETIC_URL } from "./synthetic-app.js";

const CHAINS = Number(process.env["TESTKITE_SOAK_CHAINS"] ?? 200);
const STEPS = 8;
const MB = 1024 * 1024;

/** A no-op verb: the soak measures the FLEET, not Playwright's click implementation. */
const soakVerb: VerbDefinition = {
  opKey: "soak.noop",
  sentence: "Click on {element}",
  params: [],
  needsRendering: false,
  execute: async (ctx) => {
    const page = ctx.page as { goto: (u: string) => Promise<unknown> };
    await page.goto(SYNTHETIC_URL);
    return { ok: true };
  },
};

let engine: PlaywrightBrowserEngine;
const scratch = mkdtempSync(join(tmpdir(), "tk-soak-"));

beforeAll(async () => {
  // Root dev/CI cannot launch a sandboxed chromium (§ đính chính 30-08): ask for the opt-out
  // explicitly here, and read every number below as an UNSANDBOXED measurement.
  const sandboxed = process.getuid?.() !== 0;
  engine = await launchPlaywrightEngine(
    sandboxed ? { traceDir: scratch } : { traceDir: scratch, sandbox: "off-root-dev-only" },
  );
});

afterAll(async () => {
  await engine.close();
});

describe.skipIf(process.env["TESTKITE_SOAK"] !== "1")(`soak ${CHAINS} synthetic chains`, () => {
  it("holds every RSS ceiling, leaks no context and leaves no orphan chromium", async () => {
    const plan = buildSyntheticPlan(CHAINS, STEPS);
    const nodeRssStart = readRssBytes(process.pid) ?? 0;
    let browserTreePeak = 0;
    const durations: number[] = [];
    let recycles = 0;
    let browserStartedAt = Date.now();
    let contextsOnThisBrowser = 0;

    for (const chain of plan.chains) {
      const started = Date.now();
      const ring = new ScreenshotRing({ dir: scratch, capacity: 50, policy: "failure" });
      const outcome = await runChain(chain, plan.policy, {
        engine,
        resolveVerb: () => soakVerb,
        now: () => Date.now(),
        onStep: () => {},
        log: () => {},
        screenshot: async (handle, step) => (await ring.push(step.ordinal, await handle.screenshotWebp()))?.sha256 ?? null,
      });
      await ring.discard();
      expect(outcome.verdict).toBe("passed");
      durations.push(Date.now() - started);

      contextsOnThisBrowser += 1;
      browserTreePeak = Math.max(browserTreePeak, engine.treeRssBytes());

      const reason = browserRecycleReason(
        { contextsServed: contextsOnThisBrowser, startedAtMs: browserStartedAt, rssBytes: engine.treeRssBytes(), crashed: engine.crashed() },
        Date.now(),
      );
      if (reason !== null) {
        recycles += 1;
        contextsOnThisBrowser = 0;
        browserStartedAt = Date.now();
      }
    }

    // Let the kernel reap: the 2026-08-29 spike saw 2 "orphans" when measured immediately and 0
    // after 1.5s. Asserting without a settle window produces a FALSE red.
    await new Promise((r) => setTimeout(r, 1_500));

    const nodeRssEnd = readRssBytes(process.pid) ?? 0;
    const report = {
      chains: CHAINS,
      nodeRssStartBytes: nodeRssStart,
      nodeRssEndBytes: nodeRssEnd,
      browserTreeRssPeakBytes: browserTreePeak,
      msPerChainP50: [...durations].sort((a, b) => a - b)[Math.floor(durations.length / 2)] ?? 0,
      recycles,
    };
    console.log(`SOAK REPORT ${JSON.stringify(report)}`);

    // 1. Node's RSS floor must not creep: this is the old system's slow death, made impossible.
    expect((nodeRssEnd / nodeRssStart) * 100).toBeLessThan(MEMORY.recycle.containerRssFloorGrowthPct);
    // 2. The browser tree never approaches the L1 container ceiling.
    expect(browserTreeRssPeakBytesUnder(browserTreePeak)).toBe(true);
    // 3. No context survives its chain.
    expect(engine.contextRssBytes("any-closed-context")).toBe(0);
  });
});

function browserTreeRssPeakBytesUnder(peak: number): boolean {
  return peak < MEMORY.containerLimitMb.batch * MB;
}
```

- [x] **Step 3: Chạy soak rút gọn để xác nhận nó CHẠY (không phải chỉ biên dịch)**

```bash
cd testkite
TESTKITE_SOAK=1 TESTKITE_SOAK_CHAINS=20 PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers \
  pnpm --filter @testkite/runner test:soak
```
Expected: PASS, in ra một dòng `SOAK REPORT {...}`. Đối chiếu với mini-soak spike (30 chain, node 87.0→95.3MB, cây chromium ~212MB, 750ms/chain): cùng bậc độ lớn thì đúng.

- [x] **Step 4: Chạy soak đầy đủ 200 chain**

```bash
cd testkite
TESTKITE_SOAK=1 PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers pnpm --filter @testkite/runner test:soak
```
Expected: PASS. **Dán nguyên văn dòng `SOAK REPORT` vào phần "Kết quả nghiệm thu" của `tasks/M3-orchestration-fleet.md`** — đây là bằng chứng cho exit criteria "24 context song song chạy hết đêm synthetic không OOM host".

- [x] **Step 5: Thêm job nightly vào CI**

```yaml
  fleet-soak:
    # Nightly only: 200 chains takes minutes and its value is TREND, not per-commit signal.
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: corepack enable
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: corepack prepare pnpm@9 --activate
      - name: pnpm install (frozen lockfile)
        working-directory: testkite
        run: pnpm install --frozen-lockfile
      - name: Install chromium-headless-shell
        working-directory: testkite
        run: pnpm --filter @testkite/runner exec playwright install --with-deps chromium-headless-shell
      - name: Soak 200 synthetic chains
        working-directory: testkite
        env:
          TESTKITE_SOAK: '1'
        run: pnpm --filter @testkite/runner test:soak
```

Thêm trigger lịch ở đầu workflow (cạnh `on: push/pull_request`):

```yaml
  schedule:
    - cron: '0 2 * * *'
```

**Số đo thật của Step 4 (31-08-2026, sandbox 4 vCPU, chromium KHÔNG sandbox vì chạy root) —
dòng này là bằng chứng T20 phải dán vào `tasks/M3-orchestration-fleet.md`:**

```
SOAK REPORT {"chains":200,"nodeRssBootBytes":123645952,"nodeRssStartBytes":153493504,"nodeRssEndBytes":161300480,"nodeRssFinalBytes":161488896,"browserTreeRssPeakBytes":240046080,"orphanChromiumAfter":0,"contextsLeaked":0,"msPerChainP50":877,"recycles":4}
```

Đọc: sàn RSS node 153,5MB → 161,3MB = **105,1%** (trần 130%); đỉnh cây chromium 240,0MB
(trần L1 batch 3072MB); **0 orphan** sau settle 1,5s qua **5 lần browser chết** (4 recycle theo
`browserRecycleReason` + lần đóng cuối); **0 context rò** trên cả 200 chain; 877ms/chain,
tổng 178,7s. Ba lần chạy 200 chain liên tiếp cho 105,5% / 105,1% / 103% — số này lặp lại được.

**GHI CHÚ THỰC THI T19 (31-08-2026) — soak ĐÃ TÌM RA MỘT BUG THẬT, và 3 chỗ lệch spec có chủ đích:**

1. **Rò bộ nhớ THẬT trong `apps/runner/src/executor` (đã sửa trong chính commit này).** Soak 200
   chain cho sàn RSS node bò 0,55MB/chain. Heap snapshot diff (chain 50 vs 150) chỉ đúng thủ phạm:
   MỖI CHAIN GIỮ LẠI NGUYÊN MỘT RunPlan đã parse. Chuỗi giữ tham chiếu: `setTimeout` của
   `Promise.race` không bao giờ được `clearTimeout` → closure → `reject` → promise → frame đang
   chờ → `deps` → closure `onStep` của worker → `ClaimedJob` → `plan`. 9 timer/chain (8 step +
   1 chain), sống tới 60s/180s. `unref()` KHÔNG cứu được: nó chỉ ngăn timer giữ event loop, không
   ngăn giữ bộ nhớ. Sửa: `raceDeadline()` trong `executor/timeouts.ts` (clear trong `finally`),
   dùng ở `step-runner.ts` + `run-chain.ts`; test ĐỎ trước ở `test/executor/run-chain.test.ts`
   ("leaves no timer pending", dùng `vi.getTimerCount()` — ĐỎ = 4 timer treo). Sau khi sửa: sàn RSS
   phẳng (149→159MB trong 190 chain).
2. **Soak chạy qua `Worker` + `FakeControlPlane` (HTTP thật) chứ không gọi thẳng `runChain`** như
   block code spec. Lý do: hình thái nghiệm thu ở đầu mục này ghi rõ "FakeControlPlane + chromium
   thật"; đi qua worker mới phủ được ring/trace/scratch/heartbeat — và nó bắt ngay một lỗi mà bản
   gọi thẳng `runChain` không thể thấy: `completedStepSchema.caseId` là `z.string().uuid()`, nên
   `caseId: "soak-case-7"` của spec sẽ ăn 400 VALIDATION_FAILED ở mọi `complete`. `buildSyntheticPlan`
   vì thế sinh uuid thật và dựng plan bằng `freezePlan` của compiler (thay cho
   `as unknown as RunPlan`), để plan có đúng `stepCount`/`timeoutSeconds`/`contentHash` như plan thật.
3. **Cách ĐO sàn RSS, KHÔNG phải ngưỡng, được định nghĩa lại.** Ngưỡng vẫn nguyên
   `MEMORY.recycle.containerRssFloorGrowthPct` = 130%. Hai thay đổi về phép đo, cả hai đều theo đúng
   định nghĩa của `containerRecycleReason` ("RSS đo GIỮA các job, khi không còn giữ gì"):
   (a) mốc gốc lấy sau chain warm-up đầu tiên — số đo lúc boot chưa trả chi phí một-lần của JIT/V8
   heap/HTTP client/context đầu tiên (125MB boot → 149MB sau 10 chain rồi phẳng; tính cả đoạn đó là
   báo "rò" 133% cho một sàn thực chất phẳng);
   (b) mỗi đầu tỉ số là TRUNG VỊ 10 mẫu liên tiếp, vì một mẫu đơn không phải là sàn — cùng một vị trí
   chain đo được 157MB ở lần chạy này và 218MB ở lần chạy khác (rác chưa kịp GC quanh lúc recycle
   browser). Trung vị miễn nhiễm với gai đó nhưng vẫn bắt được rò 0,55MB/chain (tách hai cửa sổ
   ~100MB). `nodeRssBootBytes` + `nodeRssFinalBytes` vẫn được in ra để người đọc tự kiểm chứng.

- [x] **Step 6: Commit** — kèm luôn fix rò bộ nhớ ở `executor/timeouts.ts` (xem ghi chú trên)

```bash
git add apps/runner/test/soak/ ../.github/workflows/testkite-ci.yml
git commit -m "M3-fleet T19: soak 200 synthetic chains — RSS ceilings, zero orphans, nightly CI"
```

---

## Task 20 — Tick backlog M3 (phần fleet)

- [ ] **Step 1: Tick các dòng thuộc nửa fleet trong `testkite/tasks/M3-orchestration-fleet.md`**

Tick 5 dòng sau kèm hash commit tương ứng (các dòng còn lại thuộc plan orchestration, **không đụng vào**):

- `Memory governance L1–L3` → T3–T7
- `Quarantine chain sau 2 OOM + breaker` → T8
- `Executor: chạy RunPlan trên Playwright…` → T9–T12
- `Presigned PUT cho artifact; trace retain-on-failure; screenshot ring-buffer NVMe` → T13, T14
- `Fleet systemd 2 host` → T17, T18
- `Soak thử: 200 chain synthetic` → T19

Với dòng `Worker (apps/runner)` và `CI gate` — tick phần fleet, ghi rõ phần control-plane do plan kia chịu.

- [ ] **Step 2: Ghi số soak thật vào backlog**

Dán dòng `SOAK REPORT {...}` từ T19 Step 4 vào cuối `tasks/M3-orchestration-fleet.md`, mục "Exit criteria", dưới dạng bằng chứng đã đo.

- [ ] **Step 3: Commit**

```bash
git add tasks/M3-orchestration-fleet.md
git commit -m "M3-fleet T20: tick backlog fleet + soak evidence"
```

---

## Self-Review

**1. Spec coverage — blueprint §1 + §5 + §5.1 + §5.2 và nhiệm vụ 1–7 của milestone:**

| Yêu cầu | Task |
|---|---|
| Worker vòng đời claim → chạy chain → báo cáo | T16 |
| Zero-credential: không credential team, chỉ run-scope token | T1 (schema env cấm DB url), T15 (hai token, run token cho mọi mutation job), T14 (không ký S3) |
| Epoch trên MỌI mutation; 409 STALE_EPOCH ⇒ dừng, không ghi gì | T15 (client ném `StaleEpochError`, không retry), T16 (test: không complete, không fail, không upload) |
| Executor RunPlan trên chromium-headless-shell | T11 (logic), T12 (engine thật) |
| 1 BrowserContext / 1 chain, đóng trong `finally` | T11 (3 test riêng: throw, timeout, thành công) |
| Timeout lồng action<nav<step<chain, công thức compiler | T9 (`budgetForChain` + `assertNested`, tái dùng `chainTimeoutSeconds`) |
| AssertionFailure ⇒ failed, KHÔNG retry; chỉ infra mới retry | T9 (`classifyError`), T11, T16 |
| L1 trần container | T1 (`containerLimitBytes` từ MEMORY), T18 (manifest + `--memory 3g`) |
| L2 cgroup lồng `memory.max = container − 400MB` | T3 (`browserCgroupLimitBytes`), T16 main wiring |
| L2 `oom_score_adj` node −500 / chromium +500 | T4 (hai lời gọi tách rời + ranh giới CAP_SYS_RESOURCE) |
| L3 per-context 350/500MB poll 5s | T5 |
| Shed 75/85/92% | T6, T16 (shed TRƯỚC khi claim) |
| Recycle theo ngưỡng | T7, T19 (áp trong soak) |
| Đọc `memory.events` ⇒ tự báo `browser_oom` kèm peakRss | T3 (parse), T4 (delta + error), T16 (nâng cấp báo cáo sau infra failure) |
| Quarantine 2 OOM ⇒ cách ly; breaker khi fleet ốm; poison-chain alert | T8, T16 |
| `ts-workers.slice` MemoryHigh 80/Max 88 | T18 |
| `ts-worker@` Restart=always, OOMPolicy=continue | T18 |
| `runnerd` register/heartbeat/PSI/drain | T17, T18 (unit) |
| Trace retain-on-failure | T11 (`trace: true` luôn bật), T12 (`stopTracing(null)` = huỷ), T16 |
| Screenshot ring-buffer NVMe + upload-on-failure qua presigned PUT | T13, T14, T16 |
| Nén WebP q70 + dedup content hash | T12 (CDP WebP), T13 (dedup SHA-256) |
| Soak 200 chain: RSS ceilings giữ, 0 orphan, API RSS phẳng | T19 + gate ảnh API T18 |
| Ảnh API không chứa browser (bất biến M1) | T18 Step 9 (gate mở rộng: playwright CHỈ ở runner) |
| Worker không import module core; nói chuyện qua internal HTTP | T1 (lint rule), T15 |
| Hợp đồng control plane cô lập một file | §"Hợp đồng giả định" + T15 |

**Ngoài phạm vi có chủ đích (không phải thiếu sót):**
- `job_runs` queue, dispatcher FIFO/leader-elect, internal API server, results 3 tầng, SSE run status, reaper heartbeat 15s/30s — **thuộc plan `2026-08-29-m3-orchestration.md`** (nửa control plane).
- Thân thật của `web.click`/`web.enter` và 33 verb còn lại — **M4** (verb-kit hiện còn `TODO(M4)`); executor gọi qua registry nên không chặn.
- Step kind `if`/`while`/`rest` — **M4**, T11 ném lỗi fatal gọi đúng tên kind thay vì bỏ qua im lặng.
- Live View CDP `Page.startScreencast` (§5.1) — **P2**, không nằm trong M3.
- Thumbnail/ThumbHash/AVIF cold-tier + tar bundle per-chain (§5.2 tầng nén sâu) — **cold job trên control plane**, không bao giờ trên worker.
- gVisor tier 1 / egress netns+Envoy — **hardening bậc thang**, M5+.
- Work-stealing giữa lane, fair-share DRR — **M5**.

**2. Quét placeholder:** không có "TBD" / "tương tự Task N" / "thêm xử lý lỗi phù hợp". Mọi step code có block code thật; mọi step chạy có lệnh + kết quả mong đợi cụ thể. Dòng `CONTRACT SYNC` ở T15 là **hành động có địa chỉ** (đối chiếu với plan orchestration trước nghiệm thu M3), không phải TODO mở.

**3. Nhất quán kiểu:**
- `MemoryLimiter`/`MemorySnapshot` khai ở T3, dùng nguyên tên ở T4 (`OomReporter`), T16 (main).
- `readRssBytes`/`sumRssBytes` (T2) dùng ở T12 (`treeRssBytes`, `contextRssBytes`), T17 (`selfRssBytes` của runnerd), T19.
- `TimeoutBudget` (T9) là tham số của `ChainContextOptions` (T10) và `runChain` (T11).
- `EngineContextHandle` (T10) là kiểu tham số của `screenshot(handle, outcome)` trong `RunChainDeps` (T11) và của `StepRunnerDeps.handle`.
- `StepOutcome` khai ở T11 (`step-runner.ts`), re-export qua `run-chain.ts`, dùng ở T16.
- `PresignedTarget` khai ở T14 (`uploader.ts`), trả về bởi `artifactTicket` ở T15 — một định nghĩa, hai nơi dùng.
- `ClaimedJob`/`StaleEpochError` (T15) dùng ở T16; `RunnerdClient` (T17) là **tập con hẹp** của `ControlPlaneClient` nên `HttpControlPlaneClient` thoả cả hai.
- `ScreenshotPolicy` lấy từ `@testkite/run-compiler` (T13) — cùng union `all|failure|none` với `RunPolicy.screenshots`.
- `MEMORY` là nguồn duy nhất: T1, T3, T5, T6, T7, T8, T9, T18 (manifest test), T19 (ngưỡng soak) đều đọc từ đó.

**4. Rủi ro đã biết + đối sách nằm trong plan:**

| Rủi ro | Đối sách |
|---|---|
| Sandbox không có cgroup v2 memory ⇒ L2 không kiểm chứng được | `MemoryLimiter` 2 impl; logic test trên fake; `test:host` gate riêng; cơ chế đã chứng minh qua v1 trong spike (dán output) |
| `oom_score_adj −500` cần CAP_SYS_RESOURCE | Trả `"applied"|"denied"` thay vì ném; log cảnh báo rõ "node is NOT protected"; hai lời gọi tách rời |
| `systemd-analyze verify` **exit 0** dù có typo directive | `verify-units.sh` bắt lỗi theo OUTPUT + T18 Step 8 có bước **kiểm chứng gate bắt được lỗi thật** |
| Hợp đồng internal API do plan khác sở hữu, viết song song | Mọi giả định nhốt trong `control-plane-client.ts` + `fake-control-plane.ts`; §"Hợp đồng giả định" liệt kê endpoint/payload/mã lỗi để đối chiếu; T15 có bước CONTRACT SYNC |
| `Promise.race` không huỷ action Playwright ⇒ tưởng đã dừng mà vẫn chạy | Đo trong spike §10; `finally { close() }` là bắt buộc, có 2 test riêng cho throw và timeout |
| Assertion soak đỏ giả vì chromium chưa kịp reap | Spike đo: 2 orphan lúc đo ngay, 0 sau 1.5s ⇒ soak có settle window, ghi rõ lý do trong test |
| Dedup 96.3% của spike là trang synthetic gần bất động | Plan chỉ khẳng định cơ chế đúng (test: 8 frame giống nhau ⇒ 1 blob); tỉ lệ thật giữ theo blueprint 30–50%, không hứa quá |
| Worker biến thành "mini control plane" theo thời gian | Lint rule cấm `@testkite/core`/driver DB trong `apps/runner/src`; env schema không có `DATABASE_URL`; test khẳng định điều đó (T1) |
| Browser lọt vào ảnh API | Gate CI mở rộng (T18 Step 9): playwright chỉ được xuất hiện trong `apps/runner`, và `apps/runner` **phải** có nó |
| Zombie ghi verdict sau khi lease bị reap | `StaleEpochError` không bao giờ retry (T15 test), worker dừng ngay và không upload gì (T16 test) |
| `if`/`while`/`rest` chưa chạy được mà im lặng bỏ qua | Ném `FatalInfraError` gọi đúng tên kind (T11), có test riêng |
| Fleet ốm ⇒ quarantine nhầm toàn bộ backlog khách | `FleetBreaker` mở ⇒ ngừng quarantine, đổi alert sang `fleet-unhealthy` (T8) |
| Ngưỡng bộ nhớ bị sao chép rồi lệch | Mọi ngưỡng đọc từ `MEMORY`; `manifest.test.ts` (T18) khẳng định manifest khớp hằng số; CI gate manifest |

**5. Thứ tự thực thi đề xuất:** T1 → T2 → T3 → T4 → T5 → T6 → T7 → T8 (thuần, song song được sau T1) → T9 → T10 → T11 → T12 → T13 → T14 → **T18** (systemd, độc lập, làm sớm để review sớm) → T15 → T16 → T17 → T19 → T20. T15/T16/T19 là ba task duy nhất cần đối chiếu với plan orchestration trước khi nghiệm thu M3.
