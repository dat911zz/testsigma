{
  "name": "@testkite/contract",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "openapi:gen": "tsx src/openapi.gen.ts"
  },
  "dependencies": {
    "zod": "^3.24.1",
    "zod-openapi": "4.2.4"
  },
  "types": "src/index.ts",
  "devDependencies": {
    "tsx": "^4.23"
  }
}
