/**
 * @testkite/ui — React 19 + Vite. A STRUCTURED step builder for no-code QA
 * (verb autocomplete from action_catalog + an element picker — NOT contentEditable).
 * TODO(M3-M4): scaffold Vite + a ThumbHash lazy-loading gallery (blueprint §5.2).
 */
export const APP = "testkite-ui" as const;
