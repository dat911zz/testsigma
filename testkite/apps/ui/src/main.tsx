/**
 * @testkite/ui — React 19 + Vite. Step-builder có cấu trúc cho QA no-code
 * (autocomplete verb từ action_catalog + element picker — KHÔNG contentEditable).
 * TODO(M3-M4): scaffold Vite + gallery ThumbHash lazy-load (blueprint §5.2).
 */
export const APP = "testkite-ui" as const;
